# Changelog

All notable changes to Mediforce.

Format: [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).
Weekly cuts via [`/add-changelog-entry`](skills/add-changelog-entry/SKILL.md) + auto-cut every Monday 09:00 CET ([`.github/workflows/changelog-cut.yml`](.github/workflows/changelog-cut.yml)).

Every non-trivial PR adds a bullet under `## [Unreleased]`. Trivial edits (typos, single-line config, comment-only diffs) may be omitted. Renovate bumps batch under `### Dependencies`.

---

## [Unreleased]

## [2026-09-20]

### Added
- New workspaces catalogue the engine’s five default step images — each named after the image itself (`mediforce-node`, `rocker/r-ver`), the way a hand-catalogued row is, with the sentence beside it carrying the meaning — existing workspaces can be backfilled with `mediforce images seed` (which writes only the defaults a workspace is missing and reports that count), and shared defaults are protected from daemon deletion [#1379](https://github.com/Appsilon/mediforce/pull/1379).
- The AI Assistant takes standing instructions from the person using it. The gear in the pane now holds a text box for how *you* want workflows built in this workspace — naming, house defaults, the shapes you always reach for — and every turn there reads it, the planning turn included. It is stored server-side per `(workspace, user)` (migration `0050`), not in the browser and not in the request: it holds for a whole session, is the same on every machine you sign in from, and is still there the next morning, which is the thing a paragraph pasted into the chat could never be. Per user as well as per workspace, because two people editing the same canvas rarely want the same answer, and the same person keeps different conventions in different workspaces; the row is private to its author among signed-in people, and a member of a shared workspace cannot read it — the gate is on both keys, so another member's text is not reachable rather than merely not shown. The deployment API key is the documented exception: it names the uid it acts for, which is what makes the CLI path below possible, and is god-mode by design (ADR-0004 §6). It refines choices, it does not relax rules: the framing says so, and an instruction that could only be followed by producing a workflow that fails the graph or schema gates loses and is named in the reply, because a canvas that cannot be saved is worse than a convention not honoured. The text is sent **after** the static system prompt and before the canvas, so the tens of kilobytes that are identical every turn stay a byte-identical prefix — the precondition for prompt caching, which this deployment does not yet ask for (no `cache_control` breakpoint is set on any OpenRouter call). Keep it in a file and push it — `mediforce assistant instructions-set --namespace <handle> --file ./conventions.md` (also `--stdin`, `--clear`, and `instructions-get` to read it back); an apiKey caller passes `--uid` because a system actor has no identity of its own, the same convention `users me` uses. An apiKey-authenticated assistant turn reads no instructions at all, for the same reason.
- A join link can be handed to a room as a QR code. The one-time "Join link created" card in **Workspace settings → Join links** now shows a QR code next to the URL, with **Present** (a full-screen black-on-white view with the workspace name, code and URL, for a projector) and **Download PNG** (for a slide). The code is generated in the browser, not by a hosted QR image service, because the URL contains the plaintext token. Like the URL, it can only be shown while the card is open: a lost code means making a new link. [#1354](https://github.com/Appsilon/mediforce/pull/1354)
- **A registry image can be pulled and catalogued from the browser** [#1372](https://github.com/Appsilon/mediforce/pull/1372). **Workspace → Images → Add image → Registry image** and `mediforce images pull --reference R [--tag T]` run `docker pull` on the deployment's daemon (the worker's new `POST /images/pull`, or in-process when the daemon is local) and catalogue the result as a `referenced` entry, so a Docker Hub or `ghcr.io` image no longer needs a host shell or a first workflow run to reach the step picker. It is the upload path with the build swapped for a pull — `addReferencedVersion` now implements both — so the rules match: the first pull needs the intent, a later tag only adds a version, a tag already on the daemon is refused. References are stored as the daemon lists them (`docker.io/library/python` → `python`), and one under another workspace's handle is refused so a registry image cannot land as that workspace's version ([ADR-0022](docs/adr/0022-image-catalog.md), amended).
- **An image the daemon already holds can be catalogued with no rebuild.** Every existing "create from what's already there" path required either build labels the platform itself wrote (Describe) or a fresh build to get one (Upload, Publish) — an image pulled or built by hand on the host had no way in at all. **Admin → Infrastructure** now offers a `+` on any daemon row with no catalog match (beside the `Layers` icon that already links a matched row to its entry), and **Workspace → Images → Add image** gains a third tab, **Existing image**, that picks from every daemon repository no entry yet describes — grouped by repository, since a `referenced` entry is keyed on it and every tag it has, now or later, resolves as one of its versions. Both open the same form and call the existing `createImageCatalogEntry` unchanged, with `source: { kind: 'referenced', reference: '<repository>' }` — no new handler, no new contract [#1370](https://github.com/Appsilon/mediforce/pull/1370).
- The AI Assistant plans before it builds. Sending a message now runs a short planning turn first (`POST /api/workflow-assistant/plan`): it states what it is about to do in the user's terms, asks only what it genuinely cannot infer — each question prefilled with the answer it would otherwise have invented, so agreeing is one click — and names the phases this particular build will go through. A build was previously one long request whose result appeared all at once, so a wrong assumption cost a minute of building and a question could only be asked after the work. Nothing to ask is the common case for an edit: the plan is said in the conversation and the build starts. While it runs the pane shows those phases with the seconds elapsed, instead of five hardcoded phrases that had nothing to do with the workflow. A planning call that fails costs nothing — the build runs exactly as it did before.
- The AI Assistant challenges a design instead of transcribing it, which is the judgment the `/design-workflow` skill applied and the in-app assistant did not. Three checks before it builds: whether the job needs a workflow at all (fetch → transform → write with no judgment and no branching is one script on a cron trigger, not five steps); whether a step called an "agent" is really deterministic work that belongs in a `script`, or a side effect that belongs in an `action` — a model doing a parser's job is slower, costs money every run and is occasionally wrong in ways a parser never is; and where code lives, with the first-run build cost of a Dockerfile stated before one is written. It also has to resolve the whole design — trigger contract, human decisions, branches, carry-over, secrets by name, files, image, skills, saved agents — and to look the last of those up (`list_secrets`, `list_agents`, `list_tool_catalog`) rather than inventing what already exists. For a build past about five steps it states its plan in two or three lines first, so a wrong assumption costs a sentence rather than a rebuild.
- The AI Assistant can see and set up the platform around a workflow, not just the workflow: `list_secrets` (key names only), `list_agents`, `list_tool_catalog`, `create_agent` and `create_tool_catalog_entry`. A step needing a saved agent no longer means leaving the assistant for the Agents page and coming back. Every one of these runs **as the person asking**, through their own `CallerScope` — there is no service account and no elevated path — so a refusal comes back as a result the assistant relays ("an admin has to do this") instead of an exception that kills the turn or a way around the gate. Secret *values* are deliberately not writable: a value typed into the chat reaches the model, the provider and the transcript, so the assistant names the key and the Secrets tab (which already lists a workflow's referenced keys) is where it is set. The two writes go through the same handlers the Agents page and the Tool Catalog page use, so what the assistant creates is validated identically and lands in the audit trail — an agent that appeared from nowhere is worse than no agent. The Tool Catalog is admin-only, and that gate holds for the assistant: a member is told who can add the server.
- A workflow carries its own files. **Files** in the version editor holds the scripts a step runs, the Dockerfile an image is built from and the skills an agent reads; the AI Assistant writes them one file at a time (`write_workflow_file`), and a run reads them from `/artifacts`. They are part of the definition, so they version with it, a rollback brings back the files that version ran with, and copy, paste and export carry them with no second mechanism. That closes the last thing that forced an author out to a git checkout: `skillsDir` resolved against a repository on the host, so anything past an inline script needed one. A carried skills directory now wins over `externalSkillsRepo` for the directory it holds, and a workflow may still take its skills from a repo while carrying its Dockerfile. Text only and capped (64 KiB a file, 256 KiB the set) so a definition row stays a definition row — data and installed dependencies belong in an image. A carried `Dockerfile` is a build source too: a step naming it builds from the materialized files with no clone, tagged by their content, so "it needs pandas and R" is answerable in the app rather than by a checkout. `stepHasBuildSource` is now one predicate shared by the register handler and the preflight checks — a step with a carried Dockerfile used to be handed the golden image, which would have built this workflow's image under the shared golden tag. Files come from disk as well as the editor: pick them, pick a whole folder (its structure is kept, so an uploaded `skills/` directory still resolves), or drop them on the panel. Text only — a binary is refused by name with the reason and the rest of the batch still lands, since a PNG or a wheel stored as mangled text would fail at run time instead. **Files** and **Secrets** are icons in the toolbar now, beside Notifications and Advanced. A definition that names a file it does not carry is flagged before the run, beside the missing-secret and missing-image warnings: a step command reading `/artifacts/<path>` with no such file, or a `dockerfile` with nothing to build it from. `skillsDir` is deliberately not flagged — with no carried skills and no `externalSkillsRepo` it resolves against the repository mounted on the host, which the browser cannot see, so the warning would fire on every workflow that ships in this repo. Skills carried this way are picked by name on an agent step, which fills in the directory the runtime reads them from — `agent.skill` and `agent.skillsDir` were two hand-typed repository paths, which is why an uploaded skill previously had nowhere to land.
- The definition-level fields that had no surface anywhere are editable, each where it is already explained. The **Input** section on a workflow's **Triggers** tab authors what the workflow accepts when it starts: every trigger on that tab is validated against it, the cron payload editor labels itself from it and the webhook example body is generated from it, so the contract is edited beside the forms it drives. Saving there registers a new version of the version a firing resolves, and moves the default pointer with it when one is set, so the edit takes effect on the next firing instead of landing on a version nothing runs; when the default is pinned to an older version the fields stay read-only and say where to change them. In the version editor, two toolbar icons cover the rest: **Advanced** holds the preamble added to the start of every agent prompt, and a **bell** holds notifications; Escape closes either one. The source panel's error pointed at "workflow settings" and the Triggers tab told the reader to define `triggerInput` in the definition; neither place existed. Environment variables, the git repositories and `url` deliberately have no control anywhere: the Secrets tab already parses a pasted `.env`, and the import dialog collects the repository the workflow page then renders. The assistant and a pasted definition still write all of them.
- The AI Assistant can author the workflow level and conditional routing. Its three tools were all step-scoped, so "make the study ID a required input", "add these house rules to every agent step" and "only escalate when severity is high" had nowhere to land however they were phrased. `update_workflow` patches the workflow-level fields — naming one field leaves the others alone — and `set_transition_condition` writes `when` on an existing edge, refusing one that does not exist rather than inventing it. `stepParams` is no longer hidden from the step tools: its comment called it legacy, but [`execute-agent-step.ts`](packages/platform-ui/src/lib/execute-agent-step.ts) merges it into the agent's input context. The assistant is now sent the current settings with the canvas, so a patch starts from what is set rather than blind.
- **A Dockerfile can build from a context wider than its own directory.** A build-mode step, a catalog entry and `mediforce images build` all took a Dockerfile path and always used the directory it sits in as the build context, so a `container/Dockerfile` that `COPY`s `scripts/` failed with `"/scripts": not found` and there was no way to fix it short of moving files. Steps (`agent.context` / `script.context`), **Add image**, **Edit**, **Build** and `mediforce images create|update|build --context` now take an optional `context` — a directory from the repo root, which `dockerfile` is then read from, as docker-compose reads it; `"context": "."` is the repo root. With no context, nothing changes: same context, same `mediforce-built:*` tag, same catalog entry id. `deriveBuildTag` folds the context in only when it is set — normalised, so `.`, `./` and `/` share one tag — since two builds of one Dockerfile from different contexts are different images and must not share a cache slot. Every build now writes a `mediforce.build.context` label, empty when it named none, because labels are inherited and an image built `FROM` one that named a context would otherwise claim it. The catalog key leaves the context out ([ADR-0022](docs/adr/0022-image-catalog.md), amended): the key is the Dockerfile's path from the repo root, so the same file built from two contexts is one entry with versions of both. `dockerfile` is read from the context, though, so a change that makes the same path name a different file (`Dockerfile` with context `container` is `container/Dockerfile`) does move the entry, and **Edit** warns only then. A context or Dockerfile that leaves the repository is refused: by the contract as a 400 for a path that climbs out with `..`, and by both builders after cloning for one the checkout reaches through a symlink — `docker build` follows a symlinked context, so `ctx -> /` would otherwise send the build host's filesystem, deploy key included, into an image. The build-failure hint names the setting now that the fix exists.
- **An image can be built from a local folder, with no repo behind it** [#1345](https://github.com/Appsilon/mediforce/issues/1345): `mediforce images build --reference <handle>/<name> --context ./dir` and **Workspace → Images → Add image → Local folder** upload the folder (less what its `.dockerignore` excludes), build it and catalogue it as a `referenced` entry; the reference must start with the workspace handle and a tag already on the daemon is refused, never replaced ([ADR-0022](docs/adr/0022-image-catalog.md), amended).
- **A Dockerfile can build from a context wider than its own directory.** A build-mode step, a catalog entry and `mediforce images build` all took a Dockerfile path and always used the directory it sits in as the build context, so a `container/Dockerfile` that `COPY`s `scripts/` failed with `"/scripts": not found` and there was no way to fix it short of moving files. Steps (`agent.context` / `script.context`), **Add image**, **Edit**, **Build** and `mediforce images create|update|build --context` now take an optional `context` — a directory from the repo root, which `dockerfile` is then read from, as docker-compose reads it; `"context": "."` is the repo root. With no context, nothing changes: same context, same `mediforce-built:*` tag, same catalog entry id. `deriveBuildTag` folds the context in only when it is set — normalised, so `.`, `./` and `/` share one tag — since two builds of one Dockerfile from different contexts are different images and must not share a cache slot. Every build now writes a `mediforce.build.context` label, empty when it named none, because labels are inherited and an image built `FROM` one that named a context would otherwise claim it. The catalog key leaves the context out ([ADR-0022](docs/adr/0022-image-catalog.md), amended): the key is the Dockerfile's path from the repo root, so the same file built from two contexts is one entry with versions of both. `dockerfile` is read from the context, though, so a change that makes the same path name a different file (`Dockerfile` with context `container` is `container/Dockerfile`) does move the entry, and **Edit** warns only then. A context or Dockerfile that leaves the repository is refused: by the contract as a 400 for a path that climbs out with `..`, and by both builders after cloning for one the checkout reaches through a symlink — `docker build` follows a symlinked context, so `ctx -> /` would otherwise send the build host's filesystem, deploy key included, into an image. The build-failure hint names the setting now that the fix exists. **Add image** and **Edit** show the Dockerfile and build context the build will use, from the repo root, as you type — so a Dockerfile path that repeats the context (`apps/x` with `apps/x/container/Dockerfile`) reads `/apps/x/apps/x/container/Dockerfile` before a build fails on it — and the prose rule under the fields is gone. The intent field is labelled **Description** in both dialogs.
- A room can be onboarded with one link. **Workspace settings → Members → Join links** mints one — or `mediforce namespace create-join-link <handle> [--expires-in-days 7] [--max-uses 30]` — and whoever holds it joins at `/join/<token>` by entering their email ([ADR-0021](docs/adr/0021-workspace-join-links.md)); `list-join-links` and `revoke-join-link` complete the set, and `mediforce users invite` finally exists too. The load-bearing decision is that **redeeming does not open a session**: it calls the same [`seedMemberAndNotify`](packages/platform-api/src/handlers/users/seed-member.ts) as the admin invite path and sends the same activation email, so a link on a slide — or a photograph of that slide — is never itself a credential, and everyone who ends up in the workspace has a mailbox they demonstrably control. A link always grants the plain `member` seat: there is no membership input and no column, because the mailbox round-trip controls who gets the *session*, never who gets the *seat*. Promoting stays `mediforce namespace set-member-role`; revoking closes the entrance and removes nobody (`namespace remove-member`). Only the token's SHA-256 is stored (migration `0047`), so the CLI prints the URL once and then says it cannot be shown again. Organizations only. `claim` takes the row `FOR UPDATE`, so two attendees cannot both take the last seat of a capped link. The *token's* failure is named plainly (`expired`, `revoked`, `exhausted`) — the holder already has the secret — while the *email's* outcome never differs, so `/join` cannot be turned into an oracle for who is already a member.
- A join-link redemption may only **create**, never modify. Redemption is an unauthenticated write that takes whatever email is typed into the form, and routing it through the same [`seedInvite`](packages/platform-infra/src/auth/postgres-invite-service.ts) an admin invite uses handed any link holder three escalations [ADR-0021](docs/adr/0021-workspace-join-links.md) never granted: typing the workspace owner's address at a `member` link **demoted them to `member`** (the membership write is an upsert), redeeming an `admin` link against an existing member promoted them, and either stamped `invited_at` on an account the domain allowlist deliberately blocks — reaching around the control that column exists to preserve. All three were silent, because the anti-enumeration property makes the response identical whether or not the address is already a member. `SeedInviteInput` now carries a required `vouchedByAdmin` with no default, so a future caller must choose: `inviteUser` passes `true` and keeps today's behaviour exactly, `redeemJoinLink` passes `false` and may only insert. A brand-new address is still stamped — nobody held it, and the link it arrived through was minted by an admin. What stays accepted is bounded and named in the ADR: a holder can cause a new membership row plus one email for an address that is not theirs, gaining nothing by it, since a session still requires that mailbox.
- Public endpoints that send mail ship with their limit. `/api/join/redeem` is reachable with no credentials and sends an email on demand, which is the exposure that turns an endpoint into a mail relay, so [ADR-0021](docs/adr/0021-workspace-join-links.md) §6 required the limiter in the same commit as the route rather than in a follow-up. The ticket route's private per-uid bucket was generalised into a shared keyed limiter ([`@/lib/rate-limit`](packages/platform-ui/src/lib/rate-limit.ts)). Redemption carries two budgets, and each key deliberately drops half of what §6 first proposed ("keyed by IP plus token") — the pair is the one shape that bounds neither side and breaks the room the feature exists for. The address half is caller-controlled (`x-forwarded-for`), so rotating the header buys a fresh bucket; the token half is caller-supplied too, so a script sending a fresh invalid token per request resets the budget and charges the Postgres `claim` without bound; and a workshop behind one conference NAT is one address redeeming one token, so a per-(address, token) budget counts the whole room as a single attendee. What ships is per token **alone** (60/hour), the budget nothing a caller sends can move and the one that stops a leaked link becoming a mail relay, plus per address **alone** (120/hour), room-sized for the NAT case and unresettable by rotating tokens; preview, which sends no mail, gets the same 120/hour per address. `clientAddress` reads the **last** hop of `x-forwarded-for` rather than the first, since every proxy appends and anything earlier is what the client chose to send; `password-login`'s `clientIpFrom` still reads the first on purpose, because an audit record wants the conventional "original client" and understands it is spoofable — the two look alike and want opposite things, so they were not merged. Keys carry the token's SHA-256 rather than the token, the limiter sweeps expired buckets (a map keyed on anything caller-influenced otherwise grows one permanent entry per request), and budget is charged after parsing so a mistyped email cannot burn an honest attendee's attempts.
- **Delete** on an entry in **Workspace → Images** retires it and the images behind it. The view had no delete at all: `mediforce images delete` existed and **Admin → Infrastructure** could remove an image row, but from the catalog an entry was forever. **It is one act, not two.** An entry exists *for* its images, so removing the record while they stay achieves nothing: for anything this namespace built, the row is re-derived on the next read and comes back marked *Needs a description* ([ADR-0022](docs/adr/0022-image-catalog.md) decision 7), and the step-editor picker goes on offering it — the only thing lost is the sentence somebody wrote. So delete takes the images too, and when the daemon holds none for the entry it simply removes the record. **Admin or owner of the workspace**, for the whole act rather than only its destructive half: a record-only delete is not a lesser act deserving a lighter gate. Creating stays a member's right, and that asymmetry is recorded in the ADR as accepted.
  - **A live workflow version blocks the delete.** Live means the version a run actually starts from — `pickRunnableVersion`, the rule every firing resolves through: the **default** version when it is itself live, otherwise the newest non-archived one. Its author can still re-point that step, so nobody has to choose to break it: the delete is refused with a **409** naming the workflow, version and steps, and the dialog offers **Archive this version** in place, since retiring one version beats deleting a whole workflow to reclaim an image. It says which version runs fall back to, and a fallback pinning the same image blocks in turn. When the pinning version is the only runnable one the button reads **Archive workflow**, because that is what it does. A version the workflow pins as its *default* is not offered that button — archiving it would leave the workflow pointing at something that cannot run, a rule only the definitions UI enforced until now.
  - **A superseded or archived version does not block, and is listed anyway.** A registered version is immutable — there is no editing v2 in place — so refusing on its account would mean an image pinned once could never be reclaimed, which is the disk leak this feature exists to help with. Re-running one after the delete fails at container start, so the loss is shown before the click rather than discovered later.
  - **The judgement is deployment-wide, the disclosure is not.** The daemon is shared, so a step in a namespace the caller cannot read breaks just the same and blocks just the same; the 409 names only what that caller may already see and counts the rest. A new `listGroupsForImageAudit` is the deliberately unfiltered read behind it, and says so.
  - **Deletes by tag, not by image id**, so an artifact a second tag still references survives rather than needing a force that would destroy a version another entry offers. Images go before the row and all of them must go: Docker refuses an image a container is using or one another image was built on, and the entry is then kept, because it is the only handle anyone has on what is left behind.
  - `mediforce images delete` is the same write, with `--keep-images` for the rare record-only case; `mediforce system rmi` still removes one image by id or tag. Per-version garbage collection is still not offered — deleting is all of an entry's versions or none.
- **Edit** on any catalogued entry in **Workspace → Images** changes everything a human wrote on it: the repository, the Dockerfile (or the image reference), the name and the intent sentence. The `PATCH` handler, the route adapter, the client method and `mediforce images update` have existed since [#1294](https://github.com/Appsilon/mediforce/issues/1294), and [ADR-0022](docs/adr/0022-image-catalog.md) decision 3 grants every workspace member the right to *edit* an entry rather than only create one — the view exposed none of it, so an entry added through **Add image** was final: a mistyped repository was permanent, and fixing a typo in the one field a human writes meant finding a terminal.
  - **Changing the source re-keys the entry rather than editing a column**, because the id derives from the source (decision 1). The handler canonicalises the new source, derives its id, writes the row there and drops the old one — so a source that merely *spells* the same key differently (`Appsilon/x` for `git@github.com:Appsilon/x.git`) stays in place, while a corrected repository or a different Dockerfile moves. It is safe by the property that makes deleting safe: no Workflow Definition references an entry, a step pins an image tag. The write is ordered new-key-then-delete, whose worst case is the corrected row beside the one it replaces — a reader can see and fix that, where the reverse order loses the entry outright. Re-keying onto a source another entry already describes is a **409**, not a silent upsert that would overwrite the occupant's sentence and delete the row being edited. A re-key writes a second audit event against the *old* id, since it is the one update that leaves an id with no row, and the CLI prints the new id rather than reporting success on the old one.
  - The dialog is **Describe** generalised rather than a second form, because both write the same fields. A `discovered` entry is not a stored row, so its button still reads **Describe**, still `POST`s, and keeps its source read-only — a build recorded it, and re-pointing it there would describe some other source while leaving this one undescribed. A `catalogued` entry reads **Edit** and opens prefilled with what it says today. The re-key is stated in the dialog before the save, not discovered after it, and the Dockerfile fields show the paths the build will use, as **Add image** does. Switching an entry between `built` and `referenced` is not offered: that changes which inputs the platform holds for it, which is **Add image**'s job.
- **A failed build leads with its cause, and the build dialog says what the build context is.** A build failure arrives as everything BuildKit printed, and the dialog was showing all of it as the error — with the one line that matters at the bottom. It now leads with a single sentence and keeps the raw output behind **Show full error**, which is still the only evidence of what happened and so is never dropped. The recognised case is the one that looks least like itself: a Dockerfile in `container/` copying `scripts/` from the directory above fails with `"/scripts": not found` for a path plainly present in the repository, because the build context is the Dockerfile's **own directory** and nothing says so. The Build dialog now states that rule, **Add image** shows the paths it resolves to, the guide documents it under [Getting a Docker image onto the platform](docs/guides/docker-image-setup.md), and the failure is named in its troubleshooting list. Declaring a wider context needed a change to `ContainerConfig` and to what an entry is keyed on, so it landed on its own — see **A Dockerfile can build from a context wider than its own directory** above.
  - The three image dialogs submitted through `mutateAsync` inside an async `onSubmit`, so a rejected write rendered its error *and* escaped as an unhandled promise rejection with nothing to catch it. All three now use `mutate` with an `onSuccess` that closes the dialog; the error still lands in the mutation's own `error`, which is what the dialog was already reading.
- **Add image** on **Workspace → Images** catalogues a repository and Dockerfile from the browser. The catalog had two write paths and neither was reachable from the view that shows it: **Describe** only ever names a source *some build already recorded*, so it cannot register a repository nothing here has built from, and everything else was `mediforce images create` or the backfill script. A workspace whose images had not been built here yet opened Images on an empty state that told them to go and find a terminal. The dialog takes the repository (`owner/repo` or a full `git@…` / `https://…` reference), an optional Dockerfile path, a name suggested from the repository the same way a discovered entry is named, and the one sentence of intent — the same `POST` **Describe** makes, because it is the same act, so `useDescribeImage` became `useCatalogueImage` and both dialogs share it. The entry lands with **no versions**, which is the honest state — an entry is an offer, and nothing has built the image — and **Build** on the new card gives it its first one. Any workspace member, matching the write gate on the entry itself ([ADR-0022](docs/adr/0022-image-catalog.md) decision 3).
- **An image can be built on demand, without running a workflow.** `mediforce images build --namespace X --repo R --commit C [--dockerfile D]`, and a **Build** action on any `built` entry in **Workspace → Images**. The platform could already build this exact image — a build-mode step does it lazily when a run starts — but only ever as a side effect of a run, which left anyone *preparing* an image with nothing to do. On a deployment there is no way around that: there is no Mediforce registry (no `docker push`, no registry URL, nothing registry-shaped in `mediforce system status`), the daemon is reached over `/var/run/docker.sock`, so "build and push it yourself" means shell access to the host. A user with a Dockerfile in a repo and no host account had no path onto the platform at all [#1344](https://github.com/Appsilon/mediforce/issues/1344).
  - **It is the auto-build path with the run taken out**, which is what makes the result indistinguishable from a step's own build rather than a parallel way of making images. The same `deriveBuildTag` mints the tag, the same builder clones and builds it, the same `buildProvenanceLabelArgs` labels it — so a build-mode step pinning that commit finds the image **cached** instead of rebuilding it, and the catalog picks it up with no code of its own: as a new version of the entry if the source is described, as a discovered entry if nobody has described it yet ([ADR-0022](docs/adr/0022-image-catalog.md) decisions 1 and 7). Nothing was taught about builds twice.
  - **Any workspace member, matching `createImageCatalogEntry` rather than the admin gate a Dockerfile-on-the-shared-host suggests.** The capability is already theirs: the same member can trigger the same build on the same host by running a build-mode step, so gating here would have removed the convenient path and not the capability, while making the feature useless for exactly the user it exists for. Whether host-side builds should be privileged is a question about build-mode steps generally, and it is not answered asymmetrically here.
  - **A failed build is reported as a failure.** The sibling daemon reads degrade to `unknown` — legitimately, since "what is in this image" has a not-knowing state — but this call is asked to *produce* an image, and returning a tag for one that is not coming would leave the caller polling an entry that never gains a version. The request stays open for the whole build (bounded at 30 minutes) instead of returning early, so the error has somewhere to land; the CLI and the dialog both say a build takes minutes rather than looking hung.
  - **One builder, reached two ways.** The worker's new `POST /images/build` and the local path both run `container-worker`'s `buildImageFromRepo`, the second through the dynamic import `LocalDockerSpawnStrategy` already uses. Cloning is not a `git clone`: `Appsilon/tealflow` and `git@github.com:Appsilon/tealflow.git` are what step authors write, and turning either into a working fetch is the SSH-then-HTTPS ladder, the deploy key and the credential redaction `cloneRepoAtCommit` implements — a second, simpler clone alongside it would pass its fixtures and fail on the real inputs. `BuildImageRequestSchema` in `platform-core` is the one shape both paths take, so they cannot drift on a field, which is the hazard the container-worker README names for anything crossing that boundary. The new route is gated exactly as `DELETE /images/:id` and the capability probe already are.
- **An image a workflow built shows up in the Image Catalog on its own, marked "Needs a description".** Until now the catalog had two write paths — `mediforce images create` and the API — and the build path used neither, so a workspace that ran a build-mode workflow got an image on the daemon labelled with its repo, commit, Dockerfile and workflow, and a catalog that had never heard of it: absent from **Workspace → Images**, and not offered by the step-editor picker that [#1298](https://github.com/Appsilon/mediforce/issues/1298) had just pointed at the catalog. The listing now also offers every source **this namespace built** that nobody has described — one **discovered entry** per `(repo, dockerfile)`, read straight off the `mediforce.build.*` labels the builders already write ([ADR-0022](docs/adr/0022-image-catalog.md) decision 7). Every fact on it is as derived as a stored entry's — versions, commits, the workflow that triggered the build, capabilities, lineage, so it groups under the golden image like any other derivative — and the one field missing is `intent`, which the card says out loud and the new **Describe** dialog fills in. Describing one is an ordinary `POST` on the source it already carries, so it lands at the same id the row was showing rather than beside it, and `createImageCatalogEntry` probes it in the same request: being described is what buys the probe.
  - **Derived on read, not written at build time**, because the two retrieve the same facts. The builder writes them onto the image and the daemon listing reads them all back, so a write would buy nothing and cost a Postgres or API dependency inside `container-worker` and `agent-runtime`, on the path where a run is starting. Derivation also works backwards, so images built before this change appear too. It is one filter over the daemon listing the entry views fetch anyway — no extra daemon call, no probe, no write — which is what makes it affordable on a listing polled every 30 seconds. Not persisted with a nullable `intent` either: that would weaken decision 2's invariant across the schema, the contract, the CLI and the picker to represent an absence that not being a row already represents, and leave rows to garbage-collect when their images go.
  - **Its capabilities fill in on the same read a stored entry's do** — expanding the card — into a memo keyed by the daemon's immutable image id rather than a row it does not have. That key is why the memo needs no TTL and no invalidation: an answer for an image id cannot go stale, because a rebuild mints a different id. The listing starts no probe for either kind of entry, since that would be a container per version on a 30 s poll, but it shows what an earlier read already paid for. Until something has probed it the entry reads `unknown`, which stays selectable without a suitability claim, and the picker labels it `not described yet · not probed` rather than trailing an empty dash where the sentence would go.
  - **Three images are not offered, and the rule does the excluding**: one built for another namespace (the build recorded which one), one the platform did not build (`postgres`, `redis`, a dangling layer — no build labels at all), and a build from a local filesystem path, which nothing can rebuild and which is test residue. Only the last needs a written rule. On the development host that prompted this, `db` gains exactly one row. **Admin → Infrastructure** is untouched and stays the raw daemon inventory.
  - A `built` entry catalogued as `https://github.com/owner/repo` now matches the images built from it. `resolveEntryVersions` compared the entry's canonical repo URL against the raw label, so an entry whose source was written in a form other than the one the step author happened to use resolved to zero versions and read as unavailable while its image sat on the daemon. Both sides are normalised.
- **`scripts/migrations/adopt_daemon_images.py` backfills the Image Catalog from the images a deployment already has.** The catalog is curated, not a view over the daemon ([ADR-0022](docs/adr/0022-image-catalog.md) decision 1), so a host with months of built images still opens **Workspace → Images** on "No images catalogued yet" — and the step editor's picker, having no catalog to read, falls back to the flat daemon listing for agent and script steps alike, which is why the two look identical. The script groups the daemon listing by **source** rather than by artifact, which is the whole point of the entry key: on a development host 31 images collapse to 7 entries, five `mediforce-agent:*` tags becoming one entry with five versions instead of five rows. It runs in two phases and refuses to skip the second: `--draft` writes a file with every mechanical field filled in and `intent` blank, `--apply` registers only the entries whose intent a human wrote. Generating that sentence was the one thing not to automate — decision 2 makes it the field that stays true across rebuilds, and "Imported from the daemon" repeated 25 times is the catalog of meaningless rows the epic exists to replace. Test artifacts (`mediforce-test-*`, `mediforce-e2e-*`), dev-infra containers (`postgres`, `redis`) and images built from a local filesystem path are dropped by default — the last of those being build residue pointing at a temp directory that no longer exists — and `--include-all` keeps them. Base images stay in: the picker already filters per executor, so `alpine` is offered to a bash script step and hidden from an agent one rather than being withheld from both. Re-applying a draft is safe, an already-catalogued source counting as skipped rather than failed, and the whole thing goes through `mediforce system images` and `mediforce images create` rather than reaching past the CLI.
- **The step editor's image picker reads the Image Catalog instead of the Docker daemon.** This is the moment an author actually feels the epic: choosing an image was the step with the least information in front of it. Options now carry the entry's name and its intent sentence rather than a bare `repo:tag`, and they sit grouped under the image each was built on, so derivatives of the golden image are together. An **agent** step is offered only entries a probe found agent-capable — `alpine` and `postgres` stop being one click away, which is the fix for the `exec: "claude": executable file not found` class of run-time failure — while an entry nobody could probe stays offered and says so (`· not probed`) rather than being silently dropped. A **script** step with an `inlineScript` is narrowed to images carrying the interpreter its `runtime` names (`r` → `Rscript`, `python` → `python3`, `javascript` → `node`, `bash` → `sh`); a step running a `command` needs no particular interpreter and is narrowed by nothing. The hardcoded `★` compare against `DEFAULT_AGENT_IMAGE` is deleted — the blank option already names the image registration fills in, which is a fact about the runtime rather than a claim about suitability [#1298](https://github.com/Appsilon/mediforce/issues/1298).
  - **It curates the picker; it is not an allowlist** ([ADR-0022](docs/adr/0022-image-catalog.md) decision 5). The free-text field beside the select still saves any string, a step pinning an image no entry describes still loads, displays and saves that value unchanged, and the `mediforce-golden-image:latest` ↔ `mediforce-golden-image` normalisation stays — without it a step carrying the untagged default would match no option and be rewritten on save. The preflight `missing-image` warning is untouched, build-mode skip included.
  - **A deployment with no catalog authors exactly as it did before.** An empty catalog, or a daemon nobody can reach, degrades the picker to the daemon listing it always showed, so an author never faces an empty picker (§13). What does *not* trigger that fallback is a catalog that covers images but suits this step with none of them: "none of these" is an answer, and re-offering the raw daemon list there would put `alpine` back in front of an agent step. The select still renders, with its blank option, so the default and the escape hatch are both still there.
  - `useImageCapabilities` is gone, replaced at its one call site by the `useImageCatalogEntries` it duplicated — both issued the same query under the same key — so the editor and the Images view now read one catalog through one hook.
- **Images** is a workspace section any member can open: the view that answers *"is there already an image like the one I need?"*, which no screen addressed before. Entries are grouped under the image each was built on — the computed base, not an alphabetical list — and each card carries the required intent sentence and the probed capability chips (`Rscript · bash · claude · agent-capable`). Search matches intent, repository and capability text, not just the name, so *"which image has R?"* is answerable; the absence of any search box at all is what [#1285](https://github.com/Appsilon/mediforce/issues/1285) §4 called out. Expanding an entry reads it on its own — the only read that carries the per-version layer summary, since it costs a `docker history` per version — and shows the version history newest first with `current` / `superseded` / `unused` marked, each version carrying the layers it adds over its base — open on the current one, a click away on the rest, since an entry accumulates a version per build — and the workflows and steps that pin each version. A workflow in a workspace the reader has not joined is named but not linked: the scan is deployment-wide, so a public workflow elsewhere belongs in the answer, while `/<handle>/workflows/<name>` is member-gated and following it would land on "Workspace unavailable". An entry whose image is gone from the daemon renders as unavailable rather than vanishing or 404ing, and an unreachable daemon degrades the whole view to "not checked" rather than an error page (§13, [ADR-0022](docs/adr/0022-image-catalog.md) decision 2) [#1297](https://github.com/Appsilon/mediforce/issues/1297).
  - **The source ladder is rendered as a ladder, because its rungs are not equivalent answers.** Rung 1 — our own build labels — gives the exact Dockerfile at the pinned commit, so the permalink is `https://github.com/${owner}/${repo}/blob/${commit}/${dockerfile}` with no fetch, no GitHub API and no auth: a private repo resolves against the viewer's own GitHub session. Rung 2 is an OCI label the image sets *itself* — safe only because [#1296](https://github.com/Appsilon/mediforce/issues/1296) subtracts the base's labels first, without which a local image of ours links to `rocker-versioned2` with full confidence — and names no Dockerfile path, so it points at the repository at that revision rather than guessing one. Rung 3 is a member's declared source, labelled declared rather than derived. Rung 4 is nothing, and says so. The new `githubPermalink` refuses a local-path repo, a non-GitHub host and an unpinned commit, rendering **no link** rather than one that 404s — `/blob/<commit>/<path>` is GitHub's URL shape and guessing another host's is worse than saying nothing. It reads the host off the reference itself rather than off `normalizeRepoUrls`, whose job is producing a clone URL and which therefore falls back to github.com for a reference it cannot place: `git@gitlab.com:group/repo.git` came back as `https://github.com/git@gitlab.com:group/repo.git`, which is a reasonable "try this" for a clone and a guaranteed 404 as a link.
  - **The layer summary is called layer commands everywhere it is named, never "the Dockerfile".** `docker history` carries no file contents, no comments, no formatting and no multi-stage structure, and its build-arg values are already redacted server-side. Rendering a Dockerfile's contents inside the platform needs [#1286](https://github.com/Appsilon/mediforce/issues/1286), which is still open — so the view states which rung it reached and why, instead of presenting a layer list as the file.
  - **A catalog read stops paying for disk statistics it never looks at.** `getDockerInfo` gathers `docker images`, the `docker image inspect` batch that annotates it, *and* `docker system df` — seconds of wall clock on a busy daemon, and read by exactly one screen, the infrastructure page's disk cards. Every Image Catalog read wants the listing and none of them want the disk, and a catalog read now happens on a page load and on a 30 s poll, so the catalog asks for the half it uses through a new `fetchDaemonImages`. `getDockerInfo` and its response shape are unchanged; the split was named as a prerequisite for this issue when [#1296](https://github.com/Appsilon/mediforce/issues/1296) measured it.
  - **Admin → Infrastructure stays the raw ops view** — every daemon row, `postgres` and dangling `<none>` layers included, disk cards and delete. Its only change is a link on a row some catalog entry describes, keyed on `repository:tag` rather than the image id, since two tags can alias one immutable id and an id map would point both rows at whichever entry it saw last. `GET /api/workflow-definitions/by-image` now takes a repeatable `image` parameter and reports which of them each workflow uses, so an entry with six versions costs one scan of the workflow definitions instead of six, and a version nobody pins is visible as unused; the single-image call is unchanged. The per-row "used by" fetch both pages duplicated moves into one `useWorkflowsByImage` hook.
- The Image Catalog is grouped by what each image was built on: every entry names its base, derived images list indented under it with roots first, and each version reports the layers it adds over that base. Lineage is computed from `RootFS.Layers` prefix containment rather than parsed from a `FROM` string, so it holds for images the platform never built — an image built on `python3.12-slim` groups under it exactly as a workflow image groups under the golden image. The daemon listing also separates the labels an image sets itself from the ones Docker copied down from its base, so `org.opencontainers.image.source` can no longer be read as this image's provenance when it is really rocker's, and build-arg values in the layer summary are redacted before they reach any client [#1296](https://github.com/Appsilon/mediforce/issues/1296).
- Image Catalog versions now expose probed runtimes and agent suitability, so known incompatible images are not offered for agent steps while unreachable probes remain explicitly selectable as unknown. Each probe is a CPU-, memory- and process-capped container with no network, the whole refresh of one entry is time-boxed, and the container-worker route that starts one is gated by `CONTAINER_WORKER_SECRET` — now forwarded to both services in `docker-compose.prod.yml`, where an unset value keeps today's ungated behaviour [#1295](https://github.com/Appsilon/mediforce/issues/1295).
- Images a workflow builds from a Dockerfile it carries are in the Image Catalog. Before, they sat on the daemon as `mediforce-artifacts:*` tags that no view showed, and an edited script left the old image behind with nothing pointing at it. They now appear under **Workspace → Images** as a `carried` source keyed on the workflow and Dockerfile, with content hashes as versions. They are offered in the step-editor picker, labelled with the workflow, and can be described like any discovered entry. A live workflow version running on one blocks its delete. **Publish as image** (`mediforce images publish`) rebuilds one version's files through the upload path into a `<workspace>/<name>` entry that outlives the workflow ([ADR-0022](docs/adr/0022-image-catalog.md)).

### Changed
- The step editor asks where a step's image comes from, instead of showing every field that could answer. One choice — a ready image, a Dockerfile the workflow carries, or one in a git repository — and only that source's fields are shown; switching clears what the new source does not use. The three are what the runtime already picks between, read back from the step by the same precedence, so the editor cannot offer a combination that resolves to something else. A carried Dockerfile is picked from the files the workflow holds rather than typed, an image is picked from the catalog with the free-text field behind a toggle, and `image` is labelled a **build tag** in the build modes, which is what it means there. A step that sets more than one source keeps every value until an author clears it, with a line saying which are ignored.
- A build the assistant cannot finish asks instead of failing. Exhausting the retry loop threw, which the pane rendered as an error toast: the turn was gone and the person had nothing to act on, while the model knew exactly what had stopped it. It now spends one more short call turning that into a plain sentence and at most two questions, each with the answer it would take, rendered in the same card the planning turn uses — answering runs the build again. An error is still raised when even that fails, because then there really is nothing to act on. Outcome errors from a partly-applied batch moved from a toast into the conversation for the same reason.
- `/design-workflow` is no longer an authoring path in the app. **Ways to author** listed it as the fourth option, and the assistant pane advertised it, because the canvas could not author the *package* — a script, a Dockerfile, a skill — so anything past an inline script meant a git checkout, a second tool and a CLI hand-off. A workflow carries its own files now, the Files panel edits and uploads them, and the assistant writes them, so naming the skill there sent people out of the app for a capability that is in it. The guide keeps the section, marked deprecated for authoring and narrowed to what still lives in a repository: generating a whole package into the canonical layout, the behaviour tests it writes under `tests/`, and pinning commit SHAs.
- `pruneWorkflowSettings` moved to `@mediforce/platform-core`, beside the `WorkflowSettings` type it has to agree with — it was a package away from it, in the UI, while the assistant reducer that produces those settings lives in core. The editor's import path is unchanged (the old module re-exports it), and its tests moved with it.
- A dry run builds the image, and says so before it starts. Every agent and script step is still swapped for `MockAgentPlugin` — no agent runs, no container starts, nothing external is called — but whether the image compiles was the one thing that mock could never answer, and it is the part that takes minutes and fails: a dry run of a workflow with its own Dockerfile reported it fine and the first real run then failed to start. The build goes through the spawn strategy, so it happens where Docker is (in process locally, as a `build-image` job on the worker when a queue is configured), and a build failure fails the dry run. The Start Run dialog says a build is coming **only when there is one** — a workflow naming ready-made images starts immediately, and warning about minutes there would be false.
- The header's **Search** badge is now a **Ticket** button. It opens the command palette straight on the existing `new-ticket` view, so filing a bug is one click instead of ⌘K-then-scan; the palette's back arrow leads to the full command list, so nothing became keyboard-only and ⌘K still opens the list directly. The load-bearing property is unchanged and was already true: a ticket is filed with the reporter's *Mediforce* session, and [`/api/tickets`](packages/platform-ui/src/app/api/tickets/route.ts) posts the issue with a server-side `GITHUB_TOKEN` — no user ever connects a GitHub account, holds a PAT, or sees an OAuth consent screen. That token was read by the route but never forwarded to the container, so ticketing could not work in any deployment however the `.env` was filled: `GITHUB_TOKEN` and `GITHUB_REPO` are now in [`docker-compose.prod.yml`](docker-compose.prod.yml) and `.env.example`, both defaulted ([AGENTS.md §13](AGENTS.md)) so an estate that sets neither still boots and the form simply reports ticketing is unconfigured.
- An admin invite sends its email even when the workspace-name lookup fails. Extracting the invite path into [`seedMemberAndNotify`](packages/platform-api/src/handlers/users/seed-member.ts) moved `getNamespace` out of the email `try` and gave it its own: a throw used to abort the send and report `emailSent: false`, leaving the account seeded but the person never told. The email now goes out with the workspace handle standing in for its display name, which is also what `/join`'s success page shows.

- "Use the GitHub MCP" now wires the MCP. An MCP server reaches a step through one route only: `resolveMcpForStep` reads the step's `agentId`, and without one there is no resolution at all, so an inline agent step gets no MCP however the request was worded. The assistant was never told this, and the capability doc embedded in its prompt still said the Tool Catalog entry and the agent binding were manual platform setup, which was true of the `/design-workflow` skill and not of an assistant holding `create_tool_catalog_entry` and `create_agent`. The prompt now states the route (catalog entry, an agent bound to it by `catalogId`, the step's `agentId`, `mcpRestrictions` to narrow), `list_agents` returns the servers each agent is bound to so an existing one can be reused rather than duplicated, and the doc says which of the two authoring paths does this by hand.
- A workflow the assistant builds hands a file from one step to the next correctly. `/output` is an ephemeral channel wiped when a step ends, so a reader step written as `pd.read_csv('/output/samples.csv')` failed with `FileNotFoundError` even though the previous step had plainly written that file, and the auto-runner retried it to the loop cap. Nothing in the prompt or the capability reference said where an output file actually lands, so the model had no way to know. Both now state it: a step's files are kept in the run worktree at `/workspace/.mediforce/output/<stepId>/<file>`, which every container mounts, and a plain value needs no file at all.
- The assistant no longer answers a person in the app with a terminal command. Its own prompt named `mediforce workflow trigger-add`, and the references embedded in it are written for someone with a checkout, so "add a weekly schedule" came back as shell. It now points at the product and never quotes those instructions back.
- A workspace with no `OPENROUTER_API_KEY` says so in the assistant pane instead of failing three times. The pane probes for the key when it opens and pulses a warning beside the title, which says on hover that `OPENROUTER_API_KEY` is missing from the workspace. One missing key used to produce three popups for a single message: the global 4xx listener fired for the planning call, again for the build, and the pane added its own on top. The assistant's calls are silent at the client now, and a request the server refused stops the turn there rather than making the same failing request twice.
- An inline script that needs a third-party package is written as a carried file with a Dockerfile instead. The inline runtime is the standard library in every language it offers, so `import pandas` and `library(dplyr)` fail at run time; the old advice was to pip-install from inside the script, which predates a workflow carrying its own files and re-ran on every execution.
- The assistant's replies render as markdown. The pane printed the raw string, so a list or `code` reached the reader as asterisks and backticks. What the person typed is still shown verbatim.
- Ways to author closes when you click away from it. The canvas pane swallows the pointer events the popover listens for, so it stayed open over the workflow it was describing.
- The assistant can put a saved workflow on a schedule. A trigger attaches to a registered workflow rather than living in the definition, so "run this every week" used to come back as homework, complete with a CLI command the person could not run. `create_cron_trigger` does it through the same handler the Triggers tab uses, as the person asking. On a workflow that has never been saved the tool is not offered at all: offered and then refusing, the model talked its way to the Triggers tab and then to a CLI command, where the honest answer is one sentence saying it cannot be scheduled until it is saved. Webhooks are declined the same way.
- The assistant checks that a role exists before a step leans on it. `allowedRoles` naming a role nobody holds is a task nobody can claim, and there was no way to tell: `list_roles` returns the roles the workspace grants and how many people hold each, and when none matches the assistant says plainly that nobody holds it yet and that it is granted in Settings then Members. It also names which of the three layers it wrote, since workspace grants, workflow access and a step's roles are easy to confuse. The pane says it too, from the roster it already reads, so it does not depend on the model remembering — and anything the person has to act on is now its own message in the warning colour rather than another paragraph of reply.
- A workflow whose input contract the first step asks for again is caught before the run, not by it. `triggerInput` is the workflow's total contract and is checked unconditionally, so a required field there must be supplied at the moment of starting: declaring one and then collecting it on the first human step means "Invalid payload" before anybody reaches that step, and a dry run that can never start. The assistant kept building exactly that shape, so the prompt now says a field is collected once — trigger input or step params, never both — and the preflight checks warn about it beside the missing-secret and missing-image warnings.
- A decision step whose transitions half-carry conditions is refused at the gate, not five minutes into a run. A verdict step's transitions normally mirror its verdict targets and carry no `when`, since the verdicts route it — but the engine falls back to those transitions whenever a step's output names no verdict, and it refuses a set where some carry a condition and some do not. An agent that answers `PASS` rather than a verdict name is ordinary, so the shape turns a routine miss into `Multiple transitions require all to have 'when' conditions` after four steps have already run. `validateStepGraph` now reports it, which means the editor, `workflow validate`, registration and the assistant's own gate all catch it first.
- The assistant can edit a workflow that is already broken, and can delete an edge. Its graph gate failed on *any* invalid result, so a workflow carrying a defect nobody in the conversation caused — pasted, imported, or authored before a rule existed — could not be edited at all: the model was told to fix something it had not done, had no tool for, and spent its turn on. The gate now compares against the errors the canvas arrived with and fails only on what the turn introduced; the inherited ones are named in the reply with an offer to fix them. `remove_transition` is the missing verb, since transitions previously only changed as a side effect of adding, removing or re-inserting a step.
- An agent step gets `/output/input.json`, the way the `/output` contract has always described it. `container-plugin.ts` says the host seeds `input.json` there before the container starts; only script steps ever got it, so an agent prompt that read the documented path found nothing and the step answered with prose instead of its output contract — which then routed nowhere. The same data is still inlined in the prompt under `## Input Data`; the file is the copy an agent can open with a tool, which is what a large input needs.
- A decision step that produced no verdict says so. Falling through to the transitions reported `No matching transition. Evaluated: verdict == "pass", verdict == "fail"`, which sends the reader to the expressions when what went wrong is the step's own output. It now names the step, the verdicts it routes on, and what the output actually carried.
- The step breadcrumb is titled like every other crumb. It swapped hyphens for spaces and stopped, so the trail read `Workflows / Tealflow / Run / collect application requirements`; it goes through `formatStepName` now, which leaves capitals a step id already carries alone.
- A pasted definition is named by its display name, not by the version's message. `title` is the message on a version ("Change from path to logic"), so a definition copied out of a registered version put a changelog line where the workflow's name belongs. `metadata.displayName` wins, then `title`, then the id, title-cased.
- The save dialog says which version a save will create. Editing v2 of a workflow with three versions produced v4, because a version is appended and never inserted, while the dialog promised v3 — the number that is right only when editing the newest. It now counts from the newest version and says, when they differ, that this saves as v4 rather than replacing v2.
- The plan the assistant states before building reads as a list again. Its lines were joined with single newlines, which markdown folds into spaces, so three steps arrived as one run-on sentence with no punctuation between them: "Fix the regex escape error Replace the patterns Test the script". They are written as list items now.
- Every image a workflow builds from a Dockerfile it carries is rebuilt once. Its content hash now also covers the workflow and namespace, so two workflows carrying identical files build two images, each labelled truthfully with the workflow the Image Catalog offers it under. The images built under the old hash carry no workflow label, so the catalog never lists them and they stay on the daemon until removed by hand (`docker image prune` or `docker rmi mediforce-artifacts:<tag>`). A carried build still reads every carried file, and a `context` named next to a carried Dockerfile is still ignored.
### Fixed
- The step-editor image picker offers `alpine` to a `runtime: bash` step again — the image the engine runs one in by default. The capability probe asked whether an image carries `bash`, while the engine executes a bash step as `sh <script>`, so `alpine` (busybox `sh`, no `bash`) probed as carrying no runtime at all and was filtered out of the only step type it is the default for; an author was left picking a 4.5 GB agent image to run a shell script. `sh` is now probed and asked for alongside `bash`, which stays a runtime of its own for a step needing bash-only syntax. Migration `0053` writes `sh` into the capabilities already cached, because a `known` probe result is never re-probed and every stored one predates the new question. It records rather than re-probes: the probe *is* `docker run --entrypoint sh`, and an image with no shell cannot start that container at all (empty stdout, cached as `unknown`), so a `known` row is itself evidence that `sh` ran on that image's PATH. Every other probed fact survives, so no entry passes through `unknown` and the curation [#1298](https://github.com/Appsilon/mediforce/issues/1298) gave the agent picker never lapses. Deploy `platform-ui` and `container-worker` together — outside local-agent mode the probe runs on the worker and the script is baked into that image, so an image catalogued against an old worker caches a pre-`sh` answer as `known`, which is never re-probed [#1377](https://github.com/Appsilon/mediforce/issues/1377).
- Agent steps no longer stall on a deployment with a queued worker. Each container job carried its workspace files as base64 in its data and return value, so one job could hold ~7 MB, and the queue kept 100 completed jobs, 200 failed ones and BullMQ's default 10,000-entry event stream, which holds every return value again: Redis grew to ~370 MB against its 256 MiB limit, ran in swap and was restarted by the OOM killer every 10–40 minutes. A worker then could not renew its job lock, and a container that exited 0 had its result lost (`Missing key … moveToFinished`), leaving the step waiting until it timed out. Files now travel under their own Redis keys, deleted as soon as the job settles and expiring after it otherwise, so a finished job keeps kilobytes rather than megabytes however large its files. The queue also keeps only 10 completed jobs for an hour and 20 failed ones for a day, and caps the event stream at 100 entries.
- The `externalSkillsRepo` skills cache can no longer get stuck empty forever. `fetchSkillsFromRepo` copied straight into its content-addressed cache dir, so a step killed mid-copy (timeout, engine restart) left a directory that existed but held nothing; `existsSync` treats that as a permanent hit, so every later run of that workflow version failed with `Skill file not found` even though the source repo had the skill all along. It now copies into a staging dir and renames into place atomically, so a crash never leaves a half-populated cache entry [#1370](https://github.com/Appsilon/mediforce/pull/1370).
- A workflow build no longer overwrites an image the Image Catalog published. A step that named an `image` and built it — from a carried Dockerfile or a repo — landed the build on that tag, so pinning a `<workspace>/<name>` image the catalog offered and running the workflow replaced it: the tag an upload refuses to overwrite was overwritten anyway, and the published entry and the carried one then offered the same artifact. Such a build now uses its derived tag and leaves the published image alone, the rule the golden image already had. A step that names both an `image` and a carried `dockerfile` is flagged before the run, since the carried files win and the pinned image is not what runs.
- The step-editor image picker offers an image once when two catalog entries describe it. A tag claimed by both a carried entry and the published one was listed twice — the same value, so the second option offered no choice — and React warned about the duplicate key.
- A step naming only a `dockerfile`, built from the workflow's `externalSkillsRepo`, no longer overwrites the shared golden image. `stepHasBuildSource` did not count that fallback, so registration wrote `image: mediforce-golden-image` onto the step and its first run rebuilt that tag on the host with this workflow's Dockerfile, for every workflow on it; preflight also warned that the Dockerfile had nothing to build it from. The predicate now takes the definition and counts the skills repo. Versions already registered with the golden image written on cannot be edited, so the runtime never builds under the golden tag: a step with a build source that names it builds and runs under its derived tag.
- A step that names its own `image` and builds it from a Dockerfile the workflow carries picks up edits to the carried files. The builder reused any image already under that tag, so after the first build an edited Dockerfile or `COPY`'d script never reached a run; the derived `mediforce-artifacts:<hash>` tag was unaffected because the hash is the content. The build now labels the content hash (`mediforce.build.artifacts`) and rebuilds when it differs — the check a repo build already makes on its commit — and blanks the inherited repo labels, so a carried image built `FROM` a platform build is no longer listed as a version of that repo's catalog entry. An image built from carried files before this change carries no label and is rebuilt once.
- An assistant turn can be halted. A build is the one call long enough that a person will want to stop it — a wrong prompt, or a plan they no longer want — and there was no way to, so it ran to the end and charged for it. A stop button sits beside the status line while a turn runs, aborts the request rather than ignoring its result, and says so in the thread: nothing was applied, since the canvas only changes when a turn returns. The seconds counter beside it now reads `2m 14s` past a minute, through the same `formatDuration` every run duration uses, instead of counting to `134s`.
- The assistant no longer reports a build it has not done. It opened with `update_workflow` and a sentence announcing the work ("I'll build the workflow you described"), and because the starter graph was already valid the turn was declared finished on that first batch: the settings landed, the steps never did, and the person read a confirmation of work that had not happened. A batch leaving a valid graph is not evidence the request is done, so the model now ends its own turn the way every tool loop expects — by answering with no tool calls. After each batch it is shown the graph as it stands and told to continue if anything asked for is still missing; the summary belongs to the turn that stops calling tools. It costs one more model call per turn.
- A Dockerfile an imported or legacy workflow builds from git survives an assistant edit. The check for "a `dockerfile` with nothing to build it from" only knew about carried files, so editing such a step stripped the field and moved the build to the repository's root Dockerfile, which is a different image. The step's own `repo` + `commit` and the workflow-level `externalSkillsRepo` are build sources too, and both are left alone.
- The assistant no longer states its plan and then asks whether to go ahead. The plan was pushed into the conversation as an assistant message, so on the build call the model found it had already said what it was going to do and replied conversationally instead of building — intermittently, because the plan reached the request only when React had committed it before the build started. Everything the pane writes about a turn (the plan, a note that something did not land, a halted turn) is narration now and never travels back to the model; only what the model itself said does.
- A routing condition on an edge the same batch had just split now lands where it obviously belongs. Inserting a step between two steps replaces the edge that joined them, so a `set_transition_condition` naming `poll → done` after a validate step went between them found nothing — and the refusal said "add the edge", so the assistant added it back, which split the graph again. When the step being conditioned has a single way out there is nothing to guess: the condition goes on that edge and the outcome reports the edge it actually set, so the summary stays true. It is refused only when the step branches, and the refusal then names the edges leaving it. A `to` that names no step at all is still reported as a mistake rather than repaired.
- A human step assigned to somebody who does not exist no longer strands the run — including where there is no user directory to resolve against, since nothing there can confirm the value names a real user either. `assignedTo` was interpolated and looked up in the user directory, and when the lookup found nobody the *raw value* became the task's assignee — so "a data manager reviews the report" plus an email address produced a task claimed by `data-manager@company.com`, a user id nobody holds. The UI showed it claimed by someone else, `completeTask` refused every other caller, and since there is no unclaim, no reassign and no owner override, the run could not be finished by anyone. Such a task is now left unassigned, so a holder of the step's role claims it, and an `task.assignee_unresolved` audit entry names the value that matched nobody. The creator fallback is deliberately skipped in this case: handing a review to whoever started the run is a different decision than the author wrote, and on a review step it is the one that removes the second pair of eyes.
- The assistant sends a job title to `allowedRoles` instead of `assignedTo`. "A data manager reviews it" names a role — anyone holding it can claim the task — while `assignedTo` takes one specific user id, which an email address or a title is not. The prompt now says so, says what the platform does with a value that names nobody, and keeps `assignedTo` for an identity the run actually knows (`${triggerPayload.userId}`).
- A workflow the assistant had to retry no longer dies in the retry. When the graph came back incomplete, the loop told it to "reconnect the disconnected step, referencing its real id from canvas state" — an instruction it could not follow: the canvas state in the conversation is the one sent at the start, a step added that turn carries an id the reducer assigned and the model has never seen, and the `clientId` it used is dead by the next response. So it guessed, every guess came back as an unknown step, and a five-step workflow burned the twelve-iteration cap and gave up on "the transition routing got tangled". The retry now states the canvas as it stands — every step by id and name, every edge — and says plainly that the previous response's clientIds no longer resolve.
- A reply that arrives while the assistant pane is scrolled up or collapsed is now noticed. The thread scrolls to the newest message, and a collapsed pane's toggle takes the primary colour with a dot on the icon, plus a spinner while a turn is running — an answer or a question nobody sees is the same as none. Opening or collapsing the pane clears it.
- The assistant cannot author a repository build context at all, which is what a placeholder SHA was a symptom of. `repo`, `commit` and `repoAuth` are dropped from its step tools: a repository build context is the user's to set in the step editor, and a model asked for a step that needs dependencies would otherwise invent a URL and a SHA. Dropped rather than rejected on purpose — refusing the call sent the model into a retry loop that burned the iteration cap and ended the turn with an error, which is worse than the bad save it replaced. A step naming a `dockerfile` the workflow does not carry loses that field for the same reason: every retry re-applies the whole accumulated batch from the original definition, so an add_step checked against the files as they stood *at that call* never saw the Dockerfile the model wrote a call later — it was told to write the file, it wrote it, and the gate failed on the replay for ever. The files a batch finishes with are what a step's `dockerfile` is checked against, once, at the end, and a step nobody carries a file for runs on the stock image.
- The assistant no longer invents a git repository to run a script file from. `workflow-capabilities.md` — embedded verbatim in its prompt — still said the only way to run a file was to bake it into an image from `dockerfile` + `repo` + `commit`, so it dutifully wrote a plausible `github.com/user/…` URL and the forty-zero placeholder SHA the `/design-workflow` skill uses while a package waits for its first commit. That names a build that can never run. The doc now leads with carrying the file (`/artifacts`), the golden rules say a workflow that carries its files pins nothing, the prompt forbids inventing either field, and `CommitShaSchema` refuses an all-zeros commit outright — so a hallucinated pin cannot reach a saved version.
- A condition set on an edge created in the same batch lands on it. `set_transition_condition` was the one assistant tool that did not resolve a `clientId`, so "add a check step and only escalate when severity is high" — one request, two calls — reported that the edge did not exist. Every other tool has resolved ids since they were introduced.
- Carry-over between runs is reachable by the assistant. `inputForNextRun` was omitted from `update_workflow`, so "remember the file listing so the next run can diff against it" — the reason a polling workflow exists — had no tool at all. It travels with the workflow level, resolves ids like the graph tools, and is validated at the same gate, so an entry naming a step that does not exist is refused before it can reach a save.
- **An archived workflow can be found again.** The workspace catalog's **Display → Archived workflows** never showed anything: since the Postgres data layer ([#534](https://github.com/Appsilon/mediforce/pull/534)) `workflows.list` drops archived rows unless asked, and the catalog never asked, so a workflow with every version archived vanished outright — no card, no toggle, no "All workflows are archived" — and its **Unarchive** button was reachable only by typing its URL. The catalog now lists archived workflows and keeps filtering them client-side, as it was built to. `listWorkflows` picks each card's version from the non-archived ones first, so an archived head over a still-running v1 is not mistaken for an archived workflow; only a workflow with nothing left to run reads as archived.
- **"Used by" was answering from a fraction of the definitions that could break.** `GET /api/workflow-definitions/by-image` inspected `latestVersion` alone and fetched with archived workflows excluded, so a step pinning an image in any other version was invisible — which made it unusable as the basis for *what breaks if I delete this*. Matching now lives in one place (`findWorkflowImagePins`), so the panel and the Image Catalog's delete cannot disagree about what counts as a pin, and `?scope=all` widens the answer to every version, archived included. The narrow default also stopped reporting a version nobody runs: it now reads the workflow's **default** version where one is set, rather than always the newest, which may be a draft.
- Root `.env` is gitignored. It is the file `docker compose` substitutes from in the compose directory — the repo root on a deployed checkout — so on any host that follows the ticketing setup it holds a live `GITHUB_TOKEN`, `POSTGRES_PASSWORD` and `AUTH_SECRET` while `.gitignore` covered only `.env.local` and `.env*.local`. Nothing was ever committed (only the two `.env.example` files are tracked), but `git add -A` on a configured host would have staged the lot.
- A pasted definition no longer names the workflow after its id, or renames the one you are editing. `name` is the definition's id, so filling the create page's **Workflow Name** with it left "landing-zone-CDISCPILOT01" where "Landing Zone — CDISCPILOT01" belonged, and the workflow was listed under the id from then on; the field now takes the pasted `title`, and the id is slugified from it as it always was. In an existing workflow's version editor the same field was worse than cosmetic: it rode along in the register body and appended the save to a *different* workflow name. `name` now reports as overwritten alongside `namespace`, since where a paste registers is the route's call or the name field's, never the file's. The paste's `title` and `description` still fill their inputs, and every other field still applies. Editing either one, or the version title, is no longer discarded on save: this page's own fields register last, so a pasted value that pre-fills an input is a starting point rather than the final word.
- A pending invitee on a Google/OIDC-only or magic-link-only deployment is emailed a way in. Gating *which email goes out* on `passwordAuthEnabled` conflated two independent decisions: whether to force the create-password flow (a deployment question) and whether the person has any way to reach the workspace at all (a `isInvitePending` question). With `ENABLE_PASSWORD_AUTH=false` a never-activated invitee — seeded by an admin invite or by redeeming a join link — got the plain workspace-notification email, whose only link is the workspace itself, so `/join`'s "we sent a sign-in link, open it to finish joining" was a promise the deployment could not keep and the joiner landed on a login screen with no credential. [`seedMemberAndNotify`](packages/platform-api/src/handlers/users/seed-member.ts) now picks the email on `pending` alone and passes `passwordSetupEnabled` through to it, which is the same split [`resendInvite`](packages/platform-api/src/handlers/users/resend-invite.ts) and `POST /api/auth/resend-setup-link` already made — the one-time link signs the invitee in and lands on `/workspace-selection` rather than a create-password page they cannot complete. An already-active user re-added to a workspace still gets the plain notification; nothing forces a password where passwords are off.
- A join-link redemption is audited as the link, not as whoever the typed address resolved to. Nobody authenticates at `/api/join/redeem` and the form takes any email, so recording the seeded account as `actorId` let a link holder who typed the workspace owner's address manufacture an audit event reading as though the owner performed the redemption. The event now carries the system actor `join-link`; the resolved `uid` was already in `outputSnapshot`, which is where it belongs.
- The **Join links** settings panel refreshes when the tab regains focus. `uses`, exhaustion and revocation all move without a local action — an anonymous redemption, another admin — so the query is [ADR-0006](docs/adr/0006-client-side-server-state.md) §4's one-shot sub-case (a), which its own comment claimed while it inherited the global `refetchOnWindowFocus: false`. A long-open settings tab showed stale seat counts until a reload; polling stays off.
- An invite to an address outside `ALLOWED_EMAIL_DOMAINS` produced an account nobody could use. `inviteUser` never consulted the allowlist but sign-in did, so an admin could invite `alice@external.com`, `seedInvite` would write her rows and the activation email would go out — and then every route she could reach with that link rejected her. Sign-in authorization is now two-term ([ADR-0021](docs/adr/0021-workspace-join-links.md) §5, amending [ADR-0002](docs/adr/0002-firebase-auth-to-nextauth.md) §4a): an email is admitted when its domain is allowlisted **or** an admin deliberately seeded the account. The second term is a new `auth_users.invited_at` column (migration `0048`) that only [`seedInvite`](packages/platform-infra/src/auth/postgres-invite-service.ts) writes — **not** "an `auth_users` row exists", which is what the ADR's own text first proposed and which would have been a regression: the OAuth adapter and the Firebase migration both write that row, so a row-existence test would have silently retired the allowlist's *other* job of evicting the people at a domain dropped from the list. The column is deliberately **not backfilled**, so this admits nobody the allowlist blocks today; re-inviting someone stamps it and repairs their account. All four sign-in paths now share one named rule, [`isSignInAuthorized`](packages/platform-ui/src/lib/email-allowlist.ts), where each previously spelled the domain check out for itself — which is how an amendment lands on one and is missed on three. **Behaviour change worth naming:** with the allowlist set, an out-of-domain address *an admin seeded* can now sign in with a password, receive a magic link and recover a setup link, where before all three refused. One that merely has an account is refused exactly as before.
- ADR-0002's "Deferred (issue #1048)" list cited an issue that does not cover its third bullet. Rate-limiting on the activation / magic-link / resend sends was filed under #1048, whose body covers only the forced-password-change guard and the Google exemption, and [`resend-setup-link/route.ts`](packages/platform-ui/src/app/api/auth/resend-setup-link/route.ts) inherited the mistake in a comment reading "Rate-limiting is deferred to #1048"; the sibling #1003 covers password-login rate limiting only. The bullet now sits under its own "Deferred (no issue yet)" heading saying plainly that it is unowned, and both it and the code comment point at the shared limiter above, so whoever picks the work up has the tool already built.
- A workflow definition copied out of a registered version can be pasted back. The source panel compared every field outside the graph against page state and refused the apply on any difference, so a copy — which carries `title`, `triggerInput`, `preamble` and the server-assigned `version`/`createdAt` — was always rejected, with an error pointing at a "workflow settings" surface that does not exist. `splitPastedDefinition` now separates the graph the canvas owns from the fields around it, validates the latter against `WorkflowAuthorableSchema`, and merges them over the loaded definition on save; server-managed and lifecycle fields need no strip list, since the authorable schema excludes them by construction. Until a settings form exists this is also the only way to declare `triggerInput`, `preamble`, `env`, `notifications` and `workspace` when creating a workflow.
- The workflow source code panel closes when you apply it. It is a modal over the toolbar, so leaving it up hid the canvas it had just changed and the Save that keeps the change, and every apply ended with a second click to get out. A refusal still leaves it open, which is where the error is shown.
- A pasted definition names the new workflow by its title, not by its id. Pasting one carrying `name: landing-zone-CDISCPILOT01` and `title: Landing Zone — CDISCPILOT01` filled the name field with the id, so the version was named correctly and the workflow itself was left calling itself by its id, case and all. The id is still derived from the name field, and a pasted `metadata` no longer replaces the display name the page just set.
- The block picker no longer hands you a step it cannot finish. "Run another workflow" produced a `spawn` step with nothing able to name its target and "Wait" hardcoded five minutes, and because `action` config is required when the executor is `action`, the whole workflow then refused to save. The action section covers `spawn` (targets, `forEach`, `continueOnSpawnError`) and `wait` (duration, deadline, condition) alongside http/reshape/email, and its `action.kind` tooltip no longer claims those three are the only kinds.
- A `table-editor` or `assignment-table` step authored in the UI no longer ships with `columns: []` and renders an empty table. `ui.config` was typed as an untyped record, so the two table views, the file-upload view, the engine's upload validator and the step editor each cast it and knew their own subset; there is now a schema per component in [`step-ui-config.ts`](packages/platform-core/src/schemas/step-ui-config.ts) and a registry keyed by `ui.component` that drives both the dropdown and the fields. Each field carries its own `.catch`, so one malformed key drops that key alone — a whole-object fallback would have read `{}` in `validateUploadPayload` and silently disabled the file-count and MIME limits the author asked for.

### Security
- `next` moves to 16.3.4, through the root `pnpm.overrides` pin that held it at 16.3.1. Two critical advisories were published against that version (GHSA-2xp9-vwfh-vxw4 and GHSA-p293-qw3h-jr36), which the supply-chain audit blocks on, and the pin meant the `^16.0.0` range in `platform-ui` could not pick the fix up on its own.
- A Docker image a live workflow version still runs on can no longer be deleted. The refusal moved below both doors onto the daemon — the Image Catalog's composite delete and **Admin → Infrastructure** / `mediforce system rmi`, whose gate is owner-or-admin of *any* namespace, which nearly every user satisfies through a personal workspace — so one stray delete can no longer break every step pinning that image, in any workspace. Deleting by id weighs every tag that names the image, since `docker rmi` on an id takes them all; when the daemon listing is unreachable those other tags cannot be checked, so the delete is now refused with a 412 rather than proceeding blind — retry, or name the image as `repository:tag`, which needs no listing. A superseded or archived version does not block: a registered version is immutable, so its pin can never be re-pointed, and refusing on its account would mean an image pinned once could never be reclaimed. The refusal names the workflow, version and steps the caller may already see and counts the rest, since the scan is deployment-wide while workflow names are not [#1378](https://github.com/Appsilon/mediforce/pull/1378).

### Added
- Authorable in the step editor: `script.timeoutMinutes` (which `agent` and `databricks` already had, so a long script was killed at the 30-minute default with no way to raise it), the verdict `label`, `intent` and `requiresComment` fields, and `params[].requiredForVerdicts`.

### Fixed
- Carry-over to the next run follows the steps it names when the canvas renames or deletes one. `inputForNextRun` entries address a step by id, and the previous fix carried the whole block forward untouched while the canvas replaced the steps underneath it — so renaming `scan` to `poll`, or deleting the step the entry read, left an entry pointing at an id no longer in the workflow. The server's cross-field check refuses that (`inputForNextRun[0].stepId 'scan' does not match any step id`), and no field on the definition page edits the block, so the rename could not be saved from the only page that offers it: the canvas change was stuck behind a value the canvas would not let you correct. The canvas now owns the block the way it already owns transitions and verdict targets — [`retargetCarryOver`](packages/platform-ui/src/components/workflows/workflow-editor-utils.ts) moves the entries with a renamed step in `updateStep`, beside the transition and verdict rewiring; [`pruneCarryOver`](packages/platform-ui/src/components/workflows/workflow-editor-utils.ts) drops entries whose step is gone, in the one normalising effect all three deletion routes reach (the diagram's **Delete step**, the assistant's `remove_step`, an applied JSON document), and a warning toast names what the next run stops receiving. The entries ride in the undo/redo snapshots with the steps, so undoing a rename brings back the entry it moved rather than stranding it. They are also part of the graph the **Workflow source code** panel applies, which is the field's only editor — with the block's `stepId`s checked against the applied steps there rather than silently pruned, since in that panel they are what the author just typed.
- The **Workflow source code** panel's **Apply** works on the definition page. It compares everything outside the graph against the fields the page hands it and refuses an apply that would change one of them — but the page handed it the whole definition, `steps` and `transitions` included, so the comparison was between a document without them and a copy with them: never equal, and every apply refused with "Edit other fields in workflow settings". The page now passes the non-graph fields only, and the graph the canvas owns (`steps`, `transitions`, `inputForNextRun`) plus the server-assigned `version`/`createdAt` stay out of the comparison.
- Saving a new version from the workflow editor keeps the fields the canvas does not edit. The save built its request body by hand-listing the fields to copy — `roles`, `env`, `notifications`, `metadata`, `externalSkillsRepo`, `url` — so everything else the definition carried was dropped on the way out: `workspace` (the git remote and the secret that authenticates the clone), `preamble`, `inputForNextRun`, `triggerInput`, and any `visibility` other than the schema default, which silently turned a public workflow private. Changing one agent step's model in `landing-zone-CDISCPILOT01` and pressing **Save** therefore also deleted the workflow's `workspace` block, leaving v31 with nowhere to clone from and nothing to authenticate with. The body is now built by [`buildRegisterBody`](packages/platform-ui/src/lib/workflow-save-utils.ts), which carries the edited version forward whole and lets the canvas override only what it changed, so a field added to the authorable surface later cannot be dropped by an allowlist nobody remembered to extend. What it does *not* carry forward is the complement of `WorkflowAuthorableSchema`: `version`, `createdAt` and `namespace` are assigned server-side, and `archived`, `deleted`, `copiedFrom` and `source` describe the version they were set on — a new version is not archived, not a copy, and did not come from the git path an earlier one was imported from.
- Saving a workflow whose graph ends in a `terminal` step no longer fails the editor's own pre-save check. Every step must declare an `executor` — the schema requires one even on a terminal, which never runs — so the Landing Zone workflow's three end states carry `executor: "script"` as filler, and the check read that as three script steps missing their config (`Script config required: "No deliveries", "Accepted", "Rejected (email sent)"`). The definition was valid; only the client-side guard rejected it, so the **Save** button was unreachable for a workflow the API would have accepted. The check now requires script config where the *plugin* requires it, through a [`scriptConfigKeyForPlugin`](packages/platform-core/src/schemas/workflow-definition.ts) extracted from the server-side rule and shared by both, rather than asking any `executor: "script"` step for a `script` or `databricks` block. That closes the mirror-image bug in the same guard: a `databricks-job` step carrying a `script` block satisfied "either config is present" client-side and was then rejected by the server, which pairs each plugin with the one config key it reads. The message names the block the step is missing (`Missing step config: "Run job" needs a databricks block`) rather than asserting any script config will do. The plugin-required check already skipped terminals, which is why only this branch tripped.
- **An Image Catalog entry catalogued before its image was built no longer reads "Capabilities not probed" for ever.** `refreshEntryCapabilities` had exactly two callers, `create-entry` and `update-entry`, so capabilities were only ever refreshed by a write — and the ordinary build-mode sequence has no write after the build: catalogue the entry, then let the first run build the image. The entry sat unprobed until somebody happened to edit it for an unrelated reason, and the picker, treating `unknown` as "offer it, do not vouch for it", kept offering the image with no suitability claim. `getImageCatalogEntry` now probes before it renders. The listing deliberately still does not — it runs on a 30 s poll across a whole catalog — so this is the read that is already user-initiated and already one entry at a time, and it reuses the daemon listing the view was going to fetch anyway. The read path asks a narrower question than the write path: a write re-probes anything not `known`, since retrying a failed image is what the editor is there for, while a read probes only what has **never been attempted**. Without that distinction an image the probe cannot answer for would start a container on every poll for as long as its card stayed open.

- The **Secrets** tab lists the keys a workflow is missing instead of an empty form. The preflight warning named them (`Secret 'VIKING_PASSWORD' not configured`) and the fix was one click away only if you arrived through the warning's own `?tab=secrets&setup=KEY` deep link — clicking the tab yourself opened "No secrets configured yet", leaving you to dig the names out of the step `env` blocks. The tab now prepopulates a row for every `{{KEY}}` the definition references, through a [`collectSecretReferences`](packages/platform-ui/src/lib/preflight-checks.ts) extracted from the check that produces the warning, so both read the same keys off the definition. Keys already set at workspace level are left out — they satisfy the reference too, and a row for one would be a prompt to duplicate it. A prepopulated row is a prompt, not an edit: **Save** stays hidden until you type a value, and rows still empty at save time are dropped rather than stored as empty strings, which would have silenced the warning without configuring anything. The workspace overview's problem list also links each missing secret to that tab — it rendered an action link for missing images only, so the one problem the reader could fix themselves was the one with nowhere to click. Three ways that prompt could have lied are closed with it: a key already stored with an empty value is marked **Needed by this workflow** rather than counted as configured; no save path stores an empty value, a pasted `.env` line of `KEY=` included, which would otherwise have overwritten the prompt and satisfied the "is this key configured" check with nothing; and `{{OAUTH:provider}}` references are left out of the list, since the runtime injects that token through the MCP binding's auth config and a secret named after the provider could never satisfy one — the preflight warning stopped naming them too. The tab also refuses to edit when the secrets read itself fails: **Save** atomically replaces the whole stored set, so editing against a set that never loaded would have deleted every secret the failed read did not return.
- Inserting a step on the workflow canvas no longer stacks the rest of the path on top of itself. The `+` on an edge anchored the new step below its parent and pushed the step it now feeds down to make room, but only that one step: everything further along the path — the step two arrows down, and the terminal **Done** dot — stayed exactly where it was, so the pushed step landed on top of its own successor and disappeared behind it. The push now travels the full length of the path, walking the steps in depth order so one pass carries the insertion down to the terminal. Only steps the insertion actually crowds move, so a gap dragged open further down survives the re-render [`workflow-graph-layout.ts`](packages/platform-ui/src/components/workflows/workflow-graph-layout.ts).
- Renaming a verdict in the step editor's **Routing** card no longer fights the typing. Each row was keyed by the verdict name and labelled with it, so every keystroke gave React a new key: the row remounted, the input lost focus after one character, and the label above it re-humanized the half-typed name (`approve` → `Approv` → `Approve d`). Rows are now keyed by position — which is what the `env` and header rows next to them already did — and the label reads **Verdict**, naming the field rather than echoing the value being edited. **+ Add verdict** also stopped silently doing nothing on the second click: it wrote the fixed key `new-verdict` into a `Record`, so a second unrenamed verdict overwrote the first instead of appearing, and the row count never moved unless you renamed each one before adding the next. It now takes the next free name via `uniqueSlug` (`new-verdict`, `new-verdict-2`, …), the same helper the step-id field uses. **+ Add header** (`X-Header`) and **+ Add variable** (`NEW_VAR`) wrote fixed keys into a `Record` the same way and are fixed with them — through a new [`uniqueName`](packages/platform-core/src/utils/slug.ts) that suffixes a name without slugifying it, since `uniqueSlug` would have renamed those to `x-header` and `new-var` and broken the conventions they follow. `uniqueSlug` now delegates to it [`step-editor.tsx`](packages/platform-ui/src/components/workflows/workflow-editor/step-editor.tsx).
- Pages stopped rendering two headers. `nav.js` and `theme.css` are cached for four hours at the edge while the HTML that depends on them is not, so the SEO-audit deploy paired fresh pages - which now ship a `<header>` of their own - with a stale `nav.js` that still prepended one. Both assets are now requested with a content hash (`nav.js?v=<sha>`), so new HTML can only ever load the script it was built against, and `generate-og-cards.ts` fails the build when a page's hash does not match the file. `nav.js` also had to stop deriving its path prefix with a regex anchored at `nav.js$`, which a query string defeats.
- The setup wizard has its header styling back. It never linked [`theme.css`](docs/theme.css), so moving the header CSS out of `nav.js` and into that file left the page with an unstyled header and no `@font-face`.
- The homepage no longer reproduces a case study verbatim. It carried the whole four-step data-delivery walkthrough - 196 shared ten-word runs against the page it was teasing, around 340 words - which is the internal duplication the audit flagged, understated: it named two pages at 150-200 words each, and it was one page at twice that. Replaced with two distinct hooks linking the studies and the new index; overlap is now three runs, all footer boilerplate.
- The FDA principles page cites the sources that actually show the joint authorship it claims. The audit doubted the EMA attribution because the FDA-hosted PDF names no co-author anywhere in its two pages, which is true - but FDA's landing page states that CDER and CBER "collaborated with the European Medicines Agency (EMA)", and EMA publishes its own copy of the same document. The claim stands; it was the citation that pointed at the one artifact not evidencing it.
- Every image on the site states its dimensions, so a page no longer reflows as pictures arrive. None of them did, which the audit flagged as a Cumulative Layout Shift risk. Values are the files' own intrinsic sizes, read off the images rather than typed. The one exception is the lightbox placeholder, which has no `src` until JavaScript gives it one and sits outside layout flow.
- Every page declares the favicon that had been sitting in the repository unused. Only the setup wizard linked [`favicon.ico`](docs/favicon.ico); the rest relied on `nav.js` creating the `<link>` at runtime, so a crawler never saw one and the audit reported the site as having no favicon at all. With all eight pages declaring it the runtime patching is gone, and `nav.js` no longer touches `<head>`.
- GitHub reads the licence as Apache-2.0 rather than `NOASSERTION`. [`LICENSE`](LICENSE) carried the 15-line short notice *above* the canonical Apache text, which is enough to defeat the match GitHub's licensee runs, so an adoption blocker for regulated buyers turns out to have been a formatting accident rather than a licensing question. The file is now the canonical text verbatim and the notice moved to [`NOTICE`](NOTICE), where Apache-2.0 section 4(d) expects attribution to live.
- The "Meet the team" block on both case studies names the team. It sat above three avatars called `author-1/2/3.jpg` with alt text `Author 1/2/3`, so the page claiming you would speak to the people who built this was the clearest evidence on the site that a template had not been filled in. The photographs were always real; the naming was not. Files renamed to the people in them - Marek Rogala, Filip Stachura and Paweł Przytuła - and the names now appear in the copy and the alt text.
- The site navigation is in the HTML. Every page's header, including the only link back to the homepage and the only link to `/setup/`, was injected by `nav.js`, so in served markup the two case studies were orphans and `/setup/` was reachable only from inside a script. Each page now ships a `<header>` with the full link set and `nav.js` replaces it to add the dropdown, the mobile drawer and active states. The header's CSS moved from a runtime `<style>` into [`theme.css`](docs/theme.css), without which the served header would have flashed unstyled, and `nav.js` is now `defer` - with the navigation already parsed there is nothing for it to block.
- The FDA principles page is parseable, and describes itself. Its ten principles - the page's whole content and the most citation-worthy thing on the site - were `<span class="principle-title">`, invisible to any heading outline, so the page offered one `<h2>` and nothing else; they are now `<h3>` under a `How Mediforce addresses each principle` `<h2>`, which also stops the outline jumping from `<h1>` straight to the About section. It had no `<meta name="description">` at all, so search results and its share card had no summary to show.
- The workflow editor canvas shows every path a workflow can take, instead of one branch at a time. Any step with more than one outgoing edge — a `decision`'s verdicts, but equally a `creation` step with several `when:` transitions — was rendered as an accordion: the branch rows listed each option, but only the expanded one had its downstream steps laid out at all, and the others were skipped by the traversal that builds the nodes. On `tealflow-auto-code-generation` that meant **Provide Application Data** existed on the canvas only while `upload_data` happened to be the selected row, so reading the workflow meant clicking through the branches and holding the shape in your head. [`workflow-graph-layout.ts`](packages/platform-ui/src/components/workflows/workflow-graph-layout.ts) replaces the single-column accordion with a layered layout: depth by longest path, so a step always sits below everything that feeds it, and branches fan out into columns. Because edges are drawn straight between node centres, a column is reserved across the layers a level-skipping edge crosses and the other steps of those layers are pushed aside, so the `use_demo` arrow drops past **Provide Application Data** rather than through it. Loops keep their amber arc and their card row — the row names where the loop lands, which the arc's far-off midpoint label did not — and every forward edge now carries its verdict name and its **+** add-step button, including the branches that used to be collapsed — inserting on a branch keeps that branch's `when:` condition on the edge into the new step, and the new card is anchored in a free column rather than on top of the sibling branch. Two verdicts landing on the same step share one arrow labelled with both, matched on whole verdict names so `approve_only` cannot swallow `approve`. Edges are handed to the canvas in traversal order, so the topmost step still owns the first inline **+** — with verdict edges emitted ahead of transitions, adding a step from the canvas landed it beside the terminal instead of at the top of the workflow. **Tidy up** re-runs the same layout rather than its own copy of it.
- Model rankings refresh themselves, and the amber "Model rankings outdated — run `python3 scripts/sync-model-rankings.py`" banner is gone. OpenRouter dropped `requests` from its public `/api/v1/models` payload, so the ranking branch in [`syncFromOpenRouter`](packages/platform-infra/src/sync/openrouter-sync.ts) had been filtering an always-empty field: every sync reported `rankingsUpdated: 0`, `rankingsUpdatedAt` never moved, and the only thing that ever wrote a request count was a human running the Python scraper against their own instance — which is what the banner nagged every admin to do. The sync now reads the rankings feed itself (`/api/frontend/v1/rankings/performance`, the one the site's own /rankings page uses) as a second fetch, and a failure there is logged and skipped so a catalogue sync still lands. `syncRegistryIfStale` (formerly `eagerSyncIfStale` — it is no longer boot-only) gains a third caller in the cron heartbeat, whose new sweep refreshes a registry that has gone 24h without a sync, so a server that has not been redeployed in weeks stays current with no per-server cron entry and no script to remember. `scripts/sync-model-rankings.py`, the banner, and `use-rankings-age.ts` are deleted, as is `migrate-with-sync.ts` — its docstring claimed the migrate container ran it after `drizzle-kit migrate`, but that container's `CMD` is only `drizzle-kit migrate` and nothing else referenced the file, so the boot-time sync has in fact always come from `getPlatformServices()`. The unattended path gains an off switch, `ENABLE_MODEL_SYNC=false` — for an estate with no outbound route to openrouter.ai, and for the E2E servers, whose model-registry journey was asserting against ~400 live models re-sorted by a live popularity ranking instead of its own three-model fixture. `Sync Now` is unaffected: the flag gates the automatic path, not an operator who explicitly asked.
- **Human actions** is scoped to the workspace you are in, and every row links into the workspace its own run belongs to. `/[handle]/tasks` asked `GET /api/tasks` and `GET /api/cowork` on the caller-scope axis with no workspace, so it listed every task and cowork session across *all* workspaces you belong to — then built each row's link from the handle in the URL. A row from another workspace resolved to a workflow that does not exist under that handle, so it redirected to a 404 and raised an "Item unavailable" toast. On a real inbox that was 191 of 352 rows unclickable by construction. Both queues now pass `namespace`, which `tasks.list` already supported and `cowork.list` gains here (contract, route, handler, client, and `AuthorizedCoworkSessionRepository.list`); the task deep-link redirect forwards to `instance.namespace` instead of the handle it was reached under; and the table resolves each row's workspace through the run map rather than assuming the page's. Both queues carry the workspace selection in their react-query keys, so switching workspace cannot serve one inbox under another's handle. Pre-existing — the role-filtered page was equally workspace-blind [#1251](https://github.com/Appsilon/mediforce/issues/1251).
- The step editor no longer warns that "no one holds" a role while it is still finding out. [`useWorkspaceRoles`](packages/platform-ui/src/hooks/use-workspace-roles.ts) computed `heldRoles` off an empty member roster whenever the roster was in flight or had failed, and `AllowedRolesField` reads that as an authoritative answer — so every configured role flashed amber on a slow load, and stayed amber forever after a failed one, since the member hook surfaced no error. `useNamespaceMembers` now reports whether `members` is the roster the server returned, and an unresolved roster keeps `heldRoles` at `null`, which the field already treats as "not known" and stays quiet about [#1252](https://github.com/Appsilon/mediforce/issues/1252).

- The homepage's "How it works" heading no longer claims the platform handles compliance. Software does not handle compliance, the site's own FDA page says as much, and the paragraph directly under the heading already stated what the platform actually does, which is enforce the Control Mode assigned to each step. Every other compliance mention on the site was already hedged correctly - "supporting" IQ/OQ/PQ documentation, "21 CFR Part 11 aligned" - so this was one line, not a sweep.
- A workspace owner can find the control that changes a member's role. It was the role badge itself wrapped in a `<button>` whose only affordance was a `title` tooltip — no dropdown, no chevron — so the feature read as absent to everyone who had not been told it was there. It is now a labelled `<select>`, which also replaces the toggle semantics: picking `admin` sets `admin` instead of flipping to whatever the other value happened to be. Still owner-only, matching `updateNamespaceMemberRole`'s `assertCallerIsNamespaceOwner` gate — the UI was never stricter than the API, it was just invisible [#1288](https://github.com/Appsilon/mediforce/pull/1288).
- `MEDIFORCE_ATTACHMENT_MAX_BYTES` now reaches the process that reads it. The knob was documented in `.env.example` and honoured by [`_limits.ts`](packages/platform-api/src/handlers/tasks/attachments/_limits.ts), but [`docker-compose.prod.yml`](docker-compose.prod.yml) never forwarded it to `platform-ui` — the service hosting the upload handler — so lowering the attachment ceiling on a deployment silently did nothing and every upload kept the 100 MiB default. Forwarded as `${MEDIFORCE_ATTACHMENT_MAX_BYTES:-}`, which staging inherits through its overlay; unset still means the default, since the resolver treats an empty value as absent.
- Every CLI example in the docs site now runs. `website/docs/` had drifted to a positional argument style the CLI does not accept — `run start my-workflow`, `workflow register ./f.wd.json`, `secret set KEY` — and [`define-command.ts`](packages/cli/src/define-command.ts) rejects extra positionals outright, so each of those 16 examples exited 2 with "Unexpected positional arguments" rather than doing anything. The real shapes are `--workflow`, `--file` and `--key`; the `namespace` family takes `--handle`, not `--namespace`; `workflow copy` needs `--target-namespace` and `workflow import` needs `--path`; `assistant ask` takes `--definition`, not `--canvas`; `task complete` takes `--payload '{"kind":"verdict","verdict":"approve"}'`, not `--verdict`. Three CLI-reference rows named commands that do not exist: `workflow trigger <name>` (it is eight subcommands — `trigger-list|add|update|start|stop|remove|export|import`), `run bulk` (`run bulk-cancel` / `run bulk-archive`), and `model validate <workflow>`, which in fact takes comma-separated **model IDs** and never sees a workflow. `agent get`/`set-visibility` address an agent by ID, not name; `system credits` requires `--namespace`; and `--namespace` on the listing commands was documented as scoping a result it is not even read for, since `workflow list` and `agent list` take no arguments and return everything the key can see. `system images` / `rmi` / `disk` were missing from the reference. Two claims contradicted [`docs/`](docs/guides/verify-a-workflow.md) and the source: a Dry Run's `spawn` action was described as starting **real** runs on three pages, where [`spawn.ts`](packages/core-actions/src/handlers/spawn.ts) propagates `dryRun` to every child — it is the one action that knows about dry runs — and the step-type tables listed three of the four types, omitting `review` (the enum is `creation | review | decision | terminal`). The cron section also now states the constraint [`cron-utils.ts`](packages/workflow-engine/src/triggers/cron-utils.ts) enforces at save: five fields, UTC, minutes on the quarter hour, so `*/5 * * * *` is refused rather than silently firing a third as often as asked.
- `pnpm check:docs` passes on the docs site instead of failing it. [`check_doc_links.py`](scripts/check_doc_links.py) called seven links broken that Docusaurus resolves correctly — an extensionless target (`../run/verify`) is a live link under `website/` and a dead one everywhere else, because GitHub renders the rest of the repo and does not fill in the extension — so it now resolves `.md`, `.mdx` and `/index.md` under `website/` only. Its `SKIP_DIRS` name match also hid a page from every check: `build` names generated output, but it is equally a docs section (`website/docs/build/`), so the name now only counts until the first `docs/` segment. That page's links had never been checked.
- `prod.mediforce.ai` no longer 525s at Cloudflare: Caddy now auto-provisions TLS certs and proxies for an optional `DOMAIN_ALIASES` list alongside the primary `DOMAIN`, so the new production hostname works while `phuse.mediforce.ai` keeps serving for backward compatibility [#1198](https://github.com/Appsilon/mediforce/pull/1198).
- Google SSO no longer bounces sign-in from `prod.mediforce.ai` over to `phuse.mediforce.ai`: `AUTH_URL`, `NEXT_PUBLIC_APP_URL`, and `DOMAIN`/`DOMAIN_ALIASES` on the prod server were still pinned to `phuse.mediforce.ai`, so the OAuth `redirect_uri` and the CSRF cookie's host disagreed whenever a login started on `prod.mediforce.ai`. Repinned to `prod.mediforce.ai` as canonical on the server, and `.env.example` now documents the requirement so the next multi-domain setup doesn't repeat it [#1198](https://github.com/Appsilon/mediforce/pull/1198).
- The save-version and "Before you start" dialogs no longer open with a four-rung diagram of the verification pipeline above the thing you came to do. Naming a version and reviewing what is actually missing are the decisions on those screens; the ladder restated platform vocabulary every time either dialog opened, pushing the version field and the warnings down the modal. The pipeline stays documented in [`docs/guides/verify-a-workflow.md`](docs/guides/verify-a-workflow.md), and the honest "some checks could not run" signal survives as one sentence in the readiness summary rather than a rung state — on every variant of that dialog, including a run that asks for trigger input. A "How verification works" link keeps the guide one click away, which was the only part of the ladder a user could act on [#1233](https://github.com/Appsilon/mediforce/issues/1233).
- Creating a workflow saves it into the workspace you are in, on all three save buttons. The namespace picker defaulted to the first workspace you belong to — usually your personal one — so a workflow started from an organisation workspace was written there while the redirect looked for it here, reporting "Workflow definition 'x' v2 not found" and "Item unavailable". **Save**, **Save & Dry Run** and **Save & Start Run** now all follow the workspace the save actually targeted: the redirect goes where it saved, and the run starts where the version exists rather than in the workspace named in the URL. The picker also stops displaying a workspace it would not write to — a route handle you are not a member of, or the moment before your workspaces have loaded. The dead `yaml-editor.tsx`, carrying the same first-workspace default with no consumers, is deleted [#1234](https://github.com/Appsilon/mediforce/issues/1234).
- The community Discord invite in `README.md` and [`docs/concepts/how-we-work.md`](docs/concepts/how-we-work.md) was dead — `discord.gg/Hkb2K7YE` returns `Unknown Invite`, while every page of the public site (`index.html`, `security.html`, `validated-ai.html`, `fda-principles.html`, the two case studies and `nav.js`) has long pointed at the live `discord.gg/TVx4VkG3C2` server. Both now match the site. `pnpm check:docs` never caught it because [`scripts/check_doc_links.py`](scripts/check_doc_links.py) resolves repo-relative paths only and skips external URLs by design. `how-we-work.md` had not been touched since it was written on 2026-03-04 and opened by claiming "we don't have a polished landing page or a sales deck", which stopped being true when mediforce.ai shipped; its intro now states what building in public actually means here (public repo, ADRs, feedback from the people running the processes). Its "Contributing" and "Staying Updated" sections, which listed Discord twice between them, are one **Getting Involved** ladder, and the three audiences it recruits finally get an on-ramp each: [`GETTING-STARTED.md`](GETTING-STARTED.md) and [`concepts/architecture.md`](docs/concepts/architecture.md) for developers, an issue link for domain experts, and [`contributing/ai-development-process.md`](docs/contributing/ai-development-process.md) off the Code-First section, none of which the doc linked before.
- [`docs/research/cowork-streaming.md`](docs/research/cowork-streaming.md) said transactional finalize "requires Postgres (post-ADR-0001) — defer", and the doc comment on [`finalize.ts`](packages/platform-api/src/handlers/cowork/finalize.ts) called it "pending Postgres migration". [ADR-0001](docs/adr/0001-firestore-to-postgres.md) has landed: there is no Firestore left in `platform-infra`, and drizzle's `db.transaction()` is already used by four repositories including `cowork-session-repository.ts`. The real blocker is a cross-repo transaction seam — `CallerScope` hands out each repository independently, so nothing can wrap the audit/runs/engine writes together. Both now say that. The note also lost ~18% of its words: the body `**Status:**` line duplicated the frontmatter, "Scope of this note" and "Background" made the same point twice, and the closing three-item follow-up list restated each section's own conclusion as well as the identical list in [`headless-migration.md`](docs/archive/headless-migration.md) and issue [#516](https://github.com/Appsilon/mediforce/issues/516).

### Added
- [`docs/404.html`](docs/404.html), so a wrong URL lands on the site rather than GitHub's unbranded page. It ships the same header and dotted ground as every other page, which item 8 made almost free by moving that CSS into `theme.css`. It is `noindex, follow`, and teaching `generate-og-cards.ts` to skip a noindex page is what made that possible: the run otherwise demands a canonical, a card and an `llms.txt` line from every page it finds.
- [`/case-studies/`](docs/case-studies/index.html) exists, so the two studies are no longer reachable only from a header link. It carries the written summary of each workflow, which is what let the homepage stop reproducing one of them, and it makes three-level `BreadcrumbList` structured data possible on both studies - the reason the earlier JSON-LD commit stopped at two blocks. `Case Studies` in the header now points at it rather than listing both children flat.
- [`docs/llms.txt`](docs/llms.txt) tells an assistant what Mediforce is and, explicitly, which of the other seven companies called Mediforce it is not. Written by hand rather than generated, because llms.txt is a curated guide and not a dump of page titles - but `generate-og-cards.ts` now fails the build when a page is missing from it, so it cannot quietly fall behind the site the way a hand-maintained list otherwise would.
- [`README.md`](README.md) links to mediforce.ai, which it never did. The repository is the most AI-crawlable surface the project has, since GitHub blocks none of the crawlers Cloudflare was blocking, and it pointed at the site nowhere. `Docs` in the header row now goes to the published docs rather than an anchor further down the same file.
- Every marketing page has an Open Graph and Twitter card, and the image is generated from the page itself. [`scripts/generate-og-cards.ts`](scripts/generate-og-cards.ts) reads each page's `<title>`, `<meta name="description">` and `<link rel="canonical">`, lays a 1200x630 card out with satori and rasterises it with sharp, so a new page gets a card by existing rather than by someone remembering to draw one. Cards are gitignored and written during the Pages build, which is why `pnpm install` now runs before Jekyll rather than after it. The run fails when a page's `og:image` names a card it does not write, so a renamed page cannot quietly ship a tag pointing at a 404. Colours come from `docs/theme.css` and the two faces the pages already use are committed under [`scripts/assets/fonts/`](scripts/assets/fonts/README.md), so a render needs no network and is identical on every machine.
- Each workflow card on the workspace home carries the version **Start Run** will actually start, next to the button — a `v3` control that opens that version's definition, so reading and opening what is live no longer means clicking through to the workflow page first. The version was already on the card as grey metadata, but it named the *latest* version while `StartRunButton` starts `defaultVersion ?? latest`: on a workflow pinned to an older default the card said v2 and the button ran v1. Both the new control and the header metadata now read a single `effectiveVersion` carried on the card data, mirroring what [`useWorkflowVersions`](packages/platform-ui/src/hooks/use-workflow-versions.ts) resolves, so the two can no longer name different definitions. The header pairs the version with the workflow title only when they belong to the same version — the list read returns the latest definition alone, so a pinned card shows its version unadorned rather than borrowing another version's title.
- [ADR-0022](docs/adr/0022-image-catalog.md) records the **Image Catalog** — the object Mediforce lacks, which is *"an image the platform offers for steps"* as opposed to *"an image the Docker daemon happens to have"*. Paper only: no table, no handler, no view, and the step-editor picker still maps `docker images` into a `<select>` exactly as before. It settles the six decisions that cross package boundaries so epic [#1292](https://github.com/Appsilon/mediforce/issues/1292) can proceed in parallel without relitigating them. An entry is keyed on its **source** — `(repo, dockerfile)`, or the untagged reference for an image built elsewhere — and **not** on the commit, so a pin bump becomes a second version of one entry instead of another top-level row, which is the structural answer to "we change one `COPY`d script and get another near-identical image"; `deriveBuildTag` is untouched and the commit stays in the image tag, because a distinct build must keep a distinct tag or the local cache serves the previous commit's binary. Runtimes, agent-capability and lineage are **derived** (probe, layer-prefix containment) and the only declared field is a one-sentence **intent**, because a hand-written capability list drifts silently — the failure AGENTS.md §11 and [ADR-0017](docs/adr/0017-retire-llm-maintained-wiki.md) exist to prevent — and intent survives a rebuild where "R 4.4, Python 3.11" does not. Entries are namespace-scoped Postgres rows with no cross-namespace visibility flag (the daemon is already deployment-wide, so the catalog is not an isolation boundary and an absent entry is never a denial), writable by any workspace member rather than admin-gated like the Tool Catalog, because an image entry executes nothing and no Workflow Definition ever references one — deleting an entry removes an offer, never a capability. The Image Catalog and the Tool Catalog are **siblings**: golden rules §7's "runnable ≠ selectable" one level down, and an image shipping an MCP executable still needs its own Tool Catalog entry. The catalog curates the **picker only** — a step may still name any image string, since an allowlist would break every existing definition (§12), reject imported packages ([ADR-0013](docs/adr/0013-workflow-packages-outside-platform-repo.md)) and refuse images the runtime can start. The ADR fixes the vocabulary — entry, version, intent, capability, lineage, base — and pins **golden image** to one meaning: the deployment's own agent-capable base image, not a quality tier; those names move into [`CONTEXT.md`](CONTEXT.md) with the first object that actually ships, rather than ahead of it [#1293](https://github.com/Appsilon/mediforce/issues/1293).
- The **Image Catalog** exists as an object: `mediforce images list` answers *"which images does this workspace offer for steps, and what is each one for?"* — the question a raw `docker images` dump has never been able to answer. An entry is keyed on its **source**, either `built` `(repo, dockerfile)` or `referenced` (an untagged image reference such as `mediforce-golden-image`), and deliberately **not** on the commit, so two builds of one source at different commits are one entry with two versions rather than two near-identical rows ([ADR-0022](docs/adr/0022-image-catalog.md) decision 1). Repo spellings are collapsed first — `Appsilon/tealflow`, `git@github.com:Appsilon/tealflow.git` and the HTTPS form all canonicalise to the URL [`buildProvenanceLabelArgs`](packages/platform-core/src/utils/image-provenance.ts) writes into `mediforce.build.repo`, so an entry matches the images actually on the daemon and one image cannot become two entries. `intent` — one sentence saying what the image is *for* — is required by the contract, not by a form, so an entry nobody can read is not expressible.
  - **Versions are not stored, and that absence is the design.** A version *is* a daemon row whose `mediforce.build.*` labels match the entry's source, recomputed on every read from the labels [#1285](https://github.com/Appsilon/mediforce/issues/1285) added — so "two builds, one entry, two versions" needs no write path at all, and the catalog cannot drift into a second source of truth about what images exist (ADR-0022 decision 2). The only stored fields are the key, the name, the intent, and an optional **declared** source reference for a pushed image the platform never built — the one place a human may state provenance, because OCI labels are inherited and reading them off such an image points at the base image's repository.
  - **A missing image is a state, never an error.** An entry whose image is gone from the daemon still lists, marked `absent`; an unreachable daemon leaves every entry `unknown` rather than failing the request or emptying the list (AGENTS.md §13). Nothing 404s because Docker had a bad day.
  - **Any workspace member may write, unlike the admin-gated Tool Catalog**, because an entry executes nothing — it names an image string an author can already type into the step editor's free-text field, and every fact on it that could mislead is derived rather than typed (ADR-0022 decision 3). Every write is audited as `image_catalog_entry.created|updated|deleted`. The source is the key and is not editable: a PATCH that tries to re-point it is a 400, not a silent no-op.
  - Ships as `image_catalog_entries` (migration `0051`), a `platform-core` repository port with an in-memory double, Zod contracts, five `(input, scope) => output` handlers, route adapters under `/api/image-catalog`, `mediforce.imageCatalog.*` on the client, and `mediforce images list|show|create|update|delete`. **Nothing reads the table at run time** — no Workflow Definition references an entry, the step-editor picker still maps `docker images` into a `<select>`, and Admin → Infrastructure still shows raw daemon truth — so an empty table is exactly today's behaviour (§12). `mediforce images` and `mediforce system images` are deliberately different things: the first is the curated per-namespace catalog, the second the raw deployment-wide daemon listing an admin uses to hunt disk; [`docker-image-setup.md`](docs/guides/docker-image-setup.md) now says which is which. Capabilities (#1295), lineage (#1296), the catalog view (#1297) and the picker (#1298) are the rest of epic [#1292](https://github.com/Appsilon/mediforce/issues/1292) [#1294](https://github.com/Appsilon/mediforce/issues/1294).
- Every image the platform builds now carries its **provenance as labels**, and the daemon listing reads them back. A build-mode step that omits `image` runs under `mediforce-built:<12 hex of sha256(repo \0 commit \0 dockerfile)>` — the shape the schema documents and encourages — so half the image list was opaque by construction: one more row per pin bump, and nothing on it saying which repo, which Dockerfile or which workflow. The builder already wrote `mediforce.build.commit` and the listing never asked for it. Both builders (in-process and `container-worker`) now write `mediforce.build.repo`, `.commit`, `.dockerfile`, `.workflow` and `.namespace` from one shared [`buildProvenanceLabelArgs`](packages/platform-core/src/utils/image-provenance.ts), and `DockerImageInfoSchema` gains the matching optional `buildRepo` / `buildCommit` / `buildDockerfile` / `buildWorkflow` / `buildNamespace` fields. Every field is optional and absent labels are the normal case, so an image built before this change — or pulled from a registry — lists, sorts and deletes exactly as it did (§12, §13).
  - **The OCI keys are emitted too, and that is a correctness fix rather than interoperability.** Labels are inherited from the base image, so a local image of ours reports `org.opencontainers.image.source = https://github.com/rocker-org/rocker-versioned2` — inherited from `rocker/tidyverse` through the golden image — and any tool reading `.source` off one of our images today gets a confidently wrong repository. Writing our own `source` and `revision` overrides the inherited values.
  - **Reading the labels needs a second daemon call, and the obvious template breaks it.** `docker images` cannot emit labels (its template context has no `.Labels` field), so provenance costs one `docker image inspect` over the distinct ids. The dotted form `{{json .Config.Labels}}` fails the *whole* invocation on an image that has no labels at all — one `postgres` row strips the provenance off every other image in the batch — so the shared format uses `{{json (index .Config "Labels")}}`, verified against a real daemon by an integration test that inspects a labelled and an unlabelled image together. A failure of that call leaves rows unannotated rather than failing the listing.
  - **"Used by" stops being blind to exactly the rows that need it.** `GET /api/workflow-definitions/by-image` matched a literal `step.agent.image` / `step.script.image`, but a derived tag exists *because* the step left `image` unset — so every `mediforce-built:*` row reported no workflows. The scan now recomputes the tag through the same `resolveBuildSource` the runtime uses, workflow-level `externalSkillsRepo` fallback included, so a derived-tag row names its workflows and steps. The `docker build` invocation also moves from an interpolated shell string to an argv list, since the new label values carry a repo URL, a workflow name and a namespace; the repo URL is redacted first so an authenticated HTTPS clone cannot bake its token into an immutable label. Foundation for epic [#1292](https://github.com/Appsilon/mediforce/issues/1292) — the search box and picker changes this issue originally proposed are superseded by #1297 / #1298, which build a catalog view rather than enriching the raw daemon list [#1285](https://github.com/Appsilon/mediforce/issues/1285).
- Four **built-in roles** ship with every deployment, and a workflow starts out gated by them. `editor` may change a workflow, `executor` may run it, `reviewer` may act on its human steps, `workflow-manager` all three. They are ordinary free-form roles — nothing in the gate recognises the names — but the platform now writes them into the two places that already enforce: registering a workflow's **first version** seeds its Access tab with `run: [executor, workflow-manager]` / `edit: [editor, workflow-manager]`, and a human block added in the editor starts at `allowedRoles: [reviewer, workflow-manager]`. Both are visible and editable on the screens that own them, and clearing a list still means "any workspace member". So granting somebody `executor` now lets them run the workflows a workspace creates, with no per-workflow configuration pass. Two grants keep the default from being a lockout: a workspace's owner holds `workflow-manager` from creation (migration `0045` backfills existing owners and admins), and whoever registers a workflow holds it narrowed to that workflow — otherwise the seeded `edit` list would refuse the author their own second Save. Workflows registered by automation (the CLI, imports, seeded builtins) stay open, because a system actor has no uid to grant, and **no existing workflow is touched** [ADR-0020](docs/adr/0020-built-in-roles-and-default-workflow-access.md).
  - On a workflow the default is a **floor**, not a starting point: a verb you restrict always keeps the built-in role that carries it, so restricting `run` to `qa-lead` stores `[executor, workflow-manager, qa-lead]` and the Access tab shows those two as locked chips. Each verb gains an explicit **restrict** toggle, which is now the way back to "any member". An unrestricted verb is never raised — an empty list stays "any workspace member". The workspace owner's `workflow-manager` is likewise re-established on every write from the **Roles** table, where its row has no Remove control: seeding it once was not enough, because a single full replace from that table left a workspace with gated workflows nobody could reach and nobody able to grant the role back. Migration `0046` brings existing rows into line — owners regain the grant, and lists that already restricted gain their floor.
  - **A `workflow-manager` can complete any human step**, whatever its `allowedRoles` say — including a step an imported package restricted to a role this deployment has never granted. Applied where the gate reads rather than written into the Workflow Definition, which travels between deployments, so no definition is rewritten and the step editor still shows exactly what the author wrote. `reviewer` deliberately gets no such standing despite carrying the same verb: 23 definitions in this repo already name it as an ordinary process role, and widening it would let a grant made for one step claim every other one. The refusal message now names `workflow-manager` too, so a reader who is refused learns which role opens the step.
  - **A personal workspace's owner holds `workflow-manager` too.** It is bootstrapped by `GET /api/users/me`, not by the create-workspace handler, so it was the one workspace whose owner got the grant only where migration `0046` reached — a personal workspace created after that migration ran had nobody holding the role every gated list and every restricted step names, and a workflow imported into it by the CLI whose step restricts, say, `reviewer` was one its own owner could not pick up. The bootstrap now writes the same grant `createNamespace` does.
  - **A workspace owner can now run and change every workflow in their workspace.** They hold `workflow-manager` and every restricted list keeps it, which reverses in practice [ADR-0019](docs/adr/0019-workspace-scoped-roles.md)'s "no owner/admin override" — with the difference that reasoning cared about: no branch in the gate, just a grant visible in the Roles table and a name visible on the Access tab. Locking an owner out of a workflow in their own workspace is no longer expressible, which is what the demo workspace had accidentally done.
  - The role pick-lists — the step editor, the Access tab and the workspace **Roles** table — always offer the four, each with a one-line description of what it grants. `UserDirectoryService` gains `grantRole`, the additive, membership-checked sibling of the full-replace `setRolesForUser`.

- A workflow gets an **Access** tab, and running or changing it can be restricted to a role. Until now no workflow mutation was gated by anything beyond workspace membership — `register`, `delete`, `archive`, `transfer`, `set-visibility` and `set-default-version` checked that the caller was a member and nothing else, not even owner/admin, so **any member of a workspace could delete or re-register any workflow in it**. The tab exposes two role lists, administered by owner/admin: `run` (starting a run) and `edit`, one verb covering all six mutations, because delete is strictly more dangerous than edit. Below them it shows who actually holds each role *for this workflow*, so an admin who gates on a role nobody holds is told rather than discovering it when the gate turns out to be a wall. Enforcement is [#1249](https://github.com/Appsilon/mediforce/issues/1249)'s `assertCallerHoldsRole`, so the semantics match the step level exactly: empty means any workspace member (no regression — every existing workflow is unchanged), otherwise the caller must hold one of the listed roles for *this* workflow, and system actors bypass, leaving cron and webhook firings unaffected. Storage is a side table keyed by `(namespace, workflow)`, not the versioned definition — registering v8 must not silently rewrite permissions — and it is cleared when a workflow is deleted or transferred. A same-workspace **copy inherits** its source's gate; without that, copying a restricted workflow to a second name would launder it. A member without the role sees the controls it gates carrying the reason — Start for `run`, and Save, Edit and the workflow's ⋯ menu for `edit` — rather than a 403 arriving as a raw API error beside whatever the step validator was already complaining about; the workflow list answers that question on the read that returns the cards, so a catalog of thirty costs no extra request. `mediforce workflow access` / `set-access` ask the same from a terminal. [ADR-0019](docs/adr/0019-workspace-scoped-roles.md) is now **finalized**, and `docs/reference/workflow-capabilities.md` disambiguates the three things called roles [#1253](https://github.com/Appsilon/mediforce/issues/1253).
- **Human actions** gains a workspace filter beside the *For me* / *All in workspace* toggle. It opens on the workspace in the URL — the one you navigated to — and a member can widen it to any set of the workspaces they belong to, or to **All workspaces**, which restores the cross-workspace roll-up the endpoint answered before it was scoped. The `namespace` axis on `GET /api/tasks` and `GET /api/cowork` now takes one workspace or several (repeatable `?namespace=`), normalised to a list by the contract so the handler reads one shape, and intersected with the caller's memberships by `AuthorizedScope.narrowToMemberships`: a selection naming a workspace you have since left still answers for the rest, and one naming only workspaces you cannot read is an empty list, never a 403. Rows stay clickable across the selection because each one carries its own workspace [#1251](https://github.com/Appsilon/mediforce/issues/1251).
- The **Assignee** column moves to the end of the Human actions table, after Created and Deadline.
- `step.allowedRoles` is enforced. It was authored on human steps, surfaced in the step editor, documented as "restrict who can act" — and checked by nothing: any member of the workspace could claim and complete any human task. `assertCallerHoldsRole(caller, namespace, workflow, allowedRoles, directory)` now lands beside `assertCallerIsNamespaceAdmin` in [`auth.ts`](packages/platform-api/src/auth.ts) and gates both [`claimTask`](packages/platform-api/src/handlers/tasks/claim-task.ts) and [`completeTask`](packages/platform-api/src/handlers/tasks/complete-task.ts), per [ADR-0019](docs/adr/0019-workspace-scoped-roles.md)'s `act` verb. The gate reads `allowedRoles` off the run's **pinned Workflow Definition**, not off the task: the engine copies only `allowedRoles[0]` into `HumanTask.assignedRole`, so a step allowing `['reviewer','approver']` would have enforced `reviewer` and silently dropped `approver` — resolving from the pinned WD fixes that truncation with no `human_tasks` migration and no way to drift from what the author wrote. Absent or empty `allowedRoles` still means any workspace member, so the many workflows that never set it are untouched. A grant narrowed to another workflow does not count — the handler passes the run's workflow precisely so `reviewer · workflow-B` fails the gate on workflow A, which is what makes the scope column mean something; the common allow costs no extra query, since `CallerIdentity.namespaceProcessRoles` already carries workspace-wide grants and only a narrowed one is resolved through the directory. apiKey/system actors bypass (engine, worker, CLI automation) and there is deliberately **no owner/admin override**: an admin who needs to act grants themselves the role first, which leaves an audit trail a silent bypass does not. A run whose pinned definition cannot be read at all — the version deleted, or the workflow transferred to another workspace, which rewrites the definition's workspace and leaves `process_instances.namespace` on the source — is refused rather than left ungated: making a definition unreachable is otherwise a bypass available to any member, since no workflow mutation is role-gated yet. Resolving is not sufficient either — version numbering restarts at 1 once the last version leaves a workspace, so registering the name again after a delete or a transfer plants a definition an in-flight run's `v1` pin would answer with; a version written after the run that pinned it is refused the same way. A definition that reads fine but does not describe the task's step is the opposite case and stays open, because it is a definite "no restriction declared" rather than an unknown. A step naming a role nobody in the workspace holds is unclaimable by anyone — correct, since an approval gate that opens when unconfigured is the worse failure — so instead of a generic 403 it says so and says the fix: "No one in this workspace holds 'reviewer'. An admin can assign it in workspace Settings → Members." #1248's seeding is what keeps that rare on day one. With enforcement in place [ADR-0019](docs/adr/0019-workspace-scoped-roles.md) moves to **accepted**, which partially supersedes [ADR-0002](docs/adr/0002-firebase-auth-to-nextauth.md) §5 (the global `user_roles` table); the rest of ADR-0002 stands. `RbacService` is deleted rather than switched on, together with the engine's dead 4th constructor slot and the client-side `AuthService` port it was the only consumer of: it resolved the caller through the browser's auth service (wrong side of the [ADR-0005](docs/adr/0005-headless-platform-api-ui-separation.md) boundary for a server authorization decision) and gated `advanceStep` — engine machinery running as the system actor — rather than the human action a user takes [#1249](https://github.com/Appsilon/mediforce/issues/1249).
- Authoring a step's role gate is a picker, not a comma-separated text box. `allowedRoles` in the [step editor](packages/platform-ui/src/components/workflows/workflow-editor/step-editor-roles.tsx) is now a chip multi-select suggesting the same vocabulary the Roles table offers — `useWorkspaceRoles(handle)`, the roles this workspace's members hold unioned with the `roles` its workflow definitions declare — and free entry still lands, because authoring a step for a role nobody has been granted yet is the normal order of work and a pick-list that refused it would be a validator the [ADR-0019](docs/adr/0019-workspace-scoped-roles.md) vocabulary is deliberately not. Chips rather than a text field because `reviewr` and `reviewer` were indistinguishable in a comma-joined string, and since [#1249](https://github.com/Appsilon/mediforce/issues/1249) that typo is no longer cosmetic: it is a task nobody can ever claim. Naming a role nobody holds **warns and saves** — a step editor that refused it would forbid the ordinary sequence of writing the workflow before granting its roles — and the warning asks the scoped question, not the workspace-wide one: `useWorkspaceRoles` gained a `workflowName` option and a `heldRoles` answer counting only grants in force here, so `reviewer` held on *another* workflow does not silence a warning about the step it cannot claim, which is the case the warning exists for. `heldRoles` is `null` rather than `[]` only while the member roster is still in flight, because "we have not asked" rendered as "nobody holds it" would flag every role on every step for as long as the fetch takes. Having no workflow name is **not** one of those unknowns: a workflow being authored has no name yet, so no grant can be narrowed to it and the workspace-wide grants are exactly what reaches it. Answering `null` there made the warning unreachable in `/workflows/new` — the editor where the whole of a new workflow is written, and where the typo is made — while working only in the version editor a saved workflow reopens into. `assignedTo` becomes a member combobox that still takes a template: a `datalist` fills in a uid without taking the keyboard away, so `${triggerPayload.userId}` stays typeable — the engine interpolates it at task creation, and a picker accepting only literal uids would have deleted dynamic assignment [#1252](https://github.com/Appsilon/mediforce/issues/1252).
- The Human actions inbox shows the work that is actually yours. `GET /api/tasks` gains an `actionable` axis: `actionable=true` keeps a task only if it is assigned to the caller, or is `pending`, unassigned and parked on a step whose `allowedRoles` the caller holds **for that workflow** — the same predicate [`claimTask`](packages/platform-api/src/handlers/tasks/claim-task.ts) enforces, shared as `resolveStepGate` + `callerHoldsRole` rather than reimplemented, since a list that answers "can act" its own way is how a UI ends up offering tasks the server then refuses. Omitting the flag returns exactly what the endpoint returned before, byte for byte. The page defaults to the narrowed view with an **All in workspace** toggle beside it, which is what makes the narrowing a default rather than a deletion (AGENTS.md §12) — someone who saw twelve tasks yesterday may see three today, and the twelve stay one click away. This also removes the *second* accidental single-role filter [#1247](https://github.com/Appsilon/mediforce/issues/1247) found and [#1249](https://github.com/Appsilon/mediforce/issues/1249) took the first half of: the page pivoted on `useViewerIdentity().role` — `session.user.roles[0]`, a namespace-free value — so a user holding two roles saw only the first one's queue and a user holding none fell back to everything in their workspaces. Both are now server-side questions, because the browser cannot answer them: the step's `allowedRoles` lives on the run's pinned Workflow Definition, not on the task, and `HumanTask.assignedRole` carries only `allowedRoles[0]`, the truncation every client-side mirror has inherited. A task claimed by somebody else is out of your inbox but still in the workspace view — not yours is not hidden. Resolving an inbox is batched per distinct run, per distinct pinned workflow version and per distinct `(workspace, workflow)` role question, so thirty tasks across three workflows cost one run read, three definition reads and at most three directory reads; the run read is a new five-column `RunDefinitionPin` projection rather than `SELECT *`, so a poll of this page never pulls the `variables` / `trigger_payload` / `previous_run` jsonb blobs it has no use for. `mediforce task list --actionable` asks the same question from a terminal [#1251](https://github.com/Appsilon/mediforce/issues/1251).
- Roles can be assigned from the product, not only from a terminal. Workspace settings gains a **Roles** table under Members — `Member | Role | Workflows`, one row per assignment — editable by owner/admin and read-only for everyone else, since knowing who the reviewer is, is what a member needs to route work. It is a table of its own rather than columns on the members table: a member holds any number of roles, each narrowed to any number of workflows, so a per-member row forces a multi-value record into one cell and the reader loses which workflows belong to which role. One row per `(member, role)` makes that correspondence the table's structure instead of something layout has to imply. The members table's own `Role` column is renamed **Membership** for the same reason — with a Roles table below it, a column headed "Role" meaning `owner|admin|member` is the `set-member-role` / `set-member-roles` confusion rendered on screen, and the CLI roster has drawn the same MEMBERSHIP / PROCESS ROLES line since [#1248](https://github.com/Appsilon/mediforce/issues/1248). The scope control is a checkbox list: storage keys a grant by `(role, workflow)` since that is what a gate looks up, but an admin granting `reviewer` on three workflows is doing one thing, not three. Checkboxes rather than a `<select multiple>`, whose second pick needs a ctrl/cmd-click nothing advertises and whose plain click silently *replaces* the selection — the control most likely to be used wrong would have been the one that quietly revokes a grant. **All workflows** is the first entry in that list and the state it opens in, rather than an inferred one: the grant is workspace-wide when no workflow is named, but "leave it empty and it means everything" is a rule the reader has to be told, where a checked box states it. Naming a workflow clears it, and it cannot be unchecked directly, because "no workflows at all" is not a grant this model can express — which is also why the last named workflow is locked: unchecking it would land back on the empty list the write reads as workspace-wide, so the gesture that looks like narrowing further would in fact grant the role everywhere, and revoking is the row's X instead. The scope control is likewise held closed until the workflow list resolves, and says so when the read fails rather than offering **All workflows** as though the workspace had none: a grant written while the narrower choices are still on their way is the widest grant there is. Narrowing therefore stays a deliberate second act — otherwise the roster fragments into per-workflow grants nobody can reason about ([ADR-0019](docs/adr/0019-workspace-scoped-roles.md)). A role holding a workspace-wide grant reads as unnarrowed even when narrowed rows sit beside it, because the `NULL` already covers every workflow and listing the others would claim a limit that is not in force. The role field is a `datalist` over a new `useWorkspaceRoles(handle)` — the roles this workspace's members already hold, unioned with the `roles` its workflow definitions declare, which is what stands in for the vocabulary table the ADR refuses to add — and it still accepts free text, because granting `reviewer` before any workflow names it is the ordinary first move and a pick-list that refused it would be a validator. Member and role filters plus a page-size pager keep the table usable past a handful of people: one row per `(member, role)` grows with both, so 80 members holding three roles each is 240 rows. Both are client-side, because `users.listMembers` already returns every member with their grants in one response. Writes go through `mediforce.namespaces.setMemberRoles()` as a full replace, so an add, a re-narrowing and a removal are the same call with a different end state, patched optimistically onto the members roster and overwritten by the server's echo. Reading grants back required the roster to stop flattening them: `users.listMembers` now returns `grants: [{ role, workflowName }]` where it returned a de-duplicated `roles: string[]`, because an editor that wrote a flat list back through a full replace would silently widen every narrowed grant its member holds — a new `UserDirectoryService.getGrantsForUser` supplies them on both backends. That read fails the whole roster rather than degrading to `[]` the way the contact-details lookup beside it does: an error rendered as "holds no roles" would make the next grant saved from that row revoke every grant the failure hid. `mediforce namespace list-members` follows suit and prints a narrowed grant as `role@workflow`, the notation the audit trail already writes, now shared as `formatRoleGrant` so the two cannot drift; it previously rendered "reviewer everywhere" and "reviewer on tealflow only" identically, which is the wrong-scope-invisible failure that command exists to catch, one dimension down [#1250](https://github.com/Appsilon/mediforce/issues/1250).

- The homepage carries `Organization` and `WebSite` JSON-LD. This is entity disambiguation: "Mediforce" maps to at least seven unrelated real companies, and nothing on the site previously stated in machine-readable form that this one is Appsilon's.
- Every marketing page carries a self-referencing `<link rel="canonical">`. GitHub Pages serves each one at both `/security.html` and `/security`, byte-identical and both 200, with nothing to say which of the two is the page. The `.html` spelling is the canonical one because that is what `nav.js` and every internal link already use.
- `mediforce.ai` now has a sitemap and a `robots.txt` pointing at it. Nothing generated one before: `jekyll-build-pages` ships no sitemap plugin and the marketing pages are hand-written HTML, so the only sitemap on the domain was the one Docusaurus emits for the `/docs/` half. [`docs/sitemap.xml`](docs/sitemap.xml) lists the eight marketing URLs in their `.html` canonical form and [`docs/robots.txt`](docs/robots.txt) points at both, and `pages.yml` fails the build if either file misses the artifact.
- Everyone at a configured email domain can be given a workspace automatically. `AUTO_JOIN_WORKSPACES` takes `domain:handle` pairs (`appsilon.com:appsilon`) and [`getMe`](packages/platform-api/src/handlers/users/get-me.ts) writes the membership on the next page load — the same lazy, idempotent bootstrap that already creates personal workspaces, so a config change lands without waiting for everyone to sign in again. Deliberately separate from `ALLOWED_EMAIL_DOMAINS`: that gates who may sign in, this gates where they land. Members join as `member`, and the join is **insert-only** — an owner's promotion to `admin` survives every subsequent load, where an upsert would have silently demoted them. A removal is remembered in `workspace_autojoin_blocks` (migration 0043) so `leave` and "remove member" are not no-ops in an auto-joined workspace; an explicit invite clears the tombstone and re-adds them. Both writes are transactional with the membership change they accompany, since a crash between them is exactly the silent re-join the table exists to prevent. The tombstone is its own table rather than a `left_at` flag on `workspace_members` because soft-deleting the member row would oblige every existing query to filter it, and the one that got missed would hand a removed user continued access — `getMembershipsForUser` feeds `caller.namespaces`. Unset means nobody is auto-joined, and a pair naming a workspace that does not exist is skipped rather than created, so a typo degrades to "off" instead of conjuring a workspace [#1288](https://github.com/Appsilon/mediforce/pull/1288).

- The product documentation site now has somewhere to live: **mediforce.ai/docs**, published by [`pages.yml`](.github/workflows/pages.yml). mediforce.ai turned out not to be in the CI pipeline at all — it is GitHub Pages' *legacy* builder serving `main:/docs`, which GitHub rebuilds itself on every push, so nothing in this repository built or published [`website/`](website/). A `docs.` subdomain was not available without a second host: a repository gets one Pages site and mediforce.ai holds it. The docs mount on a path instead, and one workflow composes both halves into a single artifact — Jekyll builds `docs/` to the site root, the Docusaurus build is copied in at `/docs`. That Jekyll step is load-bearing: every `.md` under `docs/` is rendered and already live on the public internet (`docs/README.md` serves as `mediforce.ai/README`, ADRs and guides alongside it), so publishing the folder as plain static files would have kept the `.html` pages and 404'd every Markdown one. Pull requests build the artifact without deploying it, which is also the only thing in CI that builds *or* type-checks this package — the root `tsc -b` has no `website` reference and `docusaurus build` transpiles without checking types, so the docs build was gated by nothing and a broken one would have surfaced at deploy. The trade for staying on one host is no PR preview URLs and a shared deploy, so a broken docs build blocks a marketing-site fix. Serving it requires one manual step after merge, **Settings → Pages → Source → GitHub Actions**, kept last so the live site is never in a gap [#1265](https://github.com/Appsilon/mediforce/pull/1265).
- [ADR-0019](docs/adr/0019-workspace-scoped-roles.md) (**proposed**) puts the role model up for decision before any of it is built: a process role (`reviewer`, `PI`, `approver`) would be held **inside one Workspace**, not across the Deployment. On acceptance it partially supersedes [ADR-0002](docs/adr/0002-firebase-auth-to-nextauth.md) §5, which chose a global `user_roles` table to protect namespace-blind notification targeting and left the door open for "per-workspace functional roles ... later as a real product decision if asked". The migration backfills each existing global row across that user's memberships, which would keep every notification a role holder gets for workspaces they belong to and deliberately drop the ones for workspaces they do not — a §12 regression accepted in the ADR rather than found later. Recorded against three verified facts about today's code: nothing writes `user_roles` (the invite service can, but its only caller hardcodes an empty list), nothing reads `step.allowedRoles` *for authorization* (the engine's `RbacService` call sits behind a constructor slot production passes `undefined` into), and the Human actions inbox already pivots on `session.user.roles[0]` — a namespace-free value that hides a second role's queue [#1247](https://github.com/Appsilon/mediforce/issues/1247).
- Process roles can be granted, for the first time. `user_roles` was written only by the one-time `seed-user-roles` migration and by `PostgresInviteService.seedInvite`, whose one caller hardcoded `roles: []` — so every `allowedRoles` in the 23 workflow definitions here named people who could not exist. Migration `0042` scopes the table per [ADR-0019](docs/adr/0019-workspace-scoped-roles.md): `namespace` (FK to `workspaces.handle`, cascade) plus a nullable `workflow_name` where `NULL` means every workflow in the workspace, keyed `UNIQUE NULLS NOT DISTINCT` because a primary key cannot span a nullable column. Owner/admin set a member's roles with `mediforce namespace set-member-roles <handle> <uid> --roles reviewer,approver [--workflow tealflow]` or `PUT /api/namespaces/:handle/members/:uid/roles` (full replace — the array is the end state, `[]` clears), and `listNamespaceMembers` returns each member's roles for **every** member, not only managers: a member has to be able to see who the reviewer is. `mediforce namespace list-members <handle>` prints that roster with the two columns labelled apart (MEMBERSHIP vs PROCESS ROLES), so a grant can be read back from the same surface that made it instead of only in SQL. Note the plural — `set-member-role` (singular) is still Membership (`owner|admin|member`), the distinction `CONTEXT.md` draws, now restated in both commands' help text. Two things the ADR flagged as silent when wrong are handled rather than deferred: notification targeting moved from the deployment-wide `getUsersByRole(role)` to `getUsersByRoleInNamespace(role, namespace, workflowName)`, so a grant narrowed to workflow A no longer emails its holder about runs of workflow B, and leaving or being removed from a workspace deletes that user's grants there in the same transaction as the membership row (the `namespace` FK cascades on workspace *deletion*, a different event) — without it, re-adding someone six months later silently restores a role nobody granted. Deleting a workflow drops the grants narrowed to it for the same reason, and so does transferring one to another workspace — the source name comes free either way, and the grants do not travel with the workflow because their holders need not be members of the destination. The membership check is the write's own, not the handler's: `setRolesForUser` takes the member's `workspace_members` row `FOR UPDATE` before replacing anything, so a removal committing mid-request cannot recreate the grant the cascade just deleted, and two admins editing the same member serialize instead of both deleting the set they read and inserting their own — which under READ COMMITTED leaves the union, a set neither of them asked for. The migration backfills each global row across its holder's memberships, which keeps every notification a role holder gets today for workspaces they belong to and drops the ones for workspaces they do not — the §12 regression ADR-0019 accepted deliberately — and then seeds every role a live workflow definition declares to that workspace's owner and admins, recorded as a `namespace.member_roles_seeded` audit event, so the gate #1249 adds does not land on an empty table and strand every declared step (AGENTS.md §13). `CallerIdentity` carries `namespaceProcessRoles` (workspace-wide grants only; a narrowed one needs the workflow in hand, which only the handler has) so that gate costs no second round-trip. `session.user.roles` stays a flat array and is now a cross-workspace union — the shape change belongs to the inbox that consumes it, [#1251](https://github.com/Appsilon/mediforce/issues/1251) [#1248](https://github.com/Appsilon/mediforce/issues/1248).
- Creating a workflow now states the four ways to author one, a reason to pick each, and how to start it — without a checkout. A **Ways to author** popover in the Workflow Designer toolbar (so it appears once you are actually creating something, not in the catalog header) names blocks on the canvas, the AI Assistant, import from git and the `/design-workflow` agent skill. The two paths that live outside the browser were previously undiscoverable from the product: the only in-app statement of authoring paths was a hover tooltip on **Workflow source code** listing three canvas-local ways with no reason attached, and that list is replaced by the popover it duplicated. Each entry carries its first move rather than its name alone — the skill entry gives the `git clone`, the agent to open the checkout in, and the `/design-workflow` invocation, since a skill you cannot reach is the same dead end as one you were never told about. The import entry is a click, not an instruction: it navigates to the workspace home with the importer already open, via a new `?import=source` parameter, because the dialog does not live on the editor page. Its footer opens the full guide on GitHub — a third `docs-links.ts` constant next to the Docker-setup and verification ones — instead of printing a repo-relative path only a reader with a checkout could follow. The AI Assistant's empty pane also points at `/design-workflow` for the case it cannot serve — a package with code in it, not just steps. Wording is condensed from [`docs/guides/create-workflow.md`](docs/guides/create-workflow.md) § "Pick an authoring path" into [`authoring-paths.ts`](packages/platform-ui/src/lib/authoring-paths.ts), and the guide records the pairing [#1185](https://github.com/Appsilon/mediforce/issues/1185).
- Every `packages/*/` and `apps/*/` directory now carries a `README.md` — 21 were missing, only `platform-ui` and four apps had one. [`docs/README.md`](docs/README.md) records the convention that produced them: a package or app documents itself, `docs/` covers only what spans more than one package, and a new package ships its README in the same PR. This closes the gap [ADR-0017](docs/adr/0017-retire-llm-maintained-wiki.md) accepted when the wiki was retired — "what is `packages/agent-runtime` for?" had been answerable only from the one-line `<stack>` block in `AGENTS.md`. Thirteen archived wiki entity pages seeded the new files and were verified against the code first, which is where most of the work went: `platform-infra`'s page described Firestore repositories, Firebase auth and a `firebase-admin` dependency for a package that now has none of them (drizzle + `postgres`), `workflow-engine` still listed `TriggerHandler` and `src/graph/` (now `WebhookRouter`, and graph validation moved to `platform-core`), the plugin pages named `platform-ui` as the registration site after the composition root moved to `platform-api/src/services/platform-services.ts`, `MockClaudeCodeAgentPlugin` had been renamed `MockAgentPlugin`, and `cowork-workflow-designer.wd.json` no longer exists. The four surviving plugin pages collapsed into one [`packages/agent-runtime/src/plugins/README.md`](packages/agent-runtime/src/plugins/README.md), since they describe single files in a shared directory rather than packages. Ported pages are deleted from `docs/archive/knowledge-base/`, so no second copy can disagree; the five `supply-intelligence` pages stay, describing deleted code, as the record of the drift ADR-0017 diagnosed. READMEs carry no frontmatter, matching the five that already existed — the `status`/`audience`/`last_reviewed` header stays scoped to `docs/`, and a colocated README's freshness comes from `git rm` carrying it off with the code instead ([PLAN-0017](docs/archive/PLAN-0017.md)). The convention is enforced rather than trusted: new `pnpm check:readmes` ([`scripts/check_readme_coverage.py`](scripts/check_readme_coverage.py)) fails the build when a package or app has none, and it runs in [`ci.yml`](.github/workflows/ci.yml) rather than the docs workflow because a new package containing no Markdown file matches no `**.md` path filter — the exact case it exists to catch. `AGENTS.md` gains rule 11, "sync the docs the change invalidates": the diff that renames a symbol fixes the README naming it, in that diff, never as a follow-up issue — colocation decides *which* README (touched `packages/foo/src/` → `packages/foo/README.md`), which is the routing the wiki never had. The pre-existing duplicate `10.` in the rules list is renumbered.
- The four checks that answer "will this workflow work?" — schema validation (runs on every save) → **Workflow readiness check** (image, secrets, model, credits) → **Dry Run** (agent and script work mocked) → **Run** (the only one that answers behaviour) — are documented in one place, the new guide [`docs/guides/verify-a-workflow.md`](docs/guides/verify-a-workflow.md), linked from `workflow validate` on success, which now says which gate it is and what to do next. The "Before you start" dialog reports "some checks could not run" rather than a clean bill of health when a probe it needed never completed — an unreachable image registry, a failed credits probe or a model-registry lookup that errored produce no warnings, exactly like a pass. CLI help stops contradicting the pinned terminology: `run start --dry-run` described itself as "real execution, labelled for testing" when it in fact mocks every agent and script step, and `workflow register --dry-run` now says outright that it is schema validation and executes nothing. The guide and the CLI help both state the trap the old copy left implicit: a Dry Run is **not** a sandbox — only `agent` and `script` steps are mocked, so an `email` action still sends the mail and an `http` action still hits the real endpoint. The guide also stops claiming the readiness check gates every start; it runs in the app, and `mediforce run start` and `POST /api/runs` skip it [#1188](https://github.com/Appsilon/mediforce/issues/1188).
- The Agents, Tools and Tool catalog surfaces now say what the concept is before asking anyone to configure one, so the definitions in [`CONTEXT.md`](CONTEXT.md) finally reach the user rather than living only in the repo. The Agents, Tools and Tool catalog pages carry it in a `ConceptPopover` beside the page title — a labelled info trigger rather than a hover tooltip, so the text is reachable by click, keyboard and touch, matching the popover [#1185](https://github.com/Appsilon/mediforce/issues/1185) introduced for the same reason; the agent create and configure forms, which have no title to anchor a trigger to, keep it inline as a `ConceptIntro` box standing in for a page header. An **Agent** is a reusable configuration a workflow step calls by id — system prompt plus MCP server bindings — unversioned, so editing one changes every step already referencing it; a **tool** is an MCP server, split into stdio entries an admin curates in the catalog and agents reference by id, and HTTP endpoints any member binds to an agent, with a step able to narrow that set but never widen it; a **catalog entry** is an admin-approved stdio server whose command lives in the catalog rather than in the agent, which is what keeps arbitrary commands out of agent configuration. The copy is deliberately silent about behaviour the runtime does not have, and says so where the form implies otherwise. At the time this shipped the agent's `foundationModel` was dead data — no runtime path read it — so the **Foundation model** picker on both agent forms said as much; the entry below under **Changed** closes that gap and supersedes the wording. The absence of any `skills` field on the Agent schema is recorded in `CONTEXT.md` § **Agent** — the issue assumed it already was — and two stale comments in `agent-runtime` claiming skills arrive from the AgentDefinition are corrected. Adding the callout surfaced a duplicate the page already had: an empty Tool catalog rendered "No catalog entries yet." twice side by side — once from the entry list and once from the editor pane — so the editor pane now says what it actually means ("Nothing to edit yet.") and the list drops the "enable stdio MCP servers for agents" gloss the callout above it now carries. The Tools page's **Add HTTP binding** button stops promising an action it cannot perform: an HTTP binding is a key in the agent's `mcpServers` map, with no namespace-level store to create one in, so the button was always a hop to the agents list — it is now labelled **Bind to an agent** and styled like the other navigation links rather than as the page's primary action. Creating an agent now lands on that agent's Configure page rather than back on the catalog: MCP servers can only be bound once the agent is persisted (the binding form's `agentId` is needed for the OAuth connect flow, whose tokens are stored per-agent per-server), so creation continues into the page that owns them instead of dead-ending on a grid of cards. Binding at create time is possible — `CreateAgentInputSchema` already accepts `mcpServers` — and is tracked as [#1262](https://github.com/Appsilon/mediforce/issues/1262) [#1187](https://github.com/Appsilon/mediforce/issues/1187).

### Changed
- `Case Studies` in the header is a link to the index rather than a button that only opens a menu, so the stand-in `All case studies` item is gone. Hover and `:focus-within` still open the children, and the click handler no longer binds when the trigger is a link. The case studies index was also the one page still listing both children flat in its own header - it was written by hand after the loop that rewrote the other nine.
- Cowork workflow steps now choose Chat, real-time Voice, and voice-synthesis models from the synced model registry instead of requiring authors to type provider/model IDs [#1310](https://github.com/Appsilon/mediforce/pull/1310).
- Choosing an agent for a workflow step is now a choice, and it carries the agent's model. The step editor's **Agent ID** was a free-text box, so authors copied slugs off the Agents page by hand; it is a dropdown of the saved agents visible in the workspace, which needed an optional `namespace` filter threaded through `mediforce.agents.list`, the contract, the route adapter and the authorized repository — where it is a filter over what the caller can already see, never a grant: `AuthorizedAgentDefinitionRepository.list` intersects the requested workspace with the caller's own memberships, so `GET /api/agents?namespace=someone-else` returns the public agents alone rather than that workspace's private roster. That surfaced the real inconsistency underneath: `AgentDefinition.foundationModel` was written, stored and displayed but no runtime path ever read it — a run's model came from `step.agent.model` alone, so picking an agent visibly did nothing to the model and the agent's own model field was decoration. It now supplies the run's model whenever the step leaves `agent.model` unset; the step still wins when it sets one, which is what the tooltip and the Agents page always implied. `resolveAgentIdentity` became `resolveAgentDefaults` and returns the identity prompt and the model together from the fetch it already made, and the model picker's blank option now names the model that blank actually resolves to (the selected agent's, falling back to the plugin's) rather than always the plugin's. Note for existing workflows: an agent step that left `agent.model` blank previously emitted no model at all on the `claude-code-agent` path (the CLI/gateway default applied — the plugin's `foundationModel: 'Claude Sonnet 4.6'` is display metadata, never passed to `claude --model`) and `deepseek/deepseek-chat` on `opencode-agent`; such a step now runs on the named agent's `foundationModel`. Steps that set a model are unaffected, as are steps with no `agentId`. The `claude-code-agent` plugin also stops handing the Anthropic API a spelling it does not know: models arrive as OpenRouter slugs (`anthropic/claude-sonnet-4`), which are correct through the OpenRouter gateway and wrong against a direct `ANTHROPIC_API_KEY`, where a model is a bare id — so the provider prefix is stripped when no `ANTHROPIC_BASE_URL` is set, and a non-Anthropic model fails with an actionable error instead of quietly running a different model than the step and the audit trail name. The run route's retired-model gate now runs against the definition as it will actually execute, so an inherited model cannot slip past a check that stops the same model named on the step; the unknown-model gate deliberately still reads the step only, because agent `foundationModel` values have never been registry-validated and resolving them there would start refusing workflows that run fine today.
- The site serves its own fonts. Every page pulled a Google Fonts stylesheet, and the four URLs had drifted apart - one page requested Manrope and JetBrains Mono, another neither, and four asked for a Manrope they never rendered. All four are gone, replaced by `@font-face` in [`theme.css`](docs/theme.css) over files in [`docs/fonts/`](docs/fonts). Google serves each of these families as a single variable font, so the per-weight URLs were the same bytes: twelve downloads became four files, 411 KB became 124 KB. The per-page divergence stops mattering because `@font-face` only fetches a face a page actually renders.
- `robots.txt` states a crawl policy: search allowed, AI training refused as an Article 4 reservation, and the live citation bots — `OAI-SearchBot`, `ChatGPT-User`, `Claude-User`, `Claude-SearchBot`, `PerplexityBot` — named explicitly rather than left to the wildcard. `GPTBot` and `ClaudeBot` may fetch so they actually see the no-train signal; `CCBot` and `Bytespider` stay blocked. This replaces Cloudflare's managed file, which was a default nobody chose and which blocked the crawlers that drive citation alongside the ones that train.
- Every page title ends `| Mediforce`, and none exceeds 60 characters. The two case studies carried no brand term at all - the worst two pages to omit it on, since "Mediforce" collides with at least seven unrelated companies and a case study is what gets shared. `Security` and `Validated AI` gained the clinical qualifier they lacked, the homepage came down from 75 characters, and the two case-study descriptions came down from 196 and 238 to 154 and 148, inside the ~155 Google renders. `generate-og-cards.ts` asserts the `og:title` and `og:description` against the page, so all six values moved together or the build would have failed.

- [`GETTING-STARTED.md`](GETTING-STARTED.md) re-checked against the code and cut from 487 to 172 lines, scoped back to the one job the routing table gives it — the first run. Five claims were false. `pnpm dev:mock` was headlined "Zero cloud, zero Docker, zero Postgres" while [`dev-mock.py`](packages/platform-ui/scripts/dev-mock.py) runs `scripts/dev-infra.py` and `pnpm db:migrate` — the same Docker Postgres `pnpm dev` uses, as that script's own comment spells out — and the same doc contradicted itself 350 lines later ("`pnpm dev:mock` starts that container too"); the autonomy table described L0 as "full human control", L3 as "acts, reviews / periodic human review" and L4 as "exception handling only", where [`agent-runner.ts`](packages/agent-runtime/src/runner/agent-runner.ts) defines L0 silent observer, L1 shadow, L2 annotator, L3 advisor-requires-approval and L4 autopilot — the doc's L3 was materially more autonomous than the runtime's; `pnpm dev` was credited to `docker-compose.dev.yml` (`dev-infra.py` does it — the same wrong credit already fixed in `dev-quickref`); `dev:queue` was described as "`pnpm dev` + Redis + BullMQ queue worker" when it starts no consumer; `ENABLE_PASSWORD_AUTH=true` was presented as required in three places although `isPasswordAuthEnabled` returns true unless the value is exactly `'false'`. Node was 20+ against CI's 24, and the executor list omitted `action`. Two first-run blockers were missing: `pnpm dev` needs `packages/platform-ui/.env.local` with an `AUTH_SECRET` (nothing said to copy `.env.example`), and it seeds no users — yet the doc printed the demo credentials under `pnpm dev` as though they would work, when only `dev:mock` and `pnpm seed` create that user. Duplication removed rather than reconciled: the dev-command table, the port override, the troubleshooting section (8 of its 10 rows) and a third copy of the command list all belong to [`dev-quickref`](docs/start/dev-quickref.md), which `AGENTS.md` already routes to for daily commands, ports, env vars and troubleshooting; only "sign-in fails", the one first-run symptom the quickref does not carry, stayed. The ~180 lines of inline workflow JSON teaching schema went the same way — [`docs/workflow-examples/`](docs/workflow-examples/README.md) has 13 curated examples and [`create-workflow.md`](docs/guides/create-workflow.md) owns authoring, while the doc's own agent example was admittedly fabricated (`apps/document-review` does not exist and its note said so). Sections 4 and 5 taught `curl` against `/api/workflow-definitions` and `/api/processes` for two operations the CLI covers, in a repo whose rule 4 is CLI > REST; they are now `mediforce workflow register` / `run start` against a real example file, verified with `--dry-run`. `README.md`'s quick start carried the same false Docker-free claim about `dev:mock` and is corrected. The two anchors `dev-quickref` deep-links (`#3-run-the-cli`, `#build-images-for-script-executor-steps`) are preserved.
- Root [`README.md`](README.md) is a landing page again, 352 → 104 lines. It had grown a full second copy of the dev setup — dev-mode table, Postgres/queue/seed commands, test commands, CLI env exports, Docker image builds — that [`GETTING-STARTED.md`](GETTING-STARTED.md) and [`docs/start/dev-quickref.md`](docs/start/dev-quickref.md) already own, so the two drifted (`pnpm dev:no-docker` was described as "Docker-free, UI-only" with no mention that it still needs Postgres on :5432, and `pnpm test:e2e:ui` was described as Playwright's interactive UI mode when it is the `--project=authenticated` L4 run). The staging/production Postgres ops and the per-deployment env-var tables were not duplicated anywhere, so they moved into [`docs/start/development.md`](docs/start/development.md) under Deployment, which already owns the Hetzner hosts and `bootstrap-server.py`. `GETTING-STARTED.md` is now linked once instead of four times, and the two link lists at the bottom collapsed into one table pointing at `docs/README.md`.
- The four `docs/start/` docs re-checked against the code and cut from 633 to 522 lines. Four claims were false: `dev-quickref` pointed at an `E2E_WORKSPACES` array that does not exist (the E2E cleanup list is `fixtureHandles` in [`e2e/helpers/postgres-seed.ts`](packages/platform-ui/e2e/helpers/postgres-seed.ts)), listed `dev-mock.py` as baking in "fake Firebase" keys months after Firebase left the stack, put `e2e_build_gate.py` in `scripts/` when it lives under `packages/platform-ui/scripts/`, and credited `docker-compose.dev.yml` with starting Postgres when [`scripts/dev-infra.py`](scripts/dev-infra.py) does it. Two gaps closed: `pnpm dev:queue` brings up Postgres and Redis but **not** the consumer, so jobs enqueued and nothing ran them until you also `docker compose up -d container-worker bull-board`; and `dev:mock` auto-seeds the demo fixture (`MEDIFORCE_DEV_MOCK_SEED=false` opts out), which no doc said — [`running-workspace-locally.md`](docs/start/running-workspace-locally.md) still opened with a `pnpm dev` / Ctrl-C / `pnpm seed` dance that `dev:mock` has done itself for a while. [`development.md`](docs/start/development.md) shed the duplication it had accumulated — prerequisites and setup (GETTING-STARTED owns them), the monorepo tree ([`concepts/architecture.md`](docs/concepts/architecture.md) owns it), the Postgres recipes ([`postgres-local-dev.md`](docs/start/postgres-local-dev.md)), the test-command list and gate timings (`dev-quickref` and `README.md`) — and a "Running the app" snippet that told you to `cd packages/platform-ui && pnpm dev`, skipping the database. Node version was 20+ in two docs and 24 in a third while CI runs 24; the duplicate prerequisite blocks are gone rather than reconciled. `GETTING-STARTED.md` claimed `test:e2e` "will silently run stale code" without a manual `build:e2e`, which the source-hash build gate made untrue.
- Agents now start from the nearest package/app README, load the root glossary only for domain language, and reject orphaned docs or invalid trust metadata in `pnpm check:docs`; the always-loaded `AGENTS.md` and on-demand `CONTEXT.md` are 25% and 41% shorter [#1220](https://github.com/Appsilon/mediforce/issues/1220).
- The `inputForNextRun` example moved from its own one-file directory into the tutorial set: `docs/reference/examples/previous-run-example.wd.json` → [`docs/workflow-examples/12-previous-run.wd.json`](docs/workflow-examples/12-previous-run.wd.json), renamed `tutorial-previous-run` and aligned with the folder's conventions (numbered filename, declared `roles`, the explanatory `_comment` folded into `description` and the README index row). It is now picked up by `loadWorkflowExamples`, listed in `workflows-index.json`, and reachable from the git-import browse of `docs/workflow-examples`, which the previous location was not. [`docs/reference/previous-run.md`](docs/reference/previous-run.md) stays where it is and gains a link to the example — it documents the resolver, the semantic-empty vs infrastructure-failure split and the schema rules, none of which fit a one-line tutorial index. This also fixes [`scripts/demo_previous_run.py`](scripts/demo_previous_run.py), which had pointed at `docs/examples/previous-run-example.wd.json` — a directory that has not existed since the `docs/` restructure moved the file, so the demo could not load its own workflow. The path lives in a Python string, so `pnpm check:docs` never saw it.
- `/sync-docs` rewritten around one question — *what did this change make untrue?* — and pinned as the mechanism behind `AGENTS.md` rule 11. Running it with no arguments now checks **this session's** changes (`git status --porcelain` alongside `git diff HEAD`, because a newly added package is untracked and the diff alone misses it entirely — the exact "new package has no README" case); commit, PR and audit modes stay as explicit flags. Its monitored set had a hole this size: it was defined as `docs/README.md`'s routing table plus the two root files, so the 21 colocated package and app READMEs — the most code-adjacent docs in the repo — were invisible to the tool whose job is keeping docs in sync with code. Colocated docs now need no list at all: the path is the routing (`packages/foo/src/` changed → `packages/foo/README.md` is the file that now lies), with the routing table used only for cross-cutting docs. A `change → doc` table replaces guesswork (env var → the package README *and* `docker-compose.prod.yml`; step executor → `workflow-capabilities.md`; an architectural decision → a **new** ADR, never a retro-edit), and a rename — the highest-yield case and the one that killed the wiki — is now a `grep -rln 'OldSymbolName'` across docs and READMEs. Guidance on *what to write* is new and mostly says what not to: do not restate `src/index.ts`, a new export earns a line only if it changes the package's purpose, prefer the rule over the inventory. The auto-fix/propose split is decided by whether a claim is mechanically verifiable rather than by which folder holds the file, which removes the second hand-maintained list. **The `## Exclusions` section is deleted**: every entry was already implied by `status: living` filtering or by not being in the monitored set, and it had drifted into contradicting the file it sat in — excluding `docs/testing/e2e-strategy.md` while the tier list claimed "everything under `docs/testing/`". The two rules worth keeping (CHANGELOG is a signal source, an ADR is a record) are now one sentence in the procedure. Dogfooding the rewrite on its own diff immediately caught a stale `AGENTS.md` self-reference ("Complements §10") left by the rule renumbering, and a missing **Repo checks** section in [`docs/start/dev-quickref.md`](docs/start/dev-quickref.md), which had never listed `pnpm check:docs` at all.
- `docs/` is navigable by intent and routable by agents. New [`docs/README.md`](docs/README.md) is the entry point the folder never had (`index.html` is the marketing homepage, so GitHub had no README to render): a human router plus a `topic → path → audience → status` table covering every engineering doc. Files sorted into verb-shaped folders — `start/` `concepts/` `guides/` `reference/` `testing/` `contributing/` `archive/`, with `PLAN-*`/`RUNBOOK-*` moved under `adr/plans/` and `adr/runbooks/` — and the four SCREAMING-cased names (`CONTAINER_STEPS`, `PREVIOUS_RUN`, `E2E-STRATEGY`, `ENGINE-TESTING`) are lowercase-kebab like everything else. The two completed headless-migration docs (2,427 lines, `Completed` since 2026-05-31, and the most-referenced docs in the repo) moved to `docs/archive/`, so finished history stops outranking current guidance. Every doc now declares `status: living | draft | historical`, `audience` and `last_reviewed` in frontmatter, so a stale doc announces itself instead of quietly misinforming; ADRs carry their own lifecycle (`proposed`/`accepted`/`finalized`/`superseded`) in the same field, which also collapses the three different status-header formats ADRs used into one. Agent autonomy had three live vocabularies — `architecture.md`'s Observer/Advisor/Drafter/Executor (which exists nowhere in code), the UI's CM0–CM4, and the schema's L0–L4; Control Mode is now the single name, with `executor` + `autonomyLevel` named as storage detail behind it. `sync-docs`' hand-maintained monitored-file list is replaced by the routing table, so the two can no longer disagree. Anything describing finished work moved to `docs/archive/`, which now holds the two migration docs plus the four executed ADR implementation plans and the staging-cutover runbook — every `PLAN-*` and `RUNBOOK-*` written so far has shipped (`PLAN-0003`'s `BlobStore` and `PLAN-0016`'s in-canvas assistant both verified present in the code), so `adr/plans/` and `adr/runbooks/` are gone; a plan sits beside its ADR only while the work is in flight. `last_reviewed` is seeded from each doc's last-touched commit rather than stamped with today's date, so it means what it says — a doc nobody has re-checked against the code reads as 2026-04, not as freshly reviewed. Three docs that gave instructions for a stack we no longer run were rewritten against the code rather than archived: [`docs/testing/engine-testing.md`](docs/testing/engine-testing.md) described the Firestore emulator as the integration backend and named eight paths that no longer exist, and listed as "gaps" four things now covered (the full mixed-executor loop, output propagation into `instance.variables`, crash-then-retry, and the auto-runner loop itself); [`docs/reference/previous-run.md`](docs/reference/previous-run.md) told operators that deploying the feature "requires creating that index" in `firestore.indexes.json`, a file that does not exist — the index ships with the schema migration; and [`docs/start/development.md`](docs/start/development.md)'s auth setup led with a one-time Firebase-Auth seed that ran during the staging cutover, ahead of the greenfield path every new install actually takes. `/new-test` had the same drift in its level table ("Proves Firestore + middleware + auth"). Broken references fixed repo-wide: `skills/generate-pitch` was dead (all four inputs were files that never existed in git — it now reads the docs that do exist and ships its own slide structure and Marp theme), `skills/code-review` pointed at `wiki/STYLE.md` instead of `knowledge-base/STYLE.md`, `agents/design.md` and `agents/discuss-vision.md` listed five vision docs that do not exist, `CHANGELOG` linked a never-existing `ADR-0011-*` filename, and `apps/workflow-designer/NEXT-SESSION.md` was a to-do list for `apps/process-designer`, deleted long ago. New `pnpm check:docs` ([`scripts/check_doc_links.py`](scripts/check_doc_links.py)) fails on any unresolvable link or backticked `docs/`/`skills/` path, and runs in both [`ci.yml`](.github/workflows/ci.yml) and its own [`docs.yml`](.github/workflows/docs.yml) workflow, because a link breaks from either side: `ci.yml` skips docs-only changes, so the PRs most likely to break a doc link had nothing checking them, while a code-only PR that renames or deletes a linked source file matches no `**.md` path filter in `docs.yml` [#1220](https://github.com/Appsilon/mediforce/issues/1220).
- The app, favicon and landing-page header now use the BioVerse axon mark instead of the bot logo, across `logo.png`, `favicon.ico`, `apple-touch-icon.png` and the two PWA icons; the landing site gains a favicon it never declared [#1227](https://github.com/Appsilon/mediforce/issues/1227).
- ADR numbers are unique again. Three numbers were each used by two ADRs (`0006`, `0007`, `0012`), so every `ADR-000N` cross-reference from `CONTEXT.md`, other ADRs and code comments was ambiguous. The file each number's existing references point at keeps it; the other moves to the next free number — control mode is a UI concept → [ADR-0014](docs/adr/0014-control-mode-ui-concept.md), Output Files on the run branch → [ADR-0015](docs/adr/0015-output-files-on-run-branch.md), canvas-first workflow designer → [ADR-0016](docs/adr/0016-unified-canvas-first-workflow-designer.md) (with `PLAN-0012` → `PLAN-0016`). [`docs/adr/README.md`](docs/adr/README.md) now indexes all 16 ADRs with their status; it previously listed five [#1191](https://github.com/Appsilon/mediforce/issues/1191).
- Five stale paths a first-time reader hits before any code are gone. Deleted: root `PLAN.md` (a "Status: IMPLEMENTED" plan for the shipped copy-workflow-to-another-namespace feature — its two unshipped invariants, the publish guard for public workflows referencing private agents and the lock on agents used by public workflows, are now [#1213](https://github.com/Appsilon/mediforce/issues/1213)), `packages/platform-ui/tmp/CDISCPILOT01-mock-tlg-shells.md` (committed `mock-tlg-generator` scratch output — `packages/*/tmp/` is now git-ignored), `minutes/` (two March 2026 meeting notes; the now-dead `minutes/*.html` ignore rule goes with them), and the empty `apps/workflow-designer-2/`. Moved: `docs/adr/draft/cowork-streaming.md` → [`docs/research/cowork-streaming.md`](docs/research/cowork-streaming.md), retitled as the research note it says it is — it records a deferred trade-space, not a decision, so it does not belong in the ADR numbering; the two docs linking it follow [#1193](https://github.com/Appsilon/mediforce/issues/1193).
- `docs/design/` audited against the code it describes. **`MCP-SUPPORT.md` and `AUTONOMY-LEVELS-REFACTOR.md` deleted** — the first documented `step.agent.mcpServers` as *the* way to grant an agent tools, which is now a backward-compat field (servers live on an Agent Definition and steps only *narrow* them via `mcpRestrictions`), and listed HTTP transport, OAuth and a real Tool Catalog as unbuilt "v2" work; [docs/reference/workflow-capabilities.md](docs/reference/workflow-capabilities.md) already covers the current model and is drift-guarded by a lint test. The second duplicated ADR-0014, whose durable content (Assist's definition, L2 backward-compat, L0/L1 displaying as CM0) is now folded in; the four docs linking to them repoint to [ADR-0014](docs/adr/0014-control-mode-ui-concept.md). `action-spawn.md` and `action-wait.md` stay — they hold rationale not recoverable from code — now marked Implemented, with file maps pointing at the handlers that exist (`platform-api/handlers/processes/resume-wait.ts`, `cron/heartbeat.ts`, `platform-core/utils/workflow-status.ts`) and the pre-ADR-0001 Firestore references dropped. **Both docs advertised a spawn+wait "wait until all children complete" pattern that does not exist**: no mechanism writes child completion into the parent, and neither the heartbeat nor `resumeWait` queries child status, so the documented `allCompleted` condition never fires and the run silently waits out its deadline instead. Both now state the limitation and document the shipped pattern (deadline-only wait, then a script step collects by `spawned[].instanceId`, as `apps/team-pulse` does); closing the gap is tracked in [#1215](https://github.com/Appsilon/mediforce/issues/1215).
  - **`docs/design/` retired entirely.** The audit above kept `action-spawn.md` and `action-wait.md`; a re-read found the format itself was the problem. Both were pre-implementation PR plans (*Problem / Schema / Handler Behavior / Changes by File*) wearing an `Implemented` stamp, and inlining handler source guaranteed re-rot: two days after the audit `action-wait.md` already printed a `waitActionHandler` with no `mode` on the sentinel, no `interpolate()` on `deadline`/`condition`, and edge case #8 proposing a shape check that had shipped as `isWaitSentinel()`. Nothing consumed the directory — `gen_assistant_docs.py` embeds three docs and none of them were these, no drift test guarded it, and its only inbound link was one design doc pointing at the other — while three places disagreed on what it was for (`docs/README.md` "implemented design notes", `agents/design.md` "in-flight, not yet decided"). The ~85 lines that code cannot state are salvaged: the fan-out decision and its three rejected alternatives became [ADR-0018](docs/adr/0018-fan-out-is-child-workflows.md), and the `__wait` sentinel rationale, resume path and wait edge cases moved into [`packages/core-actions/README.md`](packages/core-actions/README.md) next to the handlers. `agents/design.md` and `agents/discuss-vision.md` now point undecided shapes at `docs/research/`.
- **Add Block** opens on a **Simple** tier of pre-made blocks (Send email, Call an API, Route by condition, Wait, …) that insert a step already carrying its type, its action kind and its script runtime, beside a **Full** tier folding into two cards, **No agent** and **Agent**; Send email stays visible but greyed with the reason on hover where the instance has no email transport, driven by a new `GET /api/capabilities` that derives availability server-side so no env var names reach the browser. The workflow assistant shares the catalog: `add_step` takes a `presetId` that resolves the same payload server-side, so asking for an email step yields exactly what the panel would have inserted rather than an equivalent it composed — and a preset whose required value is missing (a recipient, a URL) is rejected rather than sent empty.

- [`docs/reference/workflow-authoring-golden-rules.md`](docs/reference/workflow-authoring-golden-rules.md) re-checked against the code and cut from 338 lines to 261. Two claims were wrong. It said each `workflows-index.json` `path` is relative to the **repo root**, contradicting the same-PR fix to [`import-from-git.md`](docs/guides/import-from-git.md) — a manifest reached through a `/tree/<ref>/<dir>` URL resolves paths under that directory, so the rule is **source root**, and the manifest JSON is no longer re-printed here at all but linked to the section that owns the format. And `continueOnError` was described as "non-critical side effects that may fail while the run continues", which reads as a general fail-soft flag; the only runtime branch that honours it is the action-executor catch, so `agent`/`script`/`human`/`cowork` steps silently ignore it — the doc now says `action` steps only. Removed as duplication of a source of truth: the three JSON blocks in §7 were copies of [`setup/tool-catalog-entry.json`](apps/golden-standard-workflow/setup/tool-catalog-entry.json), [`setup/agent-definition.json`](apps/golden-standard-workflow/setup/agent-definition.json) and the `agent-review` step of the reference workflow, and are now links to those files — the doc's own preamble already sends the reader to that package, and `setup/` was the one directory the layout block failed to list; a 20-line Dockerfile skeleton in which every line after `FROM mediforce-golden-image` was commented `# optional`, one link away from the real Dockerfile; and a prose restatement of the `executor` and `type` enum values sitting directly above the two tables that [`workflow-authoring-docs.test.ts`](packages/platform-core/src/schemas/__tests__/workflow-authoring-docs.test.ts) pins to the schema — a third copy that no test guarded. The CM1 bullet said "backward compatibility only", which is true of stored `L2` steps but not of Assist itself: [ADR-0014](docs/adr/0014-control-mode-ui-concept.md) shows the mode in the picker but disabled, pending platform support that does not exist. Three `how-to-create-workflow.md` link labels left over from the `docs/` restructure are fixed, and the embedded assistant docs regenerated — every section trimmed here (packaging, pinning, Dockerfiles, skills repos, Tool Catalog setup) is explicitly out of scope for the in-canvas assistant that carries this file verbatim in its system prompt.
- [`docs/guides/import-from-git.md`](docs/guides/import-from-git.md) re-checked against the import path and cut by roughly a third. It claimed "one `.wd.json` per import", which is only true of the CLI — the UI's Browse mode imports every workflow you select as its own import, and a mid-batch failure leaves the earlier ones imported; it also never mentioned the second UI entry point, *Import example workflows*, offered to a workspace with no workflows yet, which skips the repo question and browses this repo's own manifest. The manifest section now says each `path` is relative to the **source root**, not always the repo root — a manifest reached through a `/tree/<ref>/<dir>` URL resolves paths under that directory. Removed as restatement: five "Supported" bullets enumerating step types, image modes and preserved fields, all of which follow from the one line that matters (import runs the same handler as `workflow register`), plus a deprecated-fields note that told the reader nothing actionable. Four `how-to/…` link labels left over from the `docs/` restructure — the folder is `guides/` — are fixed in `create-workflow.md` and `workflow-authoring-golden-rules.md`, with the embedded assistant docs regenerated.
- [`docs/contributing/ai-development-process.md`](docs/contributing/ai-development-process.md) re-checked against the repo it describes, which it had drifted from in four places: it billed `AGENTS.md` as "~100 lines" when the file is 237, listed 11 of the 13 skills (`/sync-docs` and `/design-workflow` were both missing, the two added most recently), described `/code-review` as "an 8-section checklist" after it became a three-axis parallel review — Standards, Spec, Big Picture — over a 9-section checklist, and restated the gate timings (`~5s` / `~9s` / `~4min`) that `README.md`, [`docs/start/development.md`](docs/start/development.md) and [`docs/start/dev-quickref.md`](docs/start/dev-quickref.md) already carry, giving four copies to keep in step; the doc now links the two that own the subject instead. The instruction-hierarchy diagram — the whole point of a doc about structuring a repo for agents — omitted `CONTEXT.md`, `skills/_registry.yml`, and `agents/`, so a reader following it would not know custom subagents exist; it also now records that `.claude/agents/` holds **copies** rather than the symlinks `.claude/skills/` uses, which is a live drift hazard (the two pairs are byte-identical today). The line-budget claim is dropped rather than corrected to 237: the rule that actually holds is "rules only, no how-to", and a line count in prose is a number nobody will re-check. Its testing section said "TDD for UI features" while the repo rule is that **product** features land at L3. `skills/_registry.yml` declared `total_skills: 14` against 13 entries and 13 directories.
- [`docs/reference/container-steps.md`](docs/reference/container-steps.md) rewritten against the code and cut from 366 lines to 123. It was a pre-implementation design doc (*Problem / Design / Changes Required / Implementation Order / Open Questions*) carrying `status: living`, and the model that shipped inverts its central claim: it said "steps do NOT chain via git — each step has its own `repo` + `commit`", whereas a run gets **one** worktree on `run/<runId>` shared by every step, cut from one host-cached bare repo per workflow definition ([`workspace-manager.ts`](packages/agent-runtime/src/workspace/workspace-manager.ts)). `agentConfig.repo` + `commit` is now the Docker **build context**, not the agent's working repo — the single most expensive confusion the old text could cause, and it is the one thing a reader arrives with. The `entrypoint.sh` the doc printed in full (clone → run `claude` → `git add`/`commit`/`push`) does not exist and never shipped: git runs host-side, and run branches are **never pushed**, which invalidated the whole reviewer story the doc was built around — "reviewer sees the diff on GitHub", the merge-to-`main` step, the GitHub deep links in the review panel. `GitMetadata.repoUrl` deliberately carries the bare repo path so the UI cannot render `/commit/<sha>` links that 404, and `AgentOutputReviewPanel` has a test asserting it renders no git view at all. `/output` — the actual engine↔step contract (`input.json`, `prompt.txt`, `previous_run.json`, `result.json`) — was absent from a document about container mounts. Smaller drift: `MockClaudeCodeAgentPlugin` is `MockAgentPlugin` and returns no git metadata, the memory cap is 8g not 4g, network isolation is listed as shipped but is not wired, and the GitHub-App/deploy-key/PAT trade-off table described a choice already made (anonymous HTTPS, then `$DEPLOY_KEY_PATH`, with `repoAuth`/`remoteAuth` naming a workflow secret for HTTPS tokens). What replaces it is reference material for what runs today — the storage layout under `$MEDIFORCE_DATA_DIR`, the three mounts, the always-commit rule with its `◆`/`✓`/`✗` markers and trailers, the staged-diff secret scan, local vs `REDIS_URL`-queued spawn, and the `ALLOW_LOCAL_AGENTS` escape hatch — with rationale left in the source comments that own it rather than copied. The schema comment on `WorkflowWorkspaceSchema` had the same push claim ("pushes are controlled by `push` and default to `never`", for a field that does not exist) and is fixed in the same diff.
- [`docs/reference/workflow-capabilities.md`](docs/reference/workflow-capabilities.md) re-checked against the code it maps. Three claims were wrong, all in the `wait` action. The kind table said `wait` pauses "until a `duration`, a `deadline`, **or** a `condition`" — a condition-only wait has no timer, so `new Date(undefined)` throws `Invalid deadline` at runtime; `duration` or `deadline` is required and `condition` is an early exit on top of it. The doc then twice said the condition is "stored but **not polled**" and told the reader to "treat both as declarative-only until the runtime catches up" — it *is* polled: [`resume-wait.ts`](packages/platform-api/src/handlers/processes/resume-wait.ts) evaluates it on every cron-heartbeat sweep of `waiting_for_timer` runs and resumes early with `resumeReason: condition_met`. The real limitation is narrower and is what an author needs: the condition sees only **this run's** step outputs, so it cannot wait for spawned children ([ADR-0018](docs/adr/0018-fan-out-is-child-workflows.md), [#1215](https://github.com/Appsilon/mediforce/issues/1215)). Only `review.timeBoxDays` is genuinely unenforced. The trigger table also listed `event` as "in the enum but has no router" when `TriggerTypeSchema` is `z.enum(['manual','webhook','cron'])` and `event` is not in the union at all. The same `wait` error sat in the table row of [`packages/core-actions/README.md`](packages/core-actions/README.md), which owns the handler, and is fixed in this diff; two traps that README had and the map lacked move up into it — resume granularity is ~15 minutes because the heartbeat drives it, and a step id inside a bare expression must use underscores or the parser reads `output.spawn-perspectives.x` as subtraction. `databricks` was filed under "Workflow-level fields" but is a step field required by `plugin: databricks-job`, so it moves to the Scripts section. Cut from 322 to 304 lines: the "How to use this when authoring" list restated the paragraph above it, and this file is embedded verbatim in the in-canvas assistant's system prompt, so its prose is a per-request token cost. Every table row and source pointer is kept — the coverage is the point — and the embedded assistant docs are regenerated.
- [`docs/research/layer2-scores-research.md`](docs/research/layer2-scores-research.md) re-checked against the code and cut from 593 lines to 310. Nothing links to it and no script reads it, so nothing had caught the drift in the two mappings it exists to propose. The human-verdict mapping read `intent` off the task completion payload, which carries `{verdict, comment, selectedIndex?}` — `intent` lives on the task's own verdict descriptors (`task.verdicts[].intent`, defaulted by `defaultVerdictIntent()`), which is where workflow-defined verdict vocabularies land, so the proposed auto-Score would have read `undefined` and scored every verdict 0.5. Of the five deterministic checks listed as ready to fire, two have no field to check: there is no `maxTokens` on `WorkflowAgentConfigSchema` and `TokenUsage` has no `total` (it is `inputTokens`/`outputTokens`/`cachedInputTokens`/`peakInputTokens`), and no step declares an output JSON Schema for the "schema compliance" check — `StepOutputSchema` types the envelope wrapper, not the agent's `result`. The three that do work name the fields that exist (`confidenceThreshold`, not `minConfidence`). The note now says outright that a proposal needing a new field is a proposal to add the field. Removed as restatement: the per-tool prose behind the verdict table (each subsection ended in a **Recommendation** line the table already carried), ADR-0007's own layer definitions and D2–D5 rationale, and the three-persona framing that repeated the same finding once per persona. The 2026-06 model names in an example question are gone — the question is whether a cheaper model is good enough, not which one. Status stays `draft`: no `Score` schema, repository or handler exists, and ADR-0007 defers layer 2 until layer 1 has produced real traces.
### Removed

- `docs/preview/` is gone, and with it four stale copies of the marketing pages that were live at `mediforce.ai/preview/`. They were staging copies of proposed redesigns from [#165](https://github.com/Appsilon/mediforce/pull/165) in April, published so reviewers could see the new pages before they replaced the old ones; the designs shipped and the copies were never deleted. They looked maintained because mechanical find-and-replace kept touching them - [#1273](https://github.com/Appsilon/mediforce/pull/1273) gave `index.html` a 472-line revamp and the preview copy an 18-line colour-token rename in the same commit - but the copy was a version behind, missing the video hero, the Control Mode content and both case-study links, and still carried the compliance overclaim. Nothing linked to them; both READMEs listed `preview/` among the directories that "back live URLs and never move", which is why they survived five months.
- The LLM-maintained wiki is retired to [`docs/archive/knowledge-base/`](docs/archive/knowledge-base/README.md), and the `/knowledge-base` skill, its registry entry and its `.claude/skills/` symlink are gone ([ADR-0017](docs/adr/0017-retire-llm-maintained-wiki.md)). It was bootstrapped 2026-04-23 and had drifted into actively misinforming agents — five pages for deleted `supply-intelligence` packages, a gotcha routing readers through `resolveDefinitionSteps()` (zero occurrences), `platform-infra` still described as "Firestore repositories, Firebase auth", eight of fourteen apps absent. The audit found the cause is not neglect: fourteen feature PRs did touch `wiki/` after bootstrap. Drift is predicted by what a page points at — pages keyed on a repo path or symbol died, pages keyed on an external standard or a terminal symptom survived — and every dead page documented *deleted* code, because nothing in a `git rm -r packages/foo` diff points at a wiki page three directories away. The compounding the pattern promises never started either: file-back ran once in four months, touched only `wiki/log.md`, and recorded that the real updates had landed in ADR-0011 and three narrative docs; two of the five buckets `SCHEMA.md` declares (`decisions/`, `syntheses/`) were never created. Content in actual use moved rather than archived: the caveman rules `/code-review` cites as a standard are now [`docs/contributing/doc-style.md`](docs/contributing/doc-style.md), and the `@mediforce/source`, `skillsDir` and remote-E2E gotchas are folded into [`docs/start/dev-quickref.md`](docs/start/dev-quickref.md) under Troubleshooting and a new Gotchas section (`in-memory-repos-not-mocks` was dropped — [`docs/testing/engine-testing.md`](docs/testing/engine-testing.md) already covers it). `docs/archive/` is now exempt from `scripts/check_doc_links.py` by path rather than per-file frontmatter, so future archived material needs no annotation [#1220](https://github.com/Appsilon/mediforce/issues/1220).
## [2026-08-16]

### Added
- A workspace with no workflows now offers **Import example workflows** next to **New workflow**. The box opens the import dialog straight on the Mediforce examples — no repository to fill in — presented as a card grid with tag filters, and both boxes disappear as soon as the workspace holds a workflow [#517](https://github.com/Appsilon/mediforce/issues/517).

### Changed
- Workspace settings is no longer one 1,265-line client component: each section (profile, branding, icon picker, preferences, members table, invite form, secrets, administration links, danger zone) is its own component under `components/namespace/settings/`, and the members read moved into a `useNamespaceMembers` react-query hook keyed `['namespace-members', handle]` per ADR-0006, replacing a hand-rolled `useState`/`useEffect` fetch. Same page, same markup, two behaviour deltas: the members list is read once per refresh instead of twice (the page fired two identical `listMembers` calls and discarded half of each response), and a newly invited member now appears in the table without a reload — the old invite path refreshed the enrichment maps but never the rows. A dead loading skeleton (unreachable behind the page-level "Loading…") and an unused `CopyButton` are gone [#1194](https://github.com/Appsilon/mediforce/issues/1194).
- "Import from git" lists a repository's workflows as a card grid with tag filters instead of a checkbox list, sharing one presentation with the new examples box. Repository, ref, import-by-path fallback, select-all and progress are unchanged; select-all now covers the workflows the active tag leaves visible, and a selection hidden by a filter says so rather than importing unseen [#517](https://github.com/Appsilon/mediforce/issues/517).
- The four checks that answer "is my workflow working?" now have distinct names and a documented order, so asking for one no longer gets you another: schema validation (`workflow validate`) → **Workflow readiness check** (`runPreflightChecks` — missing image/secret/model/credits) → **Dry Run** (a real Run with every agent and script step mocked, proving the graph and gates but never the agent's behaviour) → a real Run. `CONTEXT.md` gains both new terms plus the collision that motivated them — `workflow register --dry-run` is schema validation and executes nothing — and pins **Deployment** as per-environment: staging, production and each customer instance are peers, so a workflow is registered into each rather than "promoted" between them. **ADR-0013** (proposed) records that production workflow packages move out of `apps/` into their own repo, registered via the CLI because git import is unauthenticated and GitHub-only, with images built from `repo` + `commit` — without that swap the extraction silently breaks every Run at its first step [#1201](https://github.com/Appsilon/mediforce/pull/1201).

- The Add step panel now explains each step type and executor instead of only naming them. The two step-type toggles say what they do — `creation` produces a result later steps read (most steps), `decision` produces none and only picks which branch the run takes next. Every executor option — Human, Script, Action, Cowork, Human review, Autonomous agent — is a full-width row stating what work belongs on it: accountability and approval for `human`, deterministic parsing/validation/conversion/API glue for `script`, built-in side effects (reshape, HTTP, email, spawn, wait) for `action`, live human-agent collaboration for `cowork`, and agent judgment with or without an approval gate for the two agent modes. The wording follows `docs/reference/workflow-authoring-golden-rules.md` §5, so choosing a step no longer requires having read that file first. The per-control-mode blurb that only restated the option it sat above is gone, together with the unused `CONTROL_MODE_DESCRIPTIONS` copy it duplicated [#1186](https://github.com/Appsilon/mediforce/issues/1186).
- `origin` carried 372 branches, 337 of them finished or abandoned. The repo now states a branch retention rule ([`docs/start/development.md`](docs/start/development.md)): `main` and `production` are permanent, a branch lives while its PR is open, merged branches are deleted on merge by GitHub's *Automatically delete head branches* setting, and anything with no open PR and no commit for 60 days is swept. Deleting a branch keeps its review history — a PR's commits stay under `refs/pull/<number>/head` [#1192](https://github.com/Appsilon/mediforce/issues/1192).

### Fixed
- Workflow editors now provide an add-step action after all editable steps are removed, so an empty workflow can be rebuilt without refreshing or recreating it [#1024](https://github.com/Appsilon/mediforce/issues/1024).
- Workshop step IDs now stay stable while a new step name is typed, then finalize to a unique slug on blur so prefix-overlapping names cannot corrupt graph transitions [#1025](https://github.com/Appsilon/mediforce/issues/1025).
- The step editor now explains how data moves between steps. Every non-terminal step's config card opens with a **Data flow** panel naming what the step receives, what becomes its output, and the exact reference later steps read it by (`${steps.<this step id>.<field>}`) — so the `/output/input.json` → `/output/result.json` contract a script must honour, and the fact that a human step's parameter values *are* its output, are visible where the step is authored instead of only in the docs. Plugin-backed steps show the plugin's own declared io contract (`script-container`, `databricks-job`, the agent plugins), falling back to authored per-executor copy when the registry is unavailable. Related tooltips that documented a `${params.*}` interpolation root were corrected — no such root exists — and the step-parameter tooltips no longer describe step params as workflow-start fields. The Parameters section on a non-human step now says the parameters are never collected there, since only the engine's human-executor branch copies them onto a task. Review feedback further corrected the guidance: the HTTP action URL tooltip now warns that a resolved URL is persisted as the step's output (so secrets don't belong in it), the HTTP response reference points at `body.json.<field>` rather than the non-existent `body.<field>`, review/decision verdicts are now described as routing via their own configured target rather than a transition condition, and human tasks with a `file-upload` / `assignment-table` / `table-editor` component or no parameters describe their actual output shape (`{files}` / `{assignments}` / `{rows}` / `{verdict}`) instead of the generic Parameters-form copy [#1029](https://github.com/Appsilon/mediforce/issues/1029).
- Retrying a failed step no longer writes a false claim into the audit trail: the `instance.retried` event recorded the pre-retry failed execution's id as `newExecutionId`, implying a pre/post linkage that never existed — the retry's real execution is created later by the runner and is unknowable at audit time. The field is removed, and `previousExecutionId` is now read before the engine resumes the run so a worker claiming it mid-retry can no longer make the event point at the retry's own execution with a null error (forward linkage tracked in [#832](https://github.com/Appsilon/mediforce/issues/832)) [#836](https://github.com/Appsilon/mediforce/pull/836).

## [2026-08-09]

### Changed
- Public workflow sharing now explains that it is link-based, shows a persistent shared state, and offers a canonical link to copy [#1141](https://github.com/Appsilon/mediforce/pull/1141).
- Import from git now defaults to the Mediforce repository URL and browses a repo-root `workflows-index.json` (the older `index.json` is still read when that is absent), keeps support for GitHub tree-scoped sources including slash-containing refs and branch-root `/tree/<ref>` URLs, and records provenance as the canonical repo plus a repo-root-relative path so `url` + `path` + `commit` locates the imported file.

### Fixed
- Navigating away from a workflow editor with unsaved changes now asks "You have unsaved changes. Are you sure you want to leave?" instead of silently discarding the work. Both the new-workflow page and the definition editor track every unsaved edit — canvas steps and transitions as well as the header name/description — and hold in-app navigation (breadcrumbs, sidebar) until the user confirms; closing or reloading the tab warns through the browser's own prompt. Browser Back is not intercepted: the App Router has no navigation-blocking API and the history-sentinel workaround leaves duplicate entries behind [#1027](https://github.com/Appsilon/mediforce/issues/1027).
- The "Workflow source code (wd.json)" editor no longer silently discards edits. Closing the modal (✕ or clicking outside) with unapplied changes now asks for confirmation before discarding them, and a warning next to "Apply JSON to canvas" makes the pending, unsaved edit visible while it's still there [#1030](https://github.com/Appsilon/mediforce/issues/1030).
- Creating an agent with only a name and a model works again. `POST /api/agents` without a namespace used to insert the agent row and then 500 appending its workspace-scoped audit entry, leaving an orphan agent that was private, namespace-less, and therefore invisible and undeletable through the API; it is now rejected with a 400 before anything is written. The new-agent form stamps the current workspace so its agent shows up in the catalog it redirects to, shows failures next to the Save button instead of silently resetting, and marks the fields that were never required (description, input, output, system prompt) as optional. The in-memory audit double now mirrors Postgres and refuses a workspace-level event with no namespace, so the next handler that forgets one fails in its unit test rather than in production [#1035](https://github.com/Appsilon/mediforce/issues/1035).
- Step parameters can no longer be saved with a blank or duplicate name. The step editor now flags an empty or duplicate parameter name inline (red border, "This field cannot be empty." / "Duplicate parameter name."), and saving blocks client-side with an error naming the offending step, instead of only surfacing as a cryptic failure after the fact [#1031](https://github.com/Appsilon/mediforce/issues/1031).
- Picking a minimal base image (e.g. `alpine:3.24`) for an agent or script step no longer fails with a raw OCI runtime dump. When Docker reports that the entrypoint binary is missing, the step error now names the image and the executable it lacks and links the Docker image setup guide, instead of burying that under four layers of runc framing. The step editor's agent image picker now sorts `mediforce-golden-image` first and marks it ★, and its blank option reads `Default — mediforce-golden-image` (or `Built from agent.repo` in build mode) rather than a neutral "Select image…", so the image registration actually falls back to is visible at edit time [#1037](https://github.com/Appsilon/mediforce/issues/1037).
- Unauthorized workspace routes now stop rendering the app shell and show a full-page explanation with a workspace picker; the shell holds a neutral loading state until the access check settles, so a workspace the user cannot open never flashes first. Only 403/404 read as access errors — a network failure or 5xx keeps the normal error handling instead of blaming permissions. Browser 4xx responses surface human-readable error toasts, except on locally handled requests such as password sign-in, where the login form reports the failure itself [#1040](https://github.com/Appsilon/mediforce/issues/1040).
- The Model Registry now shows all models by default; its optional **Top picks** filter has an explicit On/Off badge and pressed state, so users can tell when the list is narrowed [#1041](https://github.com/Appsilon/mediforce/issues/1041).
- "Delete workspace" no longer behaves like a reset on a personal workspace. `DELETE /api/namespaces/:handle` now rejects a personal workspace with 409 (`getMe` re-bootstraps one on the next request, so the delete only ever destroyed the contents), and the settings danger zone offers **Reset workspace** there instead — a new `POST /api/namespaces/:handle/reset` (`mediforce namespace reset`) that deletes every workflow with its runs and tasks while the workspace, members, secrets and audit trail survive. Both the delete and the reset now confirm twice, typing the handle and the number of workflows about to be destroyed, matching the delete-workflow dialog — a count that includes archived workflows (which reset and the workspace cascade also destroy) via a new opt-in `includeArchived` parameter on `GET /api/workflow-definitions`, defaulting to off so the catalog view is unchanged. Deleting an organization workspace also clears the two workspace-scoped tables the FK cascade never reached — `tool_catalog_entries` (no FK) and private `agents` rows keyed on the handle (`ON DELETE SET NULL` left the `namespace` mirror pointing at a re-claimable handle) [#1044](https://github.com/Appsilon/mediforce/issues/1044).
- Deleting a workflow no longer soft-deletes same-named runs in other workspaces: `getIdsByDefinitionName` / `setDeletedByDefinitionName` now match on (`namespace`, `definitionName`) instead of the name alone, which was global even though workflow names are only unique per workspace. Surfaced while adding workspace reset, which runs that cascade for every workflow in a workspace [#1044](https://github.com/Appsilon/mediforce/issues/1044).
- The delete-workflow and workspace danger dialogs no longer promise that agent runs will be removed. The per-workflow cascade soft-deletes definitions, runs and human tasks; `agent_runs` is keyed on the workspace rather than the run's deleted flag, so that history survives a workflow delete and a workspace reset. Deleting an organization workspace does remove it (`agent_runs.workspace` is `ON DELETE CASCADE`), and each dialog now says which of the two applies [#1044](https://github.com/Appsilon/mediforce/issues/1044).
- The back arrow on the Tool catalog and OAuth providers admin pages returns to where the user came from: opening either from Tools goes back to Tools, from Settings back to Settings, instead of always landing on the workspace home [#1042](https://github.com/Appsilon/mediforce/issues/1042).
- The admin Tool catalog now renders a single “New catalog entry” button when the catalog is empty [#1043](https://github.com/Appsilon/mediforce/issues/1043).
- User workflow-definition lists now return only definitions in their member workspaces, including hiding foreign public workflows [#1047](https://github.com/Appsilon/mediforce/issues/1047).
- Parallel E2E runs no longer hide Monitoring activity or workflow-status fixtures behind first-page pagination [#1134](https://github.com/Appsilon/mediforce/issues/1134).
- The "New Workflow" page now marks the Name and Description fields with a required-field asterisk while they're empty, and the disabled "Save & Dry Run" / "Save & Start Run" buttons show a tooltip naming the missing field instead of just an unexplained disabled icon; Description blocked saving with no upfront indication it was mandatory, only a disabled-button tooltip on hover [#1026](https://github.com/Appsilon/mediforce/issues/1026).

### Security
- Public workflow links are read/copy-only: non-members can no longer start runs in the owning workspace [#1141](https://github.com/Appsilon/mediforce/pull/1141).

## [2026-08-02]

### Security
- Workspace members list no longer exposes each member's email or last-sign-in timestamp to plain members: `listNamespaceMembers` now returns `email: null` and `lastSignInTime: null` for non-admin callers (owner/admin/apiKey still see them), and the settings page hides the "Email" and "Last sign in" columns for non-managers. The roster — name, avatar, role and join date — stays visible to all members. `getNamespace` now returns an `adminContact` field (the primary owner's name + email) so the run-start preflight "contact your admin" hint still works for a blocked non-admin — the one member email a plain member may see.

### Changed
- `mediforce-fullstack` now keeps interrupted issues out of selection until a human applies `fullstack:retry`, preventing stale in-progress work from being picked up automatically after two hours [#1123](https://github.com/Appsilon/mediforce/pull/1123).
- `mediforce-fullstack` agent steps now treat their configured timeout as a hard ceiling rather than a time quota. The shared preamble tells every agent to finish as soon as its output contract is satisfied and to deepen work only for a known correctness gap or material risk; each of triage, research + plan, implementation, self-review, revision, and CI-fix now has a step-specific completion gate, preventing unused time from turning into unrelated browsing, polish, or scope expansion [#1113](https://github.com/Appsilon/mediforce/pull/1113).
- `mediforce-fullstack` now researches its own open questions instead of escalating them. The gate is redefined around **who can answer**: `triage` may only emit `needs-approval` carrying at least one `decision`-kind blocker (product/policy call, unprecedented user-visible or public-API choice, irreversible change, cross-package architecture), while `missing-context` and `scope` blockers no longer justify a gate on their own — and the tie-break now says read more code rather than escalate, since `awaiting-human` has no expiry. `draft-plan` (renamed **Research + plan**, timeout 6 → 20 min) gained the clone it previously lacked by design: it works triage's blocker list against `main`, `docs/adr/`, `CHANGELOG.md` and `git log`, then emits `needsHuman` — `false` routes straight to `claim → implement` with its `resolvedAnswers` carried into the `implement` prompt, `true` keeps the human gate. Blockers survive the ticks between triage and selection via an updatable marker comment written by `apply-verdicts` and read back by `select`. The new transition tests `needsHuman == false` (not `!= true`), so a malformed value falls back to the gate rather than to autonomous code changes, and `claim` now also drops `fullstack:needs-approval` for the path that skips `notify-gate`. Every agent step emits `confidence` + `confidence_rationale` for per-step observability; `agent.confidenceThreshold` stays unset deliberately — `FallbackHandler` escalates/pauses the *run* rather than routing it, which leaves the issue on `fullstack:in-progress` until a human explicitly adds `fullstack:retry`.
- Login page reorganized to a single focused view at a time: Google / password is the clear primary, "Email me a sign-in link" is one quiet secondary link, and the rare "resend setup link" recovery is a muted footer — each secondary action swaps the whole card to its own sub-view with a Back link (follow-up to [#1019](https://github.com/Appsilon/mediforce/issues/1019)).
- **Breaking — `triggerInput` is now every workflow's total, trigger-agnostic input contract** (ADR-0012). Manual, webhook, cron and spawned firings all validate against it, so a step reading `${triggerPayload.<field>}` behaves identically no matter which trigger fired and workflows are finally decoupled from *how* they were started [#1020](https://github.com/Appsilon/mediforce/issues/1020), [#1017](https://github.com/Appsilon/mediforce/issues/1017).
  - `${triggerPayload.body}` / `.headers` / `.query` / `.schedule` / `.firedAt` **stop resolving** — the raw HTTP envelope and cron tick fields move to a new `${triggerContext.*}` namespace (new nullable `process_instances.trigger_context` column, migration `0040`), which carries transport metadata only and never declared input.
  - Validation is **unconditional**: a workflow declaring **no** `triggerInput` now rejects a non-empty firing on every path (the old `triggerInput.length > 0` guard made an empty contract mean "anything goes").
  - A webhook body's top-level keys now map 1:1 onto `triggerInput` fields and are validated, returning **400 with per-field errors** (`details`) like `start-run` does; a body that isn't a JSON object, or any undeclared field, is rejected.
  - Opaque third-party bodies are declared as a single field of the new **`object`** `triggerInput` type and the sender nests under that key — the two `personal-automations` examples migrate this way, so their senders must now wrap the body.
  - Cron rows gain an optional **static `payload`** in their config, editable per row (two schedules on one workflow can fire different constants) from the Triggers tab and `mediforce workflow trigger-add/-update --payload`; it is validated at attach time against the current contract (write rejected — so attaching a **payload-less** cron to a workflow with a required `triggerInput` field that has no `default` is now an error rather than a row that silently never fires) and again at fire time against the resolved version, where drift **skips the tick with an audit reason** instead of erroring.
  - `mediforce workflow trigger-update` now takes `--schedule`, `--payload`, or both (previously `--schedule` was mandatory) and rejects a non-cron trigger instead of stamping a schedule onto a webhook row. `--payload '{}'` now **clears the payload on create as well as update** — create previously persisted `payload: {}`, which `toPortableTrigger` then exported.
  - **Every unpinned firing now resolves the same Workflow Definition version.** Manual/API start, webhook, cron and `spawn` share one resolver (ADR-0011: the workflow's default version when it is runnable, else the newest non-archived, non-deleted version). Manual, webhook and spawn previously took the *highest* version and ignored the default pointer, so a workflow with default v1 and head v3 started v3 from the UI or a webhook while cron fired v1 — one workflow validating against two different contracts. A start without an explicit `definitionVersion` now honours the default pointer; an archived head falls back to the newest live version; and a workflow whose versions are all archived, or which is soft-deleted, now returns **404** (or a spawn error) instead of firing the archived head. An explicit `definitionVersion` still pins exactly, archived versions included.
  - The `spawn` action now validates a child's payload against **that child's** `triggerInput` before firing it, reporting a mismatch through its existing `errors[]` / `continueOnSpawnError` path — previously a spawned run was the one firing that bypassed the contract entirely, so a typo'd payload key interpolated to an empty string inside the child while the identical payload sent to `POST /api/runs` returned 400. A child whose definition cannot be read is now a spawn error rather than a firing with an unvalidated payload.
  - The `WorkflowTriggerContext` type exported by `@mediforce/workflow-engine` is renamed `WorkflowFiring` — it describes one firing, and the old name now collides with the Trigger Context concept.
  - The webhook Triggers-tab `curl` example body is now generated from `triggerInput` rather than a fixed `{"order": 42}` placeholder.
- `WebhookTriggerConfigSchema` (+ its `WebhookTriggerConfig` type) moved from `schemas/workflow-definition.ts` to `schemas/trigger.ts`, next to `TriggerResourceSchema` — its only consumers are the detached `webhook` trigger row and trigger-create validation, so it no longer belongs in the now trigger-free definition module. `HttpMethodSchema` stays in `workflow-definition.ts` (still shared with the `http` action step). Pure code-organization move; the `@mediforce/platform-core` barrel exports are unchanged [#932](https://github.com/Appsilon/mediforce/issues/932).
- Workflow **Triggers** tab polish: the sections now read **Manual → Scheduled (cron) → Webhook** (was Manual → Webhook → cron). The webhook create form drops the HTTP-method selector — the catch-all route only serves `POST`, so webhooks are always created as POST — and shows a live **Full URL** preview (`/api/triggers/webhook/<handle>/<workflow><path>`) under the **Path** input, so the whole endpoint is visible while you type instead of only the caller-chosen suffix. Both the create form and the created webhook row render a ready-to-run `curl` **Example usage** block including the required `X-Api-Key` header and a JSON body. The create button/heading now read "Create webhook trigger" instead of "Add webhook trigger" [#931](https://github.com/Appsilon/mediforce/issues/931).
- **Monitoring page** rebuilt around real, workspace-scoped data — every tab's mocked placeholders are gone:
  - **Agents**: real Cost/Container/Permissions/Log columns on the run table; KPI cards are now click-to-filter buttons (matching the Workflows tab pattern) instead of static counts.
  - **Users**: the mocked breakdown sections are replaced by a real audit-event activity table (sign-ins — including IP/UA for password sign-in, provider for OAuth — workflow triggered/cancelled, task completed) with User/Event/date-range filters.
  - **Workflows**: KPI cards (in progress / waiting for human / error / completed) computed from the same data the table below renders, click-to-filter; the "All runs" table + its filters moved here from the standalone `/runs` page (still deep-linked from workflow catalog cards).
  - **Tasks**: a new task-activity table (claimed, completed, viewed, attachment added/removed) with a Task column linking to the task and a Workflow column linking to the specific run. Adds a `task.viewed` audit action — the first real "someone looked at this" signal, recorded once per task view.
  - **Integrations**: replaces the fake healthy/degraded/down status board with a plain services list (Databricks, OpenRouter, DeepSeek, GitHub, Mailgun, SMTP, OpenAI Realtime, Google OAuth, OIDC SSO, the alert webhook, OTLP trace export) found by auditing every external API client/env var in the codebase. OpenRouter's card shows the workspace's real, live credit balance; everything else is explicitly labelled illustrative.
  - Every tab's KPI cards and tables now share one consistent style (sticky header, hover-to-filter cards, colored pill labels), keyed off the Workflows tab as the reference [#1118](https://github.com/Appsilon/mediforce/pull/1118).
- **Monitoring page pagination**: all four tabs (Workflows, Agents, Users, Tasks) now default to the 20 newest entries (sorted by event date, newest first) with a "Load more" button, instead of fetching every row up front — on staging, workspaces with thousands of runs/agent runs/audit events made the page take minutes to load, and the Users tab briefly rendered zero rows before the full unbounded fetch resolved. Workflows/Agents KPI cards now report real `COUNT(*) FILTER` aggregates computed in Postgres over the whole filtered set, not a client-side tally of the loaded rows — a card's number no longer changes as more pages load. New keyset-paginated endpoints (`GET /api/runs/page`, `GET /api/runs/status-counts`, `GET /api/agent-runs/card-status-counts`) plus cursor pagination and server-side action/actor/date filters added to the existing `GET /api/agent-runs` and `GET /api/audit-events`. New indexes on `agent_runs`, `process_instances`, and `audit_events` for `(workspace, <sort column> DESC)`. The Users/Tasks tabs' "User" filter dropdown now lists every workspace member instead of only members with activity visible in the (now-paginated, no-longer-complete) loaded page, and their event-count label now reads "N events loaded" instead of "N of M events" — Users/Tasks have no cheap DB-side total count the way Workflows/Agents do under keyset pagination.

### Removed
- Workflow Definitions are now **trigger-free** (ADR-0011): the `triggers` array is gone from the `WorkflowDefinition` / `ProcessDefinition` schemas, the authorable design-LLM surface, and the DB (`workflow_definitions.triggers` column dropped by migration `0039`). Every start path already reads the unified `triggers` table (cron #929, manual #930, webhook #931), so registering, importing, or copying a workflow now seeds only the `manual` singleton — cron and webhook triggers are attached out-of-band via `mediforce workflow trigger-add` / the **Triggers** tab, never derived from the definition. The workflow diagram render, version-summary `triggerCount`, YAML-editor scaffold, and all examples/docs drop their embedded triggers accordingly [#932](https://github.com/Appsilon/mediforce/issues/932).

### Fixed
- Save failures in the workflow editor now show the full, readable error message instead of a clipped span that was only legible by inspecting the DOM. The new-workflow and edit-version pages render the error in a wrapping `max-w-md` span (the `truncate` clip is gone) and route failures through a `handleSaveFailure` helper that rebuilds the message from the structured Zod issues in `ApiError.details` — so it names the offending field (`name is required; transitions.0.from must be a string.`) and reports every issue rather than the server's joined prose [#1034](https://github.com/Appsilon/mediforce/issues/1034).
- The per-workflow detail page's **Runs** tab no longer fetches up to `LEGACY_FIRESTORE_PARITY_LIMIT` (10k) rows and filters/sorts them client-side — the same multi-minute load the Monitoring page shed in #1126. It now renders through the shared paginated `AllRunsPanel` (keyset pagination, 20-newest default) scoped to the workflow via `workflowFilter`, keeps Cost/Started ordering global across every loaded page, and sources the header/tab run count from the same server-side `COUNT(*) FILTER` status-counts aggregation the Monitoring → Workflows KPI cards use (mirroring the panel's dry-run/archived toggles) instead of the length of an unbounded fetch [#1127](https://github.com/Appsilon/mediforce/issues/1127).
- The per-workflow detail page's **Runs** tab no longer fetches up to `LEGACY_FIRESTORE_PARITY_LIMIT` (10k) rows and filters/sorts them client-side — the same multi-minute load the Monitoring page shed in #1126. It now renders through the shared paginated `AllRunsPanel` (keyset pagination, 20-newest default) scoped to the workflow via `workflowFilter`, with the header/tab run count sourced from the same server-side `COUNT(*) FILTER` status-counts aggregation the Monitoring → Workflows KPI cards use (mirroring the panel's dry-run/archived toggles) instead of the length of an unbounded fetch [#1127](https://github.com/Appsilon/mediforce/issues/1127).
- Invites no longer force the create-password flow on a Google/OIDC-only or magic-link-only deployment. `inviteUser` gated `setMustChangePassword` + the activation email on `isInvitePending` alone, so a pending invitee on a deployment with `ENABLE_PASSWORD_AUTH=false` was emailed a "create your password" link, forced onto `/change-password`, and left with a password `POST /api/auth/password-login` would 404 on — all while they could simply sign in with their provider. The enabled-auth-methods signal (`passwordAuthEnabled`, `ENABLE_PASSWORD_AUTH !== 'false'`) is now resolved once at wiring time and exposed via `scope.system`, so the framework-free handler gates the password-setup branch on it and otherwise falls through to the plain workspace-notification email. The same gate now covers every path that could recreate the dead end: **Resend invite** (`resendInvite`) no longer re-arms the gate when password auth is off — it still emails a working one-time sign-in link, since a pending invitee has no session to fall back on, and the link simply lands on workspace selection instead of create-password. The self-service `POST /api/auth/resend-setup-link` recovery behaves identically, and `GET /api/users/me` projects `mustChangePassword: false` whenever password auth is off — so a flag already written by an earlier invite stops trapping the user on `/change-password` without needing a migration or a re-invite (the row is left intact, so re-enabling password auth restores the gate). `setPassword` itself now refuses with a `409` instead of silently storing a hash no sign-in route will accept — the write was reachable by any caller (UI, CLI, apiKey), not just the closed UI entry. The activation email also stops promising a password on such a deployment: subject, heading, body and button switch to sign-in copy, and its one-time link lands on `/workspace-selection` rather than the `/change-password` page the invitee cannot complete. The login page's **"Expected a setup link? Resend"** recovery — the only self-service way out of an expired activation link — was itself hidden whenever both password and magic-link login were off, i.e. exactly on a Google/OIDC-only deployment. It now gates on whether the deployment can send mail at all (`emailDeliveryEnabled`, reported by `GET /api/auth/magic-link-login`), which is what `POST /api/auth/resend-setup-link` actually requires — the Email provider is registered whenever email is configured, independent of which sign-in methods are offered [#1109](https://github.com/Appsilon/mediforce/issues/1109).
- A deploy-interrupted agent/script step retrying against the same Docker container name no longer fails with `Conflict. The container name "…" is already in use` (exit 125): both the local and queued (BullMQ worker) spawn paths now force-remove any stale container holding that name before `docker run`, since `docker run --rm` only cleans up on a clean exit and the retry reuses a deterministic name [#1052](https://github.com/Appsilon/mediforce/pull/1052).
- Navigating from a run's step page back to the workflow page no longer crashes with `Cannot read properties of undefined (reading 'length')`. `NextStepCard` and `useWorkflowVersion` both wrote to the react-query key `queryKeys.workflow(handle, name, version)` but stored different shapes — the card cached the whole `workflows.get` envelope (`{ definition }`), the hook cached `result.definition` — so the workflow page header rendered `latest.steps.length` against the envelope on its first paint, before its own refetch overwrote the entry. `NextStepCard` now goes through `useWorkflowVersion` instead of hand-rolling the query.
- Lazy Docker image builds now clone public `owner/repo` references over anonymous HTTPS while preserving SSH for `git@` references and rejecting invalid deploy-key paths. A shorthand or HTTPS reference that anonymous access cannot see (private repo) falls back to the SSH deploy key, and a token for a host the GitHub converter doesn't understand keeps SSH instead of dropping the deploy key. Transport resolution now lives once in `platform-core` (`resolveRepoCloneTargets`), so the local and `REDIS_URL` worker builds can no longer diverge [#1079](https://github.com/Appsilon/mediforce/issues/1079).
- Agent steps saved without a Docker image now default to `mediforce-golden-image` on registration in **every** mode — the previous gate skipped the default in local-agent mode (`ALLOW_LOCAL_AGENTS=true` + no `REDIS_URL`), leaving the persisted definition without an image and so not deployable to a real container-worker environment. Local-agent mode still ignores the image at run time; the default only makes the saved definition portable.
- Re-entered timer wait steps now dispatch a fresh wait after each workflow loop instead of reusing the previous cycle's resolved output.
- Every `mediforce-fullstack` agent prompt (and the shared preamble) pointed at `docs/CONTEXT.md` for "the canonical vocabulary and the current architecture" — a path that does not exist; the file is `CONTEXT.md` at the repo root. All six agent steps were opening nothing, going into triage/implementation without the architecture doc that answers most of what they were escalating.
- E2E seeding no longer dies on `auth_users_email_unique` when the local database already holds `test@mediforce.dev` under a different id. Since the NextAuth cutover two paths minted ids for that one email — `auth-setup` pins the stable `TEST_USER_ID` and upserts `ON CONFLICT (id)`, while `createTestUser` (used by `pnpm seed:dev`, the multi-namespace API fixture and the workspace-selection journey) minted a random uuid and upserted `ON CONFLICT (email)` — so whichever ran first won the row and the other's insert failed on the unique email, taking the whole suite down with it (`setup` failed → 139 tests skipped). `createTestUser` now accepts the id to use for a new row and all three shared-email callers pass `TEST_USER_ID`; `seedAuthSession` additionally drops any row holding that email under a different id before upserting, so an already-poisoned local database heals on the next run instead of needing manual SQL.
- Resend-invite in workspace settings no longer fails silently for an already-activated member: the button is hidden once the member has signed in, and any remaining `PreconditionFailed` refusal now surfaces as a visible error in the Members section instead of a message the closed invite form swallowed.
- Magic-link sign-in email now renders through the shared branded `emailLayout` (header, teal button, footer), matching the invite / activation emails instead of a plain unstyled body (follow-up to [#1019](https://github.com/Appsilon/mediforce/issues/1019)).
- Change-password page no longer flashes "Change your password" to a first-time invitee before flipping to "Create your new password" — the heading now waits for the `me` query to resolve (follow-up to [#1019](https://github.com/Appsilon/mediforce/issues/1019)).
- Activation / setup-link emails no longer link to `http://localhost:3000` on staging/prod: the invite notification service's fallback base URL now resolves (via `resolveInviteAppUrl`) from `APP_BASE_URL`/`NEXT_PUBLIC_APP_URL` — normalized, so a trailing slash can't produce `https://host//login` — instead of the never-set `NEXT_PUBLIC_PLATFORM_URL`. The self-service resend route also reads the deployment's `platform.baseUrl` setting like the admin invite/resend handlers, so a deployment configured only via that setting no longer emails localhost.
- Requesting a setup link for an existing OAuth-only user (signed in via Google, no password) no longer emails them a bogus "set up your account" link: `PostgresInviteService.isInvitePending` now treats any linked `auth_accounts` row as fully set up, not pending.
- Resending an invite / setup link now invalidates the recipient's prior unexpired activation token (`mintVerificationToken` deletes existing rows for the identifier before minting), so only the newest link works and `auth_verification_tokens` can't grow unbounded.
- Three holes in the Trigger Input contract (ADR-0012) closed together. A field's **`default` now applies on every firing path**, not just the manual Start Run form: `validatePayload` fills in the declared default for each field a firing omitted (`undefined`/`null` — a supplied `false` / `0` / `''` survives) and returns the resolved payload that the API, webhook, cron and `spawn` paths all fire with, so a cron row or webhook body that leaves a defaulted field out no longer produces an empty `${triggerPayload.<field>}`, and a `required` field carrying a `default` is satisfiable by a payload-less cron row. Defaults are validated like any other value, so a mistyped one fails loudly at fire time. **`datetime` fields are now validated** (string + parseable, mirroring `date`) — the type was in `TriggerInputFieldSchema` but had no validator case, so it fell through to the permissive default and a number or boolean passed as a datetime on every path. And a workflow declaring an **`object`** field is now startable from the UI: the Start Run form renders a JSON textarea (an object `default` shows as pretty JSON), parses it into a real object before firing, and blocks submission while the text is not a JSON object — previously the field fell back to a single-line text input that submitted a string the server was guaranteed to reject with "must be a JSON object" [#1020](https://github.com/Appsilon/mediforce/issues/1020).
- `workflow-triggers.journey.ts` no longer races itself: both its tests attach cron rows to the same seeded workflow while the first asserts the empty state, so under `fullyParallel` they landed in separate workers and the second's row failed the first's "no cron triggers yet". The describe now runs `serial`.
- Webhook requests no longer forward credential headers into the run. `authorization`, `proxy-authorization`, `cookie`, and `x-api-key` are dropped before the remaining headers land on the Run's `triggerContext` — which is persisted and readable from any step as `${triggerContext.headers.*}`, so forwarding them let any workflow author in the namespace interpolate the caller's credentials (including the platform's own API key) into an outbound `http` action. The endpoint has already authenticated by that point, so nothing downstream needs them [#1020](https://github.com/Appsilon/mediforce/issues/1020).
- The workflow **Triggers** tab now reads the input contract of the version a firing actually resolves (the default version when set, else the newest live one) instead of the newest version outright, and holds off on its "this workflow declares no trigger input" guidance until that definition has loaded. Previously a workflow whose default version was pinned behind its head showed a cron payload editor and `curl` example built from the wrong version's `triggerInput` — advice the server then rejected — and both briefly claimed no input was accepted while the definition was still in flight. Editing a cron row's schedule no longer resubmits its unchanged payload, so a row whose payload has drifted behind a newer contract can still be retimed (drift is a fire-time skip by design, not an edit-time block) and the `cron.trigger.updated` audit entry no longer reports "payload updated" for every edit [#1020](https://github.com/Appsilon/mediforce/issues/1020).
- Importing a portable trigger-config file with `--replace` no longer deletes the wrong row or partially applies when an entry collides with two different existing rows at once — a webhook entry that collides by name with one row and by type (webhook is a per-workflow singleton) with another now drops **both** before recreating instead of dropping one and 409ing on the other. The mandatory `manual` singleton is never removed to free its name (a non-manual entry colliding with it is skipped, not applied over the deleted manual row), and each row a replace drops is now written to the audit trail (`<type>.trigger.deleted`, basis "Trigger replaced during import") rather than deleted silently. Import now also pre-validates webhook method (POST-only) alongside cron schedules before writing anything, so a file carrying a legacy non-POST webhook is rejected whole instead of persisting the entries ahead of it as a partial import. The portable file schema is now `.strict()`, so a stale runtime dump carrying `namespace` / `lastTriggeredAt` / `workflowName` is rejected on import instead of silently stripped [#933](https://github.com/Appsilon/mediforce/issues/933).
- Workflow authoring and MCP guidance no longer emit or describe the removed definition-level `triggers` field; manual-start rejection now correctly reports that its detached manual trigger is disabled or absent [#932](https://github.com/Appsilon/mediforce/issues/932).
- The workflow page header now reflects **live** cron-trigger state: it reads the unified `triggers` table (via a shared `useWorkflowTriggers` react-query hook) instead of the advisory triggers declared on the definition version, so stopping a schedule in the **Triggers** tab immediately drops "Runs automatically · Every N minutes" (now shown as "Schedule stopped · …") from the header, and a trigger added to a workflow that never declared one shows up there too. The Triggers tab shares the same hook, so a start/stop/edit invalidates one cache and refreshes both header and tab in the same tick [#929](https://github.com/Appsilon/mediforce/issues/929).
- Cancelling a run now reaps any in-flight step execution and agent run rows, so cancelled runs no longer leave the current step rendering as `running` forever. The reap is also guarded against a late worker write: a still-executing agent/script whose plugin finishes *after* the cancel no longer resurrects the reaped `StepExecution`/`AgentRun` to `completed` or persists its output/cost onto the cancelled instance — the runtime bails once it sees the parent is no longer active, and terminal `AgentRun` writes only apply to a row still `running` [#912](https://github.com/Appsilon/mediforce/issues/912).
- `ENABLE_MAGIC_LINK` is now forwarded to the `platform-ui` container in `docker-compose.prod.yml` — the flag was read in code but never passed through, so magic-link login could not be turned on in production regardless of `.env`.
### Added
- Triggers are now **portable across instances** via an import/export trigger-config file (ADR-0011), the capstone of the triggers-detachment epic. `mediforce workflow trigger-export <workflow>` writes an instance-free `triggers.json` (array of `{ name, type, enabled, … }` entries carrying only portable config — no `namespace`, `workflowName`, `lastTriggeredAt` cursor, or derived URL), and `mediforce workflow trigger-import <workflow> <file>` materializes those rows in the target namespace: webhook URLs **re-derive** for the target host, cron cursors **anchor to `now`** (a materialized schedule never back-fires), and each entry is validated + audited by reusing the existing `createTrigger` resolution. Conflict policy is **seed-if-absent** — an existing trigger name is skipped unless `--replace` is set, which drops the row first and recreates it from the file. Also on the **Triggers** tab as Export/Import buttons (with an "overwrite existing" toggle) and the `mediforce.triggers.export` / `import` client + `GET /triggers/export` / `POST /triggers/import` routes. New `TriggerConfigFileSchema` in `platform-core` is the single detached form [#933](https://github.com/Appsilon/mediforce/issues/933).
- Webhook triggers are now managed as detached resources on the unified `triggers` table (ADR-0011), mirroring cron (#929) and manual (#930). The catch-all endpoint `POST /api/triggers/webhook/<namespace>/<workflow><path>` now resolves an **enabled `webhook` trigger row** by exact path + method instead of scanning `definition.triggers`, so attaching, stopping, or removing a webhook takes effect immediately — no new workflow version, and a stopped/removed webhook's URL 404s. Operators can attach / start / stop / remove a webhook from the CLI (`mediforce workflow trigger-add --type webhook --method --path`, which prints the derived URL), the new **Webhook** section on the workflow's **Triggers** tab (one row showing the live URL + start/stop/remove), and the `mediforce.triggers.*` client (`create` now returns the derived `webhookUrl`). Webhook config stays `{ method, path }`; one webhook per workflow is enforced in the handler (a second → 409), path format is validated at the one boundary via `WebhookTriggerConfigSchema`. Registering a workflow that declares a webhook trigger seeds a row **if absent** (opt-in — never auto-seeded), and a one-time migration (`0033`) back-fills rows from every workflow's latest live definition so existing endpoints keep resolving across the cutover. The unused pre-detachment `WebhookTrigger` / `TriggerHandler` / schema-registry classes are removed [#931](https://github.com/Appsilon/mediforce/issues/931).
- Manual triggers are now detached resources on the unified `triggers` table (ADR-0011), fully decoupled from the workflow definition. Hand-starting is gated on an **enabled `manual` trigger row** for `(namespace, workflow)`, not on `definition.triggers`. The manual trigger is a **per-workflow singleton** named `manual`, always present and enabled by default — it can be **started/stopped but never added or removed** (a second create → 409, a delete → rejected; workflow deletion still cascades it away). Every read of the gate now comes from the table: the `ManualTrigger` guard, the workflow-page **Start Run** button + header badge, the definition-editor Start button (all via `useWorkflowTriggers`), and the **workflow catalog** cards (via a new `manualStartEnabled` field on the list API, one cross-namespace read — no per-card N+1) — so `hasManualTrigger` is no longer derived from `def.triggers` anywhere. The **Triggers** tab shows a fixed **Manual** row (labelled "Manual" regardless of any legacy stored name) with only start/stop; a stopped manual trigger disables Start Run with a "start it in the Triggers tab" hint. Register/import/copy all seed the singleton via one shared helper, and a one-time migration (`0032`) back-fills an enabled `manual` row for every live workflow. A hand-start with the manual trigger stopped now returns **409** (`ManualTriggerNotDeclaredError` message) instead of a 500. `mediforce workflow trigger-add --type manual` and `mediforce.triggers.*` manage it from the CLI/API [#930](https://github.com/Appsilon/mediforce/issues/930).
- Cron triggers are now managed as detached resources on the unified `triggers` table (ADR-0011), closing the two-unused-stacks window #928 opened. Operators can add / re-schedule / start / stop / remove a cron trigger on an existing workflow — no new definition version — from the CLI (`mediforce workflow trigger-add|trigger-list|trigger-update|trigger-start|trigger-stop|trigger-remove`, scoped to `--type cron`), the new **Triggers** tab on the workflow page, and the `mediforce.triggers.*` client / `POST|GET|PATCH|DELETE /api/workflow-definitions/:name/triggers…` routes. The heartbeat is now **row-driven** — it enumerates `triggers.listEnabledByType('cron')`, resolves each row's target to the workflow's default→latest live version (skipping deleted/archived/unresolvable targets), fires, and advances the fire cursor via `recordTriggered` — instead of scanning `def.triggers` + the `cron_trigger_state` overlay. Registering a workflow that declares a cron trigger seeds a row **if absent** (cursor anchored to `now`, no back-fire), a one-time migration (`0031`) back-fills rows from every workflow's latest live definition so existing schedules keep firing across the cutover, and the retired `cron_trigger_state` overlay (schema, repo, `scope.cron`, migration `0005` table) is dropped [#929](https://github.com/Appsilon/mediforce/issues/929).
- `sync-model-rankings.py` follows OpenRouter's rankings endpoint move to the versioned `/api/frontend/v1/rankings/performance` path (old path now 404s), so the model-registry ranking sync works again.
- A workflow whose **entry step is a decision/review step** no longer dead-ends with "Workflow routing error". The auto-runner creates the current step's human task, but wasn't copying the step's `verdicts` onto it (only the engine's advance-into-next-step path did), so the task rendered as a params-only form with no verdict picker; completing it recorded no verdict and the engine couldn't route the decision step. The auto-runner now copies verdicts onto the task, matching the engine path.
- Workflow **breadcrumb** shows the title-cased workflow name (e.g. "Promotions") instead of the raw lowercase URL slug ("promotions"), matching the Workflows list.
- Workflow **detail page header** now shows the workflow's display name as a title, with the `Private`/`Archived` pills inline beside it, instead of leaving those pills stranded on an otherwise-empty row above the description.
- Cancelling a run now reaps any in-flight step execution and agent run rows, so cancelled runs no longer leave the current step rendering as `running` forever. The reap is also guarded against a late worker write: a still-executing agent/script whose plugin finishes *after* the cancel no longer resurrects the reaped `StepExecution`/`AgentRun` to `completed` or persists its output/cost onto the cancelled instance — the runtime bails once it sees the parent is no longer active, and terminal `AgentRun` writes only apply to a row still `running` [#912](https://github.com/Appsilon/mediforce/issues/912).
### Changed
- Workflow designer is now **canvas-first** ([ADR-0016](docs/adr/0016-unified-canvas-first-workflow-designer.md)): the two-column split is replaced by a full-width diagram canvas with floating panels — an "Add Block" step picker (control-mode rows CM0–CM4 + a creation/decision toggle), the step editor as a right sidebar, and the source-code editor and secrets moved into modals. An AI Assistant pane ships as a placeholder shell (wired in a later phase). Nodes are draggable with a "Tidy up" auto-layout, and the app shell gets a visual revamp (emerald theme, condensed nav and header).
- Registering a workflow whose agent step omits a Docker image now **defaults to the golden image** (`mediforce-golden-image`) instead of rejecting the registration with a `ValidationError`; a step that names its own image is unaffected.
- **Decision steps now route natively by verdict**, like review steps: the engine follows a decision step's verdict targets, and the step editor exposes verdict routing for both `review` and `decision` step types.
- **Registering a workflow now rejects structurally-invalid definitions** — the graph (reachability, terminal step, dangling transitions/verdict targets) and step references are validated at registration, not just schema-parsed. Note: a previously-stored definition that is incomplete can no longer be re-registered until fixed.
- The **workflow `validate` endpoint / CLI preflight now runs the same graph + reference gate** as register, so a definition can no longer pass `validate` and then fail `register`.
- Saving a workflow (new workflow or new version) now lands on the workflow's **Runs** section instead of the just-saved definition editor; **Save & Start Run** is unchanged.
- **Dry runs now mock every agent type, not just Claude.** Previously `dryRun` was only a tag — real agents ran and real side effects fired; the only mock (`MOCK_AGENT`, a process-wide dev/e2e switch) swapped in a canned plugin for `claude-code-agent` alone. A dry run now resolves a generic `MockAgentPlugin` for *any* agent/script step (claude / opencode / script-container / databricks-job), per-run, so testing a workflow's structure never burns a real LLM call or spawns a container. Action steps (http/email/spawn) are unchanged — their inputs are the author's explicit choice, collected in the start dialog.

### Added
- `ALLOWED_EMAIL_DOMAINS="*"` is an explicit, logged opt-out that lets any email domain sign in via Google/OIDC — boot WARNs loudly that the restriction is disabled, while an empty/unset allowlist still fails boot (ADR-0002 §4a).
- Workspace branding in **Workspace settings** (organizations): owners/admins can upload a **logo** image and set a **main** and **auxiliary** brand color. The logo replaces the Lucide icon wherever the workspace is shown (sidebar switcher, workspace header, workspace picker) and falls back to the icon when unset; the two colors override the `--primary` / `--accent` design tokens app-wide (plus `--ring` / `--sidebar-primary` / `--sidebar-ring`, with a contrast-picked foreground). Light and dark derive **separately**: dark lifts the main color to the lightness its default token already uses so a dark brand color stays legible, and clamps the auxiliary color into the near-black low-saturation band `--accent` occupies in dark — it is the `hover:bg-accent` surface, so a hover state hints at the brand rather than competing with the content on top of it. Logo is stored inline as a base64 data URL on the workspace record and travels with the existing `namespaces.get` / `users.me` payloads — no new upload/serving route; uploads are downscaled to 256px on the longest edge before encoding (SVGs pass through untouched), so a multi-megabyte brand-kit PNG stores in the single-digit KB range instead of being rejected, and the schema cap is a backstop rather than a limit users have to respect. Colors are `#rrggbb` hex on the same record. All three flow through the existing `PATCH /api/namespaces/:handle` (empty string clears), and the base64 logo is kept out of the audit snapshot. Branding is organization-only in the handler, not just hidden in the settings UI — a personal namespace renders its linked user's avatar, so `icon` / `logo` / brand colors on one are rejected with `409 precondition_failed` naming the offending fields. `mediforce namespace update` covers the same fields as the UI via `--logo` (a file path, or a `data:` URL; note the CLI has no browser canvas so it does **not** downscale — an oversized source is rejected by the cap rather than resized), `--brand-primary-color` and `--brand-accent-color`. Uploading an **animated GIF** stores its first frame: the downscale re-encodes rasters as PNG, which carries no animation — an animating sidebar logo would be a distraction, and passing GIFs through unresized would skip downscaling on the format most likely to be multi-megabyte. All five logo/icon renders across the app now share one `WorkspaceAvatar` component, so a corrupt stored logo falls back to the workspace icon everywhere instead of showing a broken-image glyph at the four sites that previously had no error path.
- Magic-link sign-in (Auth.js Email provider, env-gated `ENABLE_MAGIC_LINK`) gives password-only users a first-password / recovery path: the link only reaches existing allowlisted users, who set a password after signing in ([#1001](https://github.com/Appsilon/mediforce/issues/1001), [#1002](https://github.com/Appsilon/mediforce/issues/1002)).
- Invite onboarding: a pending invitee gets an activation email with a one-time 7-day link that signs them in and lands them on "Create your new password"; the Email provider infra powers this even when magic-link login is off, and a self-service `resend-setup-link` recovers an expired link ([#1002](https://github.com/Appsilon/mediforce/issues/1002), ADR-0002).
- **Save & Dry Run** button in the workflow editor, beside Save & Start Run — saves the workflow and immediately starts a dry run of it. Because each button now carries its run mode, the pre-start dialog (shown when there are trigger inputs or warnings) no longer re-asks Dry Run vs Start; it shows a single confirm for the chosen mode. The dual choice remains only on the generic Start Run button used elsewhere.
- Output File preview modal gains a **fullscreen toggle**: large HTML reports rendered in the sandboxed iframe previously nested scrollbars inside the centered `max-w-4xl`/`max-h-[85vh]` box. The new maximize button (shown for any previewable file) expands the modal to fill the viewport, and in fullscreen the HTML iframe fills the available height and scrolls its own content — a single native scrollbar instead of the iframe's clamped-height scrollbar nested inside the modal's.
- Deploy-interrupted agent/script steps now recover in **seconds as a retry** instead of hanging ~45 min then failing as a timeout (ADR-0010 §4). Staging redeploys `platform-ui` on every merge to `main` and agent steps run 20–30 min, so a deploy landing mid-step is common. A new `SIGTERM` graceful-shutdown hook (wired in `instrumentation.register()`) marks each in-flight step execution `interrupted` — a new terminal-ish `StepExecution` status, distinct from `running`/`failed` — before the process exits, driven by a shared `globalThis`-backed in-flight registry (`instanceId → executionId`) that both the auto-runner and the hook see. A boot-time sweep then immediately re-kicks any run left `interrupted`, and the auto-runner's reap guard retries an `interrupted` prior execution as a **fresh attempt** (we know it was a deploy, not a genuine timeout) rather than reaping it as a timeout failure — still bounded by the persisted `MAX_STEP_ATTEMPTS` cap since the interrupted row counts. The retry also terminalizes the interrupted attempt's orphaned `AgentRun` (marked `interrupted`) so it doesn't linger `running` in the Agents history, and the run's step history renders the interrupted attempt as **Interrupted** rather than a stuck "Running". The #906 timeout-reap remains the backstop for deaths SIGTERM can't observe (SIGKILL, OOM, hard crash) [#907](https://github.com/Appsilon/mediforce/issues/907).

### Changed

- Authentication moved from Firebase Auth to NextAuth / Auth.js v5 — Google OAuth, a password sign-in route, and revocable database sessions in Postgres (ADR-0002).
  - Firebase Auth, the Firebase Admin SDK and the auth emulator are gone; the user directory, global process-roles and invites read Postgres, and a one-time seed keeps every existing uid so users re-login without losing data.
  - `pnpm dev:mock` starts the dev Postgres container instead of the Firebase emulator; the E2E suite seeds a database session cookie directly.
  - Operational staging cutover runbook (`docs/archive/RUNBOOK-0002-staging-cutover.md`) plus a guarded per-environment seed wrapper (`scripts/migrate-firebase-auth-to-postgres/migrate-to-nextauth.py`); cdisc is paused from auto-deploy until its own cutover ([#1014](https://github.com/Appsilon/mediforce/issues/1014)).

## [2026-07-19]

### Added
- Unified `triggers` data model — the plumbing for the triggers-detachment epic ([#927](https://github.com/Appsilon/mediforce/issues/927)). A `TriggerResource` schema (discriminated union over `manual` / `webhook` / `cron`, `event` reserved), `TriggerRepository` interface + in-memory double, a Postgres `triggers` table (migration `0030`, keyed by `(namespace, workflow_name, trigger_name)`, partial webhook-path uniqueness + partial enabled-by-type index) with a parity-tested repo, and an authorized wrapper reachable at `scope.triggers` (workspace-gated) and `scope.system.triggers` (heartbeat's cross-namespace `listEnabledByType('cron')`). No user-visible behaviour change — nothing reads or writes the table yet; the cron-only `cron_trigger_state` overlay is left untouched. See [ADR-0011](docs/adr/0011-triggers-detached-unified-resource.md) [#928](https://github.com/Appsilon/mediforce/issues/928).
- **Canvas mutation mechanics**: add / insert / delete / reorder steps directly on the diagram with undo/redo and slugified step IDs; a JSON source editor (replacing the YAML editor) that validates the graph and step references on paste before applying; and a header save flow offering **Save**, **Save & Start Run** (register then start via a resolver-ref handoff), and version naming ([ADR-0016](docs/adr/0016-unified-canvas-first-workflow-designer.md)).
- **In-canvas AI Assistant** ([ADR-0016](docs/adr/0016-unified-canvas-first-workflow-designer.md) Phase 4): a chat pane in the workflow designer that edits the canvas via `add_step`/`update_step`/`remove_step` tool calls, plus a `mediforce.assistant.ask` handler/contract, a route adapter, and a `mediforce assistant ask` CLI command. The handler runs a bounded tool-call loop — applying tool calls and re-prompting when the model leaves the graph structurally incomplete, and building large workflows across several turns by treating a model length-cutoff as a chunk boundary (the steps that already landed are kept and the model is told to continue), rather than failing the whole request. Terminal steps are canvas-managed, so the tools reject `type: "terminal"` with an actionable hint instead of letting the model loop on it. The tool schema exposes every field a user can author by hand — including `ui` (custom task bodies: **file-upload** for dataset/PDF uploads, `assignment-table`, `table-editor`), `assignedTo` (pre-assign a human task), and `continueOnError` — so the assistant has parity with the manual editor. In the pane, successful edits show inline while errors surface as an error toast, and a rotating status ("Planning… / Building steps… / Validating…") plays while it works. No workflow engine or session persistence involved (design-time only).
- **Canvas mutation mechanics**: add / insert / delete / reorder steps directly on the diagram with undo/redo and slugified step IDs; a JSON source editor (replacing the YAML editor) that validates the graph and step references on paste before applying; and a header save flow offering **Save**, **Save & Start Run** (register then start via a resolver-ref handoff), and version naming ([ADR-0016](docs/adr/0016-unified-canvas-first-workflow-designer.md)).
- Workflows can carry a human-facing **display name** (`metadata.displayName`), shown across the UI (workflow header, run cards) in place of the slug.
- Integration test coverage for the workflow engine's full execution loop (`packages/workflow-engine/src/__tests__/integration.test.ts`): mixed agent → human → agent → terminal lifecycle with state asserted at every transition, agent-output propagation into `instance.variables` and the downstream step's input, agent-crash → failure → retry → complete recovery, and verdict-based routing (approve vs revise) — all driven through the in-memory repos, no emulators [#794](https://github.com/Appsilon/mediforce/pull/794).

### Fixed
- Flaky suite timeouts on `main`: three heavyweight test files (`bin-shim` CLI subprocess spawns, `workspace-docker` real-container integration, `provider-form` multi-field `userEvent` submits) inherited vitest's default 5000 ms per-test timeout. Each passes in isolation but tips over that budget under the full monorepo `vitest run`, where every package's suite runs in parallel and saturates CPU — surfacing as three `Test timed out in 5000ms` failures plus one `userEvent` double-fire. Each `describe` now sets an explicit `{ timeout }` (20 s / 60 s / 15 s) scoped to just these known-heavy files, so the default stays tight everywhere else and won't mask genuinely slow product code.
- Workspace-invite and resend-invite emails no longer link to `localhost` when the deployment env is misconfigured: the invite/login/workspace URL now resolves at send time from a new `platform.baseUrl` platform setting first, falling back to `NEXT_PUBLIC_PLATFORM_URL` then localhost as before. Operators can point invite links at the real host with a single `mediforce config set platform.baseUrl https://<host>` (no `.env`/compose-passthrough edit, no SSH). Resolution is deployment-global (every workspace shares the host; the handle is already the path segment), read via `scope.system.platformSettings` in the invite/resend handlers and applied by the email adapter over its construction-time fallback. Writing platform settings (`PUT /api/config` / `setConfig`) is now restricted to system-actor callers — the CLI / agent runtime — so an authenticated end user can no longer repoint the deployment-wide invite-link host; the base-URL override is also normalised (trim, strip trailing slashes, blank → fallback) at the email adapter, not just at the config resolver, so any future caller gets robust links.
- Agent/script step timeouts are now **durable**: a step whose driver dies mid-run (deploy, crash, OOM) no longer stays `running` forever. The heartbeat's stranded sweep ([#892](https://github.com/Appsilon/mediforce/issues/892)) previously *re-ran* such a step; the auto-runner now detects the stranded `running` execution(s) on re-entry and, once **every** in-flight row is past the step's effective timeout, **reaps each through the existing timeout fallback** (`fallbackReason='timeout'`, escalate/fail/flag per `fallbackBehavior`) — so a dead-driver timeout behaves identically to a live one — while an execution not yet past its timeout defers the whole step to the heartbeat so a live step is never double-run. A `continue_with_flag` reap advances the run past the flagged step instead of re-dispatching it. A script **timeout** now fails the run deterministically instead of relying on the loop-guard (scripts have no escalation path); a live script error keeps its prior behaviour, tracked in [#924](https://github.com/Appsilon/mediforce/issues/924). A persisted `MAX_STEP_ATTEMPTS` cap (floating above a review step's `maxIterations`) bounds re-kick/retry loops so no step can loop forever, and `resolveStepTimeoutMinutes` is now the single source feeding both the `Promise.race` and the container-kill timer (in-process **and** the BullMQ worker), closing the `20 min` vs `30 min` gap that misclassified an unconfigured step's timeout as an `error`. See [ADR-0010](docs/adr/0010-durable-step-timeout-recovery.md) [#868](https://github.com/Appsilon/mediforce/issues/868).
- Workflow diagram now fits the whole graph on load instead of pinning zoom at 0.95, so tall workflows no longer render their top steps clipped above the canvas (and under the page header) where they couldn't be clicked or hovered; `fitView` may now zoom out to `minZoom: 0.2` and caps at `0.95` for small graphs.
- Task-attachment uploads over 10 MiB no longer fail with an opaque "Internal error". Next 16.2's default `experimental.proxyClientMaxBodySize` (10 MiB) truncates any larger request body in the router layer *before* the route runs, which makes `req.formData()` throw — while the app's own attachment ceiling is 100 MiB (`ATTACHMENT_MAX_BYTES`), so every SDTM dataset over 10 MiB 500'd at the `upload-inputs` step. Raised `proxyClientMaxBodySize` to 110 MiB (attachment ceiling + multipart overhead), and the upload route now maps a truncated/unparseable body to a `413 payload_too_large` with an actionable message instead of a generic `500 internal`.
- Workspace-invite and resend-invite emails no longer link to `localhost` when the deployment env is misconfigured: the invite/login/workspace URL now resolves at send time from a new `platform.baseUrl` platform setting first, falling back to `NEXT_PUBLIC_PLATFORM_URL` then localhost as before. Operators can point invite links at the real host with a single `mediforce config set platform.baseUrl https://<host>` (no `.env`/compose-passthrough edit, no SSH). Resolution is deployment-global (every workspace shares the host; the handle is already the path segment), read via `scope.system.platformSettings` in the invite/resend handlers and applied by the email adapter over its construction-time fallback. Reading and writing platform settings (`GET`/`PUT /api/config` — `getConfig`/`getConfigByPrefix`/`setConfig`) is now restricted to system-actor callers — the CLI / agent runtime — so an authenticated end user can no longer repoint the deployment-wide invite-link host or read secret settings such as `alert.webhook.url` (the invite/resend handlers still read `platform.baseUrl` directly via `scope.system`, so this gate does not affect them); the base-URL override is also normalised (trim, strip trailing slashes, blank → fallback) at the email adapter, not just at the config resolver, so any future caller gets robust links.
- `mediforce-fullstack` no longer re-triages the same declined (`fullstack:manual`) issue on every tick: the "re-judge on edit" check compared `issue.updated_at` against the manual label event, but `updated_at` is bumped by *any* activity — including a human adding an unrelated label — which latched issues like #425 into an endless re-triage loop. `fetch-candidates` now treats an issue as edited only when `updated_at` outruns the newest recorded issue *event* (label/assignment changes create an event at the same instant, so they are ignored; body edits and comments are the only changes that don't, so they still re-judge), and `apply-verdicts` re-stamps the label when re-declining an already-manual issue so a genuine edit re-judges exactly once instead of looping.
- Runs stranded in `running` are now recovered by the cron heartbeat. When a run's auto-runner request dies mid-step (deploy, crash, or request timeout — the whole step loop runs inside one long-lived `/run` request), the run is left `status: running` with no live driver. The heartbeat's sweeps only looked at `paused` runs (resume `waiting_for_timer`, fail `paused`/null-reason orphans), so a stranded `running` run was invisible to every sweep and sat at its current step forever — e.g. a `mediforce-fullstack` run wedged at `arm-timer` for 2h+ while its PR's CI had long since gone green. The heartbeat now also sweeps `running` runs idle past their current step's own configured timeout plus a grace margin (falling back to a 45-minute default when the step can't be resolved), so a step that legitimately runs longer than the default is never mistaken for stranded, and re-kicks them; the re-kick is idempotent — `/run` returns 409 while a live driver still holds the per-process lock, so only genuinely driverless runs advance [#892](https://github.com/Appsilon/mediforce/issues/892).
- Cancelling a run now cancels its still-actionable (`pending`/`claimed`) human tasks too, so dead runs no longer leave zombie "Action needed" items lingering in the task queue; the cancel audit event records how many tasks were cancelled [#639](https://github.com/Appsilon/mediforce/pull/639).
- Agent steps with input files no longer 500 on server-side download. Attachment `downloadUrl`s are minted browser-side as same-origin *relative* paths (`/api/attachments/:id/blob`), which Node's `fetch` rejects ("Failed to parse URL"). The container plugin now resolves them against the platform base URL (`APP_BASE_URL`, falling back to `http://localhost:9003`) and attaches the system `X-Api-Key` — server-side self-fetches carry no browser session cookie — but only when the resolved origin matches our own base, so the key never leaks to a third-party host that supplied an absolute URL [#915](https://github.com/Appsilon/mediforce/pull/915).
- Switching workspace from a list route (Runs/Agents/Tools/Tasks/Monitoring) now lands on the same list in the target workspace instead of the workspace home, so e.g. switching while viewing your run history keeps you on the target workspace's run list rather than dumping you on home; resource-detail routes (a specific workflow name or run id) still fall back to the target workspace home, since an id from one workspace won't resolve in another [#623](https://github.com/Appsilon/mediforce/issues/623).

## [2026-07-05]

### Added
- `task_assigned` notifications now dispatch when a human task is created (opt-in via a `task_assigned` entry in the workflow's `notifications`), so role members are pushed an email instead of having to pull the "Human actions" inbox; a pre-assigned (`assignedTo`) or fallback-to-creator assignee is also notified even when outside the configured roles, so a claimed task never leaves the responsible person polling the inbox. Fires from both task-creation paths — the engine's `advanceStep` (next step is human) and the API auto-runner (an already-current human step, e.g. a workflow whose first step is human) — via one shared `engine.dispatchTaskAssignedNotification`; mirrors the existing `agent_escalation` dispatch [#818](https://github.com/Appsilon/mediforce/issues/818).
- `mediforce-fullstack` gains a `TRIAGE_ONLY` mode: when set, the pipeline triages the batch and persists the verdict labels (incl. auto-closing proven-obsolete issues) then stops before `select`, so a run can label the backlog without opening any PR — pairs with `FULLSTACK_REASSIGN` to re-label the whole backlog for review. The flag is echoed into `fetch-candidates` / `apply-verdicts` output as `triageOnly` (transition expressions can't read env) and routes both to `done-empty`. `TRIAGE_BATCH_MAX` is now a `{{…}}` env ref (tunable without re-registering) and its default drops from `25` to the safer `10` for the grounded clone-and-verify triage pass.
- Run **Files** panel now has a "Download all" button in the section header, so every Output File across all steps can be saved in one click instead of downloading each row individually.
- Output Files can now be previewed in-browser from the run **Files** tab and the step-level file list — a "View" action opens a modal that renders markdown, HTML (sandboxed iframe), CSV/TSV (table, first 100 rows), images, SVG (as an image, so embedded scripts can't execute), plain text/JSON/log, and PDF; unsupported types (rtf, xlsx, …) and text files over 5 MiB fall back to the existing download. Bytes come from the same authenticated, workspace-scoped file route — no new download surface.
- Capability map ([docs/reference/workflow-capabilities.md](docs/reference/workflow-capabilities.md)) extended to cover previously-undocumented surfaces, each cross-referenced to the schema *and* the runtime that enforces it: agent autonomy L0–L4 / `confidenceThreshold` / `fallbackBehavior` / `review` loop / `allowedTools` internet access / `continueOnError`; MCP & tool governance (`agentId`, `mcpRestrictions`, Agent Definitions, Tool Catalog, OAuth) and effective-MCP resolution; notifications; richer human steps (the `file-upload`/`assignment-table`/`table-editor` component registry, completion-kind union, verdict `intent`/`requiresComment`, param `options`/`requiredForVerdicts`); cowork `outputSchema`/`outputSchemaRef` + voice tuning; the full trigger set (incl. `event` flagged as enum-only/no-router) and `triggerInput` field types; workflow-envelope fields (`visibility`, `roles`, `preamble`, `source` provenance); and action-handler nuances (spawn 50-child cap, email rate limits, http non-2xx semantics).
- `design-workflow` skill upgraded: new capability map ([docs/reference/workflow-capabilities.md](docs/reference/workflow-capabilities.md)) cross-referencing every executor, action kind, both expression languages, human-step UI, scripts, and models to the source files that define them (drift-guarded by the doc-lint test); a distilled `spawn`+`forEach` fan-out tutorial ([docs/workflow-examples/11-fan-out-orchestration.wd.json](docs/workflow-examples/11-fan-out-orchestration.wd.json)) plus a capability index in the examples README; the skill now syntax-checks (Phase 3b) and behavior-tests (Phase 3c) every generated script, defers commit-SHA pinning to a Phase 4 post-commit step (with a `git commit --amend` unify path), and corrects the model-list guidance (`mediforce model list` needs a deployment + `MEDIFORCE_API_KEY`; prefer short Claude aliases offline).
- Workflow authoring docs split into a process guide ([docs/guides/create-workflow.md](docs/guides/create-workflow.md)) and a production checklist ([docs/reference/workflow-authoring-golden-rules.md](docs/reference/workflow-authoring-golden-rules.md)), backed by the end-to-end `apps/golden-standard-workflow` reference package. A CI doc-lint test re-derives every cross-reference, the `executor`/`type` enum tables, the referenced `mediforce` CLI commands, and the ADR links from source, so the docs fail the build instead of silently drifting [#784](https://github.com/Appsilon/mediforce/pull/784).

- Agent steps now record `tokenUsage.peakInputTokens` — the largest single-turn prompt the model held during a run — and the executor logs a `[saturation]` line (`peak / contextLength = N%`) per step. Unlike the summed `inputTokens` (kept for cost), this is the real context-occupancy signal for sizing agent batches (e.g. how many issues to hand triage at once). Populated from OpenCode `step_finish` per-turn token counts.

### Changed
- Website "How it works" and README "How It Works" sections now describe the current CM0–CM4 Control Mode taxonomy (No agent, Assist, Cowork, Human review, Autonomous agent) instead of the retired L1–L4 autonomy-level labels, with icons and colors matching the workflow designer's step-type picker [#882](https://github.com/Appsilon/mediforce/pull/882).

### Fixed
- `mediforce-fullstack` agent steps lost their result contract: the prompts told the model to write `result.json` to `${output_dir}`, but the platform maps `{output_dir}` to the git worktree (`/workspace`), while the result contract must go to the separate Result Contract Directory (`/output`). The model wrote `/workspace/result.json` (committed to the run branch), the engine couldn't find the contract and fell back to raw text, so every JSON-returning step (`triage`, `draft-plan`, `self-review`, `revise`, …) silently produced no structured output — e.g. `triage` classified an issue but `apply-verdicts` received zero verdicts and labelled nothing. All six agent prompts now write to the literal `/output/result.json`, matching the script steps and the platform's own contract-directory instruction. Verified on staging: `triage` verdicts now reach `apply-verdicts` and the issue is labelled.
- Pre-assigned human tasks (`assignedTo`) whose resolved value is an email are now resolved to the user's uid via the user directory before persisting as `assignedUserId`, so the task surfaces in the approver's queue (queues match on uid). Previously the email was stored verbatim, matched no user, and the task vanished from every queue — this stranded `mediforce-fullstack` approval gates whose `FULLSTACK_REVIEWER_MAP` used email values, as the README documented. An email that matches no user now hard-fails the run instead of silently stranding.
- Run execution history now shows the correct control-mode badge for agent steps stored without an explicit `autonomyLevel` — previously they fell through to "No agent" (CM0) even though an agent was running; they now display as "Autonomous agent" (CM4), which matches the design intent that CM0 is reserved for `human | script | action` executors only.
- community-digest `gather-changes` step now fails loudly on GitHub API errors instead of silently emitting an empty digest — non-2xx responses (notably 403/429 rate limits) now throw and exit non-zero via `main().catch`, with a clear message surfacing the `X-RateLimit-Reset` time; the only tolerated non-fatal case is a 409 on branch-commits fetch (branch deleted between listing and fetching) [#622](https://github.com/Appsilon/mediforce/issues/622).
- Added a composite `(actor_id, timestamp DESC)` index (`audit_events_actor_idx`) to `audit_events` so `PostgresAuditRepository.getByActor` (`WHERE actor_id = ? ORDER BY timestamp DESC LIMIT n`) uses an index scan instead of a full scan + sort on the append-only audit table [#612](https://github.com/Appsilon/mediforce/issues/612).
- Editing an OAuth provider no longer forces the admin to re-enter the `Client secret` — leaving the field blank on edit omits it from the PATCH body so the stored secret is preserved; the placeholder reads "Leave empty to keep current secret" to make the affordance obvious [#332](https://github.com/Appsilon/mediforce/issues/332).

## [2026-06-21]

### Fixed
- Context-saturation logging now covers the `claude-code-agent` plugin, not just OpenCode. Peak per-turn context occupancy is computed from the stream's per-turn `assistant` usage (summing `input_tokens` + cache-read + cache-creation, since caching hides most of the prompt from `input_tokens`) and folded into the result event's usage as `peak_input_tokens`, so `logContextSaturation` reports a real peak instead of nothing. Previously only the final turn's `usage` was surfaced, which is not the peak — Claude Code steps produced no `[saturation]` line, leaving batch-size tuning blind.
- Workflow-designer cowork chat no longer hangs on its mandatory first step: the `mediforce-mcp` server crashed on startup because `@mediforce/platform-core` was an undeclared (phantom) dependency and its `workflow-examples` loader had no subpath export, so `list_workflow_examples` was silently unavailable (the connection failure is swallowed). Declared the dependency, added the `./workflow-examples` export, and import it via the package specifier.
- Postgres workflow definitions now persist imported workflow `source` provenance, so get/list calls keep the advertised url/path/commit after GitHub imports.
- A malformed or legacy-shaped `source` (e.g. the pre-commit `{ repo, path, ref }`) no longer drops the entire workflow definition on read — the informational provenance is dropped best-effort and the workflow still loads and runs.
- `pnpm test` is now fully self-contained — Playwright `globalSetup` auto-starts the Firebase Auth emulator when absent and runs pending DB migrations, so E2E tests pass without manual pre-flight steps.

### Changed
- Autonomy levels overhaul across workflow designer and execution history — step-add popover is now a two-step wizard (pick step type, then pick executor: human / agent / script / cowork); step editor shows executor identity and L2/L3/L4 autonomy level instead of the "control mode" abstraction; execution history chips and the agent run table surface the raw autonomy level badge; executor identity in step history renders as `agent:<plugin>`; branch icons in the decision diagram updated to chevrons; schema fields (`executor`, `autonomyLevel`) unchanged [#783](https://github.com/Appsilon/mediforce/pull/783).

### Added
- CI deploy now targets both `staging.mediforce.ai` and `cdisc.mediforce.ai` — the staging deploy workflow fans out over a server matrix (parallel, per-target cancellation, `fail-fast: false`).
- Canonical workflow-definition schema validation endpoint (`POST /api/workflow-definitions/validate`) + `mediforce workflow validate` CLI command, both running the same `parseWorkflowTemplate` schema (cross-field rules included) that registration uses. The workflow-designer `validate` step now calls this endpoint instead of a hand-rolled partial JS reimplementation that drifted from the schema.
- Runtime workflow-schema injection for cowork designers — chat and voice workflow-designer sessions now resolve the authorable WorkflowDefinition schema in-process from the running app before the session starts, without a schema-fetch container step or baked voice snapshot.
- Workflow designer now learns the correct schema from 11 CI-tested examples + 6 anti-patterns served via `list_workflow_examples` MCP tool — eliminates stale inline schema that caused script steps to generate with the old `step.agent` config instead of `step.script` [#787](https://github.com/Appsilon/mediforce/pull/787).
- Import workflows from git now records the resolved commit SHA as provenance (`source: { url, path, commit }`) by resolving the requested ref to an immutable SHA and fetching the file at that SHA; a `.wd.json` that declares its own `namespace` imports cleanly (the target namespace wins, matching `workflow register`); the import dialog gains a branch/tag/commit field (defaults to main), an "Import by path" mode that also kicks in when a repo has no `index.json` manifest, and no longer pre-selects every browsed workflow. Scope (one-time copy, public GitHub only) documented in [docs/guides/import-from-git.md](docs/guides/import-from-git.md) and [ADR-0009](docs/adr/0009-workflow-import-scope-boundary.md).
- Task-attachments backend — `BlobStore` (filesystem impl) + a `task_attachments` table behind a workspace-scoped repository, with headless upload/list/download/delete routes and a typed client; bytes live on the `~/.mediforce` volume, metadata in Postgres, size guarded by `MEDIFORCE_ATTACHMENT_MAX_BYTES` (ADR-0003 PR2) [#765](https://github.com/Appsilon/mediforce/pull/765).
- Docker image validation at workflow registration — server warns when a referenced image is not found on the platform, CLI deduplicates server vs local warnings, and the workflow editor shows an amber toast on save [#734](https://github.com/Appsilon/mediforce/pull/734).
- SMTP email provider as alternative to Mailgun — organisations can now use their own SMTP infrastructure instead of Mailgun by setting `SMTP_*` env vars; `EMAIL_PROVIDER` auto-detects or can be set explicitly; includes read-only admin status page, `mediforce email status` CLI command, and bootstrap script prompts for initial setup [#748](https://github.com/Appsilon/mediforce/issues/748).
- **Run view UX** — execution history panel (renamed from "Step Status"), scrollable workflow diagram, active step info (started time, live elapsed timer, executor name/claimer for claimed human tasks)
- **Task view** — spreadsheet-style table replaces card grid; status column, clickable run links, bulk cancel via batch endpoint, hide-completed toggle. Absolute dates replace relative timestamps in the table. `process × action` cross-grouping (sub-grouping within a process group by action type) is not carried forward in the table layout; each grouping mode renders a flat table.
- **Process card** — step-progress dots rendered against the run's actual definition version instead of the latest definition
- Pre-flight model validation: agent steps with unknown model IDs show an "Unknown model" warning (with Levenshtein-based "did you mean?" suggestions) in the start-run dialog before the run begins.
- Start-run dialog: selecting a different workflow version from the dropdown no longer skips pre-flight checks — the dialog now waits for the new version's definition and validation to load before allowing start.
- Extracted `normaliseModelId` to `platform-core` — was duplicated 5 times across 3 packages.
- OpenRouter credits indicator and the start-run low-credit warning now use the effective spendable balance `min(key limit, account credits)` (indicator shows a `(key limit / credits)` breakdown), instead of the key-limit ceiling alone which overstated available funds.
- Codex now mirrors Claude's repo-local skill and agent discovery layout via `.codex/skills/*` and `.codex/agents` symlinks, with `AGENTS.md` documenting how Codex should translate Claude-specific skill tool syntax.

### Removed
- Firebase Storage removed entirely — the task file-upload UI now uploads, lists, and downloads attachments through the headless attachments API instead of `firebase/storage`, and the Storage SDK, emulator, `storage.rules`, and `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET` are gone; the per-byte upload progress bar becomes a per-file spinner (single non-streaming upload) and legacy demo attachments that were never migrated no longer render (ADR-0003 PR4).
- Dead uploaded-skills feature — dropped `AgentDefinition.skillFileNames` + the `agents.skill_file_names` column, the skill-upload UI, and the Firebase Storage skill download from agent identity resolution; `skillsDir` is now the only skill mechanism (ADR-0003 PR1).
- E2E GIF recording: the recording helper, ffmpeg conversion scripts, the `docs/features/FEATURES.md` gallery, and `test:e2e:record`/`:gif` scripts (3 README screenshots kept). UI journey tests stay as normal L4 e2e (page-error tracking kept in `helpers/page-errors.ts`).

### Fixed
- Cowork workflow designer can render diagrams again — nothing depended on `@mediforce/mediforce-mcp`, so pnpm never linked its `mediforce-mcp` bin onto PATH; the chat handler's spawn failed with ENOENT and the MCP tools (including `render_workflow_diagram`) were silently absent, so the designer narrated "let me render the diagram" against a toolset that lacked the tool and the Artifact panel's Preview tab never appeared. Added `@mediforce/mediforce-mcp` as a `platform-ui` dependency so the bin links onto the spawning process's PATH.
- Cowork chat no longer silently drops large artifacts — the OpenRouter call left `max_tokens` at the client default of 4096, so building a multi-step workflow truncated the `update_artifact` tool-call JSON mid-stream, `JSON.parse` threw, and the artifact panel stayed empty behind a misleading "Artifact update skipped (parse error)." message. The chat turn now requests 16384 output tokens, the client surfaces `finishReason`, and a truncated turn reports an actionable "response truncated at the token limit — build it up across several updates" error to both the user and the model instead of a parse error.
- `render_workflow_diagram` no longer rejects in-progress workflows that route purely via step `verdicts` — its input schema required a top-level `transitions` array even though the renderer already handles its absence, so rendering a verdicts-only draft threw a Zod error; `transitions` now defaults to `[]`.
- Cowork MCP tools now work when env templates (`{{SECRET}}`) reference workspace secrets — `connectMcp` passes the resolved secrets to `McpClientManager` so template interpolation succeeds in both local dev and staging [#787](https://github.com/Appsilon/mediforce/pull/787).
- Binary file-download routes (task-attachment blob, run output files, agent output file) now send `X-Content-Type-Options: nosniff` — defense-in-depth against MIME-sniffing a user-supplied content-type into an executable type (ADR-0003).
- Skill-loading steps no longer intermittently fail with "Skill file not found" from another workflow's cache — the container plugin singleton kept the skills-cache dir in a mutable field that leaked across runs; the path is now derived on demand from the workflow's `externalSkillsRepo`.
- External skills repo now clones public repos over anonymous HTTPS — `fetchSkillsFromRepo` no longer forces an SSH clone (which fails in the agent container, where there is no `ssh` binary) when no auth token is configured; SSH/deploy-key transport is used only for genuine `git@` refs.
- Deploy-key cache now recovers when the cached path is replaced by a directory (EISDIR); auto-runner stuck-loop errors now surface the underlying step error in `mediforce run get` [#776](https://github.com/Appsilon/mediforce/pull/776).
- Bootstrap now generates `REDIS_PASSWORD` — staging overlay requires `--requirepass` but the variable was missing, causing Redis to crash-loop on fresh deployments [#777](https://github.com/Appsilon/mediforce/pull/777).
- `repo` → `external_skills_repo` migration applies on staging again — the previous `0026`/`0027` pair carried an out-of-order journal `when` timestamp (2025-06-19) that drizzle-kit silently skips on any DB already past it, leaving the new column uncreated and every workflow listing empty. Merged both into a single `0026_external_skills_repo` migration (column add + `repo` copy + invalid-value repair) with a correct timestamp.
- Image-less build-mode workflow agents now resolve their derived Docker tag before choosing execution mode, so `dockerfile`/`repo`/`commit` agents run in containers instead of falling back to local host execution.
- Workflow editor agent image fields keep custom Docker tag entry available even when discovered local Docker images are listed.
- E2E test setup deletes only the workspace handles it owns (`test`, `tenant-a`, `tenant-b`, and journey-specific handles) and resets seeded agent definitions — personal namespaces and their registered workflows are no longer wiped when running `pnpm test:e2e`, while interrupted MCP-binding journeys no longer dirty the next run.
- `datetime` params now store UTC ISO strings — scheduled wait steps fire at the user's intended local time instead of being off by the browser-to-UTC offset; the datetime field shows the detected browser timezone (e.g. "Europe/Warsaw (UTC+2)") so users can see exactly what time the server will act on.
- Runs stuck at a wait step with no recorded `pauseReason` are now surfaced as errors instead of silently stalling forever: the auto-runner verifies the `waiting_for_timer` write after it commits and escalates to `failed` if the write was lost; the heartbeat sweeps and escalates any surviving `paused/null` orphans; the run view shows an actionable message ("Resume this run to restart from the current step") instead of "Workflow stopped unexpectedly".
- Staging deploys now pick up each push to main — `build-images.yml` now builds native `linux/amd64 + linux/arm64` images via a matrix job on GitHub-hosted ARM64 runners; the Hetzner ARM64 staging server was silently falling back to a stale local `be65eae4` build because the amd64-only CI images had no matching manifest for `arm64/v8`; deploy script pull errors are now visible instead of swallowed [#740](https://github.com/Appsilon/mediforce/pull/740).
- `scripts/sync-model-rankings.py` now uses OpenRouter's frontend rankings JSON endpoint instead of a brittle private server-action scrape, restoring local model ranking sync.
- On-demand preview deploys now work: `/deploy` on a PR runs a real `vercel build` + `vercel deploy --prebuilt` via the Vercel CLI, replacing the empty-commit `[preview]`-marker hack that silently no-op'd.
- Execution history panel now shows all iterations of looped steps — steps that completed before the current loop revisit (e.g. a timer-wait that ran between two visits to the same decision gate) were previously hidden or incorrectly marked pending due to the steps API returning only the latest execution per step and using a positional definition-order algorithm for status derivation.

## [2026-06-14]

### Added
- The "Before you start" pre-flight dialog now shows actionable resolution paths per warning instead of a static hint: missing-image warnings link to "Configure build source" (workflow editor), "Build manually" (Docker setup tutorial), and "Contact admin" (mailto: to the namespace owner); missing-secret warnings deep-link directly to the Secrets panel for the affected key. Backed by a new `GET /api/namespaces/:handle/admin-contact` endpoint that resolves the earliest owner's email [#312](https://github.com/Appsilon/mediforce/pull/312).
- **Task view** — spreadsheet-style table replaces card grid; status column, clickable run links, bulk cancel via batch endpoint, hide-completed toggle
- **Process card** — step-progress dots rendered against the run's actual definition version instead of the latest definition

### Changed
- **StepExecutor strategy pattern (ADR-0008)** — agent and script steps now execute through separate `StepExecutor` strategies (`AgentStepExecutor`, `ScriptStepExecutor`) instead of a monolithic `AgentRunner` with `isScript` branches. Each strategy owns its full lifecycle (run, audit, advance/review, cost tracking). `AgentPlugin` interface renamed to `StepExecutorPlugin` to reflect that scripts and Databricks jobs are peers, not special-cased agents:
  - `StepOutputEnvelope` base schema in platform-core [#688](https://github.com/Appsilon/mediforce/pull/688)
  - `PluginRunner` extracted from `AgentRunner` [#691](https://github.com/Appsilon/mediforce/pull/691)
  - `StepExecutor` interface + `AgentStepExecutor` + `ScriptStepExecutor` [#692](https://github.com/Appsilon/mediforce/pull/692)
  - `AgentPlugin` → `StepExecutorPlugin` rename [#694](https://github.com/Appsilon/mediforce/pull/694)

## [2026-06-07]

### Added
- `databricks-job` plugin — workflow steps can trigger an existing Databricks job (`jobs/run-now` + polling) and route transitions on the JSON the notebook exits with; secrets `DATABRICKS_HOST`/`DATABRICKS_TOKEN`, `${steps.*}` param interpolation, single-task jobs in v1. Includes `scripts/databricks-spike.py` for manual verification against a real workspace.
- Agent-produced files are no longer lost — everything an agent step leaves in `/output` is preserved as **Output Files** on the run branch (`.mediforce/output/<stepId>/`, ADR-0015) and is reviewable and downloadable:
  - API: `GET /api/runs/<runId>/files` lists a run's files; `GET /api/runs/<runId>/files/<path>` serves the bytes (binary-safe, RFC 6266 attachment); `mediforce.runs.listOutputFiles` / `downloadOutputFile` client methods.
  - UI: "Files" card on the run detail page (grouped by step, hidden when empty) + per-step paperclip chips expanding to that step's files.
  - CLI: `mediforce run files <runId>` and `mediforce run download <runId> [path] [-o dir]`.
- **OpenTelemetry tracing for agent runs (ADR-0007 layer 1)** — `agent-runtime` emits OTel spans: `mediforce.agent.run` (root, with full workflow correlation attributes: namespace, workflow name+version, stepId, agentRunId, processInstanceId, autonomy level) and `openrouter.chat.completion` (GenAI semantic conventions: request/response model, token usage). Content captured only when `MEDIFORCE_OTEL_CAPTURE_CONTENT=true`: step input / envelope result on agent-run spans (OpenInference `input.value`/`output.value`, rendered by Phoenix), prompt/completion text on LLM spans. Export is opt-in via `OTEL_EXPORTER_OTLP_ENDPOINT` (registered in platform-ui instrumentation); only `@mediforce/*` spans are exported (Next.js auto-instrumentation noise filtered out, `MEDIFORCE_OTEL_EXPORT_ALL_SPANS=true` to keep it); Phoenix added to `docker-compose.yml` as the dev trace viewer on `:6006`.
- **Dry run mode** — workflow runs can be tagged as dry runs for testing. Full execution with a `dryRun` flag, spawned children inherit it. CLI: `run start --dry-run`, `run watch`, `run logs`, `task complete`. UI: violet badge on run list/detail, All/Production/Dry Runs filter, "Dry Run" button in start-run dialog.
- **Dry run feedback loop in Workflow Designer** — the cowork agent can now test workflow definitions mid-conversation via 6 new MCP tools (`dry_run_workflow`, `get_run_status`, `list_run_tasks`, `complete_task`, `get_run_logs`, `list_models`). Agent starts a dry run, polls progress, pauses on human steps with a proposed payload, analyzes results, and iterates. Namespace auto-injected from run context [#684](https://github.com/Appsilon/mediforce/pull/684).
- ADR-0007: LLM evaluation & observability — four-layer model (traces → scores → eval datasets → eval runs), hybrid system of record (traces external via OpenTelemetry GenAI conventions, scores/datasets/eval runs as platform entities), Agent Run as the unit of evaluation, per-deployment content-capture switch. Glossary gains Trace / Score / Eval Dataset / Eval Run.
- `verdict-with-params` task kind lets a single human step collect structured param values and a verdict together — previously required two separate steps [#658](https://github.com/Appsilon/mediforce/pull/658). Includes `ParamVerdictView` component and `datetime` param type support.
- **Merged workflow designer** — consolidated `workflow-designer` (v21), `workflow-designer-2` (v3), and `cowork-workflow-designer` (v5) into a single cowork-based `workflow-designer` with create/edit mode support, live validation, HTML diagram previews, and a rich system prompt covering the full WorkflowDefinition schema.
  - Cowork sessions now validate artifacts live on every `update_artifact` call (wires up the previously unused `validateOutputSchema`), with results shown in the artifact panel and enforced as a gate on finalize.
  - New `update_presentation` built-in tool lets cowork agents push HTML previews rendered in a sandboxed iframe tab alongside the JSON tree explorer.
  - `platform-mcp` stdio server exposes `render_workflow_diagram` — a deterministic HTML renderer matching the platform's visual language (step type colors, executor badges, verdict pills).
  - `POST /api/render/workflow-diagram` REST endpoint (same renderer).
  - Artifact panel: collapsible JSON tree explorer replaces raw `JSON.stringify`; tabbed Data/Preview view.
  - Built-in tool calls (`update_artifact`, `update_presentation`) now persist as live tool turns visible in the cowork chat UI.
  - Postgres migration 0018: `validation_result` (jsonb) + `presentation` (text) columns on `cowork_sessions`.
### Added
- Workflows can now be imported from a public GitHub repo as a one-time copy: `mediforce workflow import --repo <url> --path <wd.json-path> --namespace <ns>` CLI command, a manifest-browser dialog in the workspace toolbar (fetches `index.json`, multi-select, progress bar), and `POST /api/workflow-definitions/import` + `GET /api/workflow-definitions/manifest` server-side handlers — imported definitions carry `source.{repo,path,ref}` provenance metadata [#561](https://github.com/Appsilon/mediforce/issues/561).

### Changed
- **Breaking (schema + data migration 0023):** deterministic script-step config moved from `step.agent` to a typed `step.script` key; `agent`/`autonomyLevel`/`cowork` are now rejected on `executor: 'script'` steps and audit events record `executorType: 'script'` (forward-only). Stored workflow definitions are rewritten in-place by Postgres migration `0023_script_step_config` — it must ship in the same release, because definitions are re-parsed on read.
### Fixed
- Script container `runtime: "python"` now invokes `python3` — previously failed on golden image (and any image without a `python` symlink) with `exec: "python": not found` [#675](https://github.com/Appsilon/mediforce/pull/675).
- Queued (BullMQ) Docker execution no longer corrupts binary files (PDF, XLSX, ZIP) and now preserves nested output directories — file payloads cross Redis as base64 keyed by POSIX relative path, matching local-mode behaviour.
- **Run cost under-reported for cached agent runs** — the container-agent token extractor read only `input_tokens`/`output_tokens` from the CLI result event and dropped `cache_read_input_tokens` and `cache_creation_input_tokens`, so prompt-cached runs (where cache reads dominate input) showed costs many times lower than OpenRouter actually charged. Cache-creation tokens now fold into `inputTokens` and cache-read tokens are tracked as `cachedInputTokens`, priced at the registry `cacheRead` rate (falling back to the input rate). (#654)
- Completed param-only tasks (e.g. data-entry steps with no verdicts) no longer show a "Waiting for step output... Verdict buttons will be enabled..." box — that message only made sense for review steps, and the panel now renders nothing when the previous step produced no output to show [#672](https://github.com/Appsilon/mediforce/pull/672).

### Changed
- Human steps in the run view are now first-class: a waiting step shows the full task UI (verdict buttons, params, claim/access banner); a completed human step shows the read-only task body alongside execution input/output — the separate task detail page is gone. Task inbox renamed "New actions" → "Human actions"; all inbox links point directly to the step view. `/tasks/{id}` deep-links (emails, bookmarks) redirect transparently to the owning step.
- GETTING-STARTED now covers two first-run blockers that were previously only discoverable by hitting the error: setting `MEDIFORCE_API_KEY` for the CLI (must match `PLATFORM_API_KEY`), and building local `script`-executor images (`mediforce-golden-image`, `mediforce-node`) via `scripts/rebuild-docker-images.sh` before running workflows with `script` steps [#670](https://github.com/Appsilon/mediforce/pull/670).
- Deploys can no longer leave orphaned containers behind — `deploy.sh` and `deploy-staging.sh` pass `--remove-orphans` to `docker compose up`, so containers from since-removed compose services die on deploy instead of silently consuming shared queues with weeks-old code (root cause of the staging incident where a stale agent worker wrote base64 `script.mjs` payloads) [#682](https://github.com/Appsilon/mediforce/pull/682).

## [2026-05-31]

### Changed
- `pnpm dev` / `pnpm dev:queue` now preflight infra via `scripts/dev-infra.py`: it checks Docker is installed, the daemon is running, and the Compose v2 plugin exists, then brings up Postgres (and Redis) with `--wait` so the DB is healthy before migrations run. Missing prerequisites now stop with an actionable message (e.g. `sudo apt install docker-compose-v2`) instead of silently booting the app against a non-existent database and surfacing `ECONNREFUSED 127.0.0.1:5432` mid-request. Prerequisites + troubleshooting documented in GETTING-STARTED.md / dev-quickref.
- Removed redundant `dev:postgres` script (use `pnpm dev`); `dev:no-docker` now sets `DATABASE_URL` so it boots post-#534; doc cleanup (ADR-0001 Accepted, `STORAGE_BACKEND` refs scrubbed, headless-migration docs marked Completed).

### Fixed
- Workspace-selection page no longer renders a blank picker when `/api/users/me` fails — `useAllUserNamespaces` surfaces `isError`/`error` and the page shows an explicit error (with a Try-again) for an errored or empty bundle, redirects straight through for a single workspace, and only shows the picker for two or more. Previously a 500 was swallowed into an empty namespace list and rendered as "Choose a workspace" with no cards.
- Forced password-change, workspace bio/profile edits, agent MCP-binding writes, and namespace member/delete mutations no longer 500 — their audit events now carry an FK-valid `workspace` (the acting/affected namespace) instead of throwing against the Postgres NOT NULL column; `namespace.deleted` emits before the cascade and is anchored to the owner's surviving personal namespace. [#534](https://github.com/Appsilon/mediforce/pull/534)
- Cutover script maps agent-run `duration_ms` + token counts from the correct envelope keys (`duration_ms`, `tokenUsage.{inputTokens,outputTokens}`), so migrated runs keep their duration/token data. [#534](https://github.com/Appsilon/mediforce/pull/534)
- Run History / `mediforce agent-run list` no longer 400 on null-envelope runs stored as `'{}'` instead of SQL NULL (read tolerates it, write stores NULL, migration `0015` backfills). [#534](https://github.com/Appsilon/mediforce/pull/534)
- Soft-deleted workflows no longer leak into the catalog list (regression from the Firestore→REST cutover dropping the UI's `!deleted` filter). [#534](https://github.com/Appsilon/mediforce/pull/534)
- `PostgresAgentRunRepository.create()` now upserts (`onConflictDoUpdate`) on a repeated run id — the runner writes the same id twice (running → terminal) and the plain insert threw `agent_runs_pkey`, a regression from Firestore `.set()`. [#617](https://github.com/Appsilon/mediforce/issues/617)
- Staging deploy broke after [#572](https://github.com/Appsilon/mediforce/pull/572): the `migrate` init-container stage derived `FROM builder`, so its arg-less build re-ran `next build` whose `/workspaces/new` prerender failed with `auth/invalid-api-key` (Firebase build args reach only the `platform-ui` service). Migrate stage now derives from `deps` + source, skipping the Next build entirely.
- Flaky `forced-password-change.journey.ts` ([#578](https://github.com/Appsilon/mediforce/pull/578)) — `updatePassword` revokes the session's ID token, so the forced-reset flow now re-authenticates and `clearMustChangePassword` retries on a revoked-token 401 (forcing a token refresh) before clearing the gate, instead of letting the stale-token 401 fail the gate closed and bounce the user back to `/change-password`.
- Polling hooks now stop on 4xx (`useProcessInstances`, the task queues, and the cowork-session list/by-instance hooks) via a shared `stopRetryOn4xx` util and a `refetchInterval` error gate — previously a 403/404 caused runaway API/Postgres load on every tick, burning budget (post-#591 cost regression).
- `useProcessNameMap` now scopes its `runs.list` poll to the active workspace handle (`namespace: handle`) instead of fetching across every visible workspace every 5 s — a cross-workspace data-scope leak.
- `useAgentEvents` polls incrementally via a new `afterSequence` cursor on `processes.agentEvents` instead of re-reading the entire `agentEvents` subcollection every 1.5 s — the Phase-4 cutover lost `onSnapshot` deltas and turned a chatty open run into ~20k Firestore reads/min.
- Moved `validateEnv` (and its `process.exit`) into a node-only `instrumentation-node.ts`, dynamic-imported from the `NEXT_RUNTIME === 'nodejs'` branch, so Turbopack no longer parses `process.exit` for the Edge build — silences the `process.exit ... not supported in the Edge Runtime` warning that flooded the dev log on every compile.
- `StepParamSchema.type` widened from a strict enum to `z.string().min(1).default('string')` so human-task GETs no longer 400 when the param uses a widget hint the UI already renders (e.g. `textarea`, `multiselect`). Non-string `type` still fails parsing — only the enum was the bug. Route adapter also `console.error`s handler ZodErrors so similar mismatches are debuggable from the server log.
- `CoworkSession` schema now defaults `agent`→`chat` and `voiceConfig`→`null` so legacy chat-only sessions (predating the voice fields) survive the cutover's full-scan Zod reads instead of 400ing the whole `cowork.list`. `findMostRecentActive` drops its `createdAt` orderBy (picks the latest in memory) so the by-instance lookup no longer needs an undeployed composite index — fixes the 500 that hid the run page's "Open cowork" button.

### Changed
- ADR-0001 final cutover — `users/{uid}` profile migrated to Postgres `user_profiles` (`mustChangePassword` only); invite + forced-password writes now route through `PostgresUserProfileRepository`. [#534](https://github.com/Appsilon/mediforce/pull/534)
- Firestore fully removed — only Firebase Auth + Storage remain. Dropped the Firestore repos, admin/client handles, rules/indexes, and the emulator from every config. [#534](https://github.com/Appsilon/mediforce/pull/534)
- Plain `pnpm dev` now boots the full local stack (Postgres up + migrate + UI); `dev:local` removed, `dev:no-docker` stays docker-free, `dev:queue` opts into Redis/BullMQ.
- Postgres repo row-mappers strip nullable columns once at the boundary via new `compact()`/`parseRow()` helpers in `platform-core` instead of per-field conditional spreads. [#614](https://github.com/Appsilon/mediforce/issues/614)
- `useProcessNameMap` now reads a projected `GET /api/runs/names` (`mediforce.runs.listNames`, `{ id, definitionName }` only) on a 30 s cadence instead of fetching up to 10k full `ProcessInstance` docs every 5 s — cuts the ~24 s/request payload bloat ([#588](https://github.com/Appsilon/mediforce/issues/588)).
- Workspace-home run counts + 3-run preview now come from a server-side `runSummary` on `workflows.list` instead of polling up to 10k runs every 5 s and grouping client-side; `includeCompletedRuns` mirrors the "show completed" toggle.
- Phase 4 done ([PR-final #591](https://github.com/Appsilon/mediforce/pull/591)) — zero `firebase/firestore` in `platform-ui` (`api-boundaries.test.ts` tripwire); PG PR2 ([#534](https://github.com/Appsilon/mediforce/pull/534)) unblocked.
- ADR-0001 — server data layer migrated to Postgres; Firestore admin SDK deleted (14 repos, L2 parity + L3 API E2E). `STORAGE_BACKEND` flag removed (Postgres is the only backend). Cutover script + verifier + operator checklist in `scripts/migrate-firestore-to-postgres/`. [#534](https://github.com/Appsilon/mediforce/pull/534).
- Dev ergonomics + Vercel preview gating ([#585](https://github.com/Appsilon/mediforce/pull/585)) — `pnpm dev` honors `PORT`, E2E scripts honor `E2E_PORT` (defaults `9003` / `9007` unchanged), so two worktrees can run side-by-side without colliding. Root `dev*` scripts auto-run `pnpm install --prefer-offline --silent` (~2s no-op when up to date) so fresh worktrees boot without remembering to install. Vercel preview builds now skip on every non-main push; trigger one on demand by posting `/deploy-preview` on a PR — a workflow pushes an empty `[preview]` commit that the new `vercel.json` ignoreCommand allow-lists. Sidebar SHA badge picks up the branch name on dev (cherry-picked from [#583](https://github.com/Appsilon/mediforce/pull/583)).
- ADR-0001: Drizzle migrations run as a compose init container (`migrate` service, build target `migrate`) instead of an in-app boot step. Drops `packages/platform-infra/scripts/migrate.mjs` and the `outputFileTracingIncludes` workaround in `packages/platform-ui/next.config.mjs` (PR #570 hotfix for staging `ERR_MODULE_NOT_FOUND`).
- Phase 4 PR5 — cowork session + chat moved to react-query: `[handle]/cowork/[sessionId]/page.tsx` no longer imports `firebase/firestore`; chat send is a `useMutation` with optimistic prepend, cancellation-race protection, and a 1 s polling cadence flip while in-flight. `ChatCoworkSessionOutput` gains additive `session` + `turns` fields so the UI can replace optimistic state without a follow-up GET. New `mediforce cowork {get, get-by-instance, chat}` CLI subcommands.
- Phase 4 PR4.5 — firestore residual sweep so PR-final is delete-only. New `UserProfileRepository` (`scope.userProfiles`) reading/writing `users/{uid}.mustChangePassword`; `GetMeOutput.user` extended with `mustChangePassword: boolean` (default `false`); `POST /api/users/me/clear-must-change-password` + `mediforce users clear-must-change-password` CLI (audit `user.password_change_acknowledged`). Five settings-page mutations migrated: `PATCH /api/namespaces/:handle` (`namespace.updated`; `bio` is two-state — `undefined` leaves it untouched, any string overwrites incl. empty), `DELETE /api/namespaces/:handle` (`namespace.deleted`, cascade via `NamespaceRepository.deleteNamespaceCascade`), `POST /api/namespaces/:handle/leave` (`namespace.member_left`; owner blocked → `precondition_failed`), `DELETE /api/namespaces/:handle/members/:uid` (`namespace.member_removed`, atomic via `removeMemberWithOrganizations`), `PATCH /api/namespaces/:handle/members/:uid` (`namespace.member_role_changed`, owner-only). `auth-context.tsx`, `app/(app)/[handle]/settings/page.tsx`, and the namespace/user sections of `app/(app)/[handle]/page.tsx` rewired through `useUserMe` / `useNamespace` / the new `use-namespace-mutations` hooks. New CLI: `mediforce namespace update|delete|leave|remove-member|set-member-role`. Personal-namespace pages now treat the org-list panel + member-avatar profile links as owner-only (was: visible to any signed-in viewer) — deliberate privacy tightening.
- Phase 4 PR4 — namespaces + identity bundle migrated off `firebase/firestore`. New `GET /api/users/me` (lazy personal-namespace bootstrap + `user.personal_namespace_created` audit), `GET /api/namespaces/:handle`, `POST /api/namespaces` (`namespace.created` audit, atomic via `NamespaceRepository.createNamespaceWithOwner`). `auth-context.tsx`, `use-namespace`, `use-namespace-role`, `use-all-user-namespaces` rewired (selectors over `useUserMe()`); `use-user-namespace.ts` deleted in favour of `usePersonalNamespace`. `workspaces/new` uses `useMutation` with list-affecting optimistic prepend; `[handle]/settings` drops the members `onSnapshot`. New `HandleSchema` write-boundary constant. New CLI: `mediforce users me`, `mediforce namespace get|create`.
- Phase 4 PR2 — agent-runs + monitoring migrated off `firebase/firestore`. New headless endpoints `GET /api/agent-runs` (cursor-paginated, `runId`/`stepId` filters), `GET /api/agent-runs/:agentRunId`, `GET /api/namespaces/:handle/monitoring/summary` (all-time `running` / `paused` / `completed` / `failed` per status, mirroring the pre-PR2 `useMonitoringData` hook this endpoint replaced). `useAgentRuns` / `useAgentRunsForStep` / `useAgentRun` rewired to react-query (STANDARD LIVE 5 s, CRITICAL LIVE 1.5 s for detail); `useMonitoringSummary` runs NICE LIVE 30 s with focus-refetch. New `mediforce agent-run` CLI subcommand.
  - Agent-run workspace gating bypassed for parity with the pre-PR2 Firestore subscription; per-row gate restore tracked in [#588](https://github.com/Appsilon/mediforce/issues/588).
  - `FirestoreProcessInstanceRepository.listAll` skips per-row parse so legacy docs no longer 400 the monitoring summary.
  - Monitoring summary perf: `ListInstancesOptions.namespace` pushes the workspace filter into Firestore (new `(namespace, deleted, createdAt DESC)` composite index), and a bulk `HumanTaskRepository.getByInstanceIds` collapses the per-run tasks N+1 into one chunked `where('processInstanceId','in', …)` fan-out. ~11 s → ~1.7 s on a 113-run workspace in dev.
- Phase 4 PR3 — runs / processes / audit domains migrated to react-query (ADR-0006); `mediforce.tasks.list` gains caller-scope axis (no params = my workspace queue); `runs.list` adds a `namespace` filter and now returns the full `ProcessInstance` shape (PRD §9 read-path convergence).
- Phase 4 PR1 — react-query foundation (ADR-0006) + tasks domain migrated off `firebase/firestore` (task detail page, claim button optimistic, role-scoped queues). New `mediforce task` CLI subcommand.
- ADR-0005 Phase 2.5 — 18 endpoints (workflow-definition mutations, agents, secret read companions, processes archive/bulk) migrated to headless handlers; `actions/processes.ts` and `actions/definitions.ts` deleted. `transferWorkflow` now gates both source and target namespace, `deleteWorkflow` audit actor fixed, and archive / setDefault / register / copy / setVisibility now emit audit events.
- ADR-0005 Phase 2.5 — workflow-secret value reveal + atomic bulk save migrated to headless handlers (`getWorkflowSecretsFull`, `saveWorkflowSecrets`) behind `GET`/`PUT /api/workflow-secrets/values`; `getWorkflowSecrets` / `saveWorkflowSecrets` Server Actions deleted; UI uses `mediforce.workflowSecrets.values()` / `.save()`; new audit actions `workflow_secret.values_revealed` and `workflow_secret.bulk_saved`.
- ADR-0005 Phase 2.6 — six admin/users endpoints (oauth-providers, tool-catalog, docker-images, users/members, users/invite, users/resend-invite) migrated to headless handlers; role-gate plumbing + `PLATFORM_*_API_KEY` collapsed (closes [#218](https://github.com/Appsilon/mediforce/issues/218)).
- ADR-0005 Phase 3.1 — cowork endpoints migrated to headless handlers; `app/actions/cowork.ts` and the dead `/message` SSE route deleted; streaming overhaul deferred to [#516](https://github.com/Appsilon/mediforce/issues/516).
- Decompose `tasks/complete-task.ts` ([#528](https://github.com/Appsilon/mediforce/issues/528)) — Phase 3 god-function split into helpers; main handler reads as short recipe.
- `packages/platform-ui/src/middleware.ts` renamed to `proxy.ts` (export `middleware` → `proxy`) per Next.js 16's [middleware-to-proxy](https://nextjs.org/docs/messages/middleware-to-proxy) convention. Silences the `⚠ The "middleware" file convention is deprecated` warning that fired on every dev compile. Matcher and auth behaviour unchanged; integration tests renamed in lockstep.
- ADR-0005 Phase 3 — kick-driven mutations migrated to headless handlers. `tasks/complete` (5-variant discriminated union body: `kind` ∈ `verdict | params | upload | assignment | rows`), `processes/resume`, `processes/:id/steps/:stepId/retry`, `cron/heartbeat`, and `POST /api/processes` now all run as `(input, scope) => output` handlers. Heavy `lib/resolve-task.ts` body (481 LOC) folded into a new `WorkflowEngine.completeHumanTask` so the handler stays cancel-run-shaped (workspace gate → engine call → audit + kick). New `RunKicker` abstraction (`scope.system.runKicker`) replaces eight inline `fetch('/api/processes/:id/run')` self-fetches with a single prod impl (`createHttpSelfFetchRunKicker`) + a test spy (`noopRunKicker`). `POST /api/processes` response shape breaks from `{ instanceId, status }` to entity-echo `{ run: WorkflowRun }` per ADR-0005 §5; UI (`start-run-button.tsx`) + CLI (`mediforce run start`) + E2E suites updated in the same PR. Five Server Actions deleted (`completeTask` × 4 verdict/params/upload/assignment variants + `completeTableEditorTask`; `startWorkflowRun`, `resumeProcessRun`, `retryFailedStep`); `POST /api/tasks/:taskId/resolve` route deleted (UI-unused per audit). ADR-0005 §7 closed audit-action set extended with `instance.retried`, `cron.trigger.fired`, `process.resumed_after_task`. New CLI: typed client methods on `mediforce.tasks.complete`, `mediforce.runs.resume`, `mediforce.runs.retryStep`, `mediforce.cron.heartbeat`. Internal `processes/:id/advance` + `processes/:id/run` stay inline (future BullMQ executor ADR migrates the auto-runner loop).

### Added
- ADR-0001 tracer-bullet (Firestore → Postgres): `tool-catalog` is the first repository with a Postgres backend behind `STORAGE_BACKEND=postgres`. New `packages/platform-infra/src/postgres/` houses the Drizzle schema (`tool_catalog_entries` keyed on composite `(workspace, id)`), the `PostgresToolCatalogRepository` implementation, the forward-only SQL migration runner, and a shared `ToolCatalogRepository` contract that both the in-memory double and Postgres backend satisfy (`InMemoryToolCatalogRepository` now validates payloads via `ToolCatalogEntrySchema` for parity). `docker-compose.yml` ships a `postgres:16` service; CI gains a `postgres-tests` job that exercises the Postgres branch via a `services:` Postgres container. Firestore remains the default backend; the flag scopes to tool-catalog until each follow-up PR migrates its own repository.
- Pre-assigned human tasks ([#521](https://github.com/Appsilon/mediforce/issues/521)) — a `human` workflow step can declare `assignedTo` (e.g. `"${triggerPayload.userId}"`); the auto-runner interpolates it against the run's sources and creates the task `claimed` and pinned to that `assignedUserId` instead of open to the whole role. An `assignedTo` that resolves to nothing fails the run rather than silently falling back to an open task; `assignedTo` on a non-human step is rejected at WD validation. The personal task queue (`useMyTasks`) now scopes assigned tasks to their owner — unassigned tasks stay role-wide.
- Workflow `wait` action kind ([#521](https://github.com/Appsilon/mediforce/issues/521)) — pauses a run for a fixed `duration` or until a `deadline`, with an optional early-exit `condition`. The handler returns a `__wait` sentinel that the auto-runner turns into a `paused`/`waiting_for_timer` instance; the `cron/heartbeat` sweep and `POST /api/processes/:id/resume-wait` resume eligible runs. The wait step execution follows a `running → paused → completed` lifecycle — its resolved `{ resumeReason, waitedSeconds, resolvedAt }` output is written once on resume, never the raw sentinel.
- ADR-0001 tracer-bullet (Firestore → Postgres): `tool-catalog` is the first repository with a Postgres backend behind `STORAGE_BACKEND=postgres`. New `packages/platform-infra/src/postgres/` houses the Drizzle schema (`tool_catalog_entries` keyed on composite `(workspace, id)`), the `PostgresToolCatalogRepository` implementation, the forward-only SQL migration runner, and a shared `ToolCatalogRepository` contract that both the in-memory double and Postgres backend satisfy (`InMemoryToolCatalogRepository` now validates payloads via `ToolCatalogEntrySchema` for parity). `docker-compose.yml` ships a `postgres:16` service; CI gains a `postgres-tests` job that exercises the Postgres branch via a `services:` Postgres container. Firestore remains the default backend; the flag scopes to tool-catalog until each follow-up PR migrates its own repository.
- Backlog triage workflow (`apps/backlog-triage/`) — fetches open GitHub issues, an LLM suggests assignments (human or AI workflow) with priority and rationale, a triager reviews and edits in a table, then dispatch PATCHes GitHub and POSTs the Mediforce process-start API for agent delegations.
- `assignment-table` human-task UI for many-to-one item assignment with per-row assignee/priority/note. Items flow via `task.options` from the previous step; the assignee allowlist and labels live in `task.ui.config`.
- Generic `table-editor` human-task UI ([#469](https://github.com/Appsilon/mediforce/issues/469)) — schema-driven columns (`static` | `single-select` | `multi-select` | `text`) declared in `task.ui.config.columns`, one form per `task.options[i]` row, pre-filled from `option.suggestion[columnId]`, with `allowEmpty:false` single-selects gating submit. Emits a stable `{ rows: [{ itemId, values: { <columnId>: <value> } }] }` map (`completeTableEditorTask` → `resolve-task`). `assignment-table` is now a thin back-compat shim that builds an equivalent column config internally, so existing workflows keep working with zero WD changes.
  - `backlog-triage` tags issues in-platform ([#469](https://github.com/Appsilon/mediforce/issues/469)): the `tag-issues` step now renders a `table-editor` (pick a category + priority per untagged issue) and a new `apply-tags` script POSTs the labels to GitHub — creating any label the repo lacks — looping `tag-issues → apply-tags → fetch-backlog` until every issue carries a category. Replaces the old "open each issue on GitHub, tag it, come back and re-check" round-trip; `check-tags` now hands the untagged issues straight to the table instead of writing a markdown checklist.
- Markdown presentation support for script/agent steps ([#468](https://github.com/Appsilon/mediforce/pull/468)). Scripts and agents can now write `/output/presentation.md` instead of (or alongside) `/output/presentation.html`; the platform reads MD first and renders it via `react-markdown` + `remark-gfm` + `rehype-sanitize` inside Tailwind `prose` typography. Cuts inline-script preview boilerplate from ~20 lines of hand-rolled HTML and inline styles to a 1-line bullet template, and unifies typography across workflows. Legacy raw-string `presentation` envelopes auto-coerce to `{kind:'html', content}` — no Firestore migration. Iframe sandbox stays for the cases that genuinely need JS / interactivity. `apps/backlog-triage` `check-tags` migrated as the reference example.

- ADR-0004 + PLAN-0004 implementation (Phase 1.7 of the headless-API migration): scoped data-access authorization wrapper layer in `packages/platform-api/src/repositories/`. Every API handler now accepts a `CallerScope` instead of raw repositories; 13 `Authorized<Entity>Repository` wrappers enforce workspace membership + WorkflowDefinition visibility on every read/write. Soft-delete filtering stays at the storage layer (lands with ADR-0001's `WorkspaceScopedRepository`). The 10 Phase 1 GET handlers from [#450](https://github.com/Appsilon/mediforce/pull/450) lose their inline `callerCanAccess` guards (now in the wrapper). The static guard becomes `no-raw-repo-imports.test.ts` (structural — handlers cannot import raw repositories); `auth-coverage.test.ts` removed ([#458](https://github.com/Appsilon/mediforce/pull/458), [#463](https://github.com/Appsilon/mediforce/pull/463)).
  - Code-review tightening on [#463](https://github.com/Appsilon/mediforce/pull/463): `AuthorizedWorkflowDefinitionRepository.getLatestVersion` is now visibility-gated (no foreign-namespace private-WD enumeration); `AuthorizedWorkflowRunRepository.update` asserts `namespace` immutable and blocks non-system `deleted` patches (the `getById` gate alone would let a member smuggle a run across workspaces); `getModel` throws `NotFoundError` instead of bare `Error` so the route adapter maps it to 404.
- `@mediforce/platform-api/repositories` subpath export: `CallerScope`, `createCallerScope`, `AuthorizedRepository` base, and per-entity wrappers.
- `@mediforce/platform-api/testing` subpath export: `createTestScope`, `userCaller` — builds a real `CallerScope` from in-memory repos for handler tests downstream of `platform-api`.

### Changed
- CLI migrated to the [`citty`](https://github.com/unjs/citty) library and a thin `defineCommand` wrapper in `packages/cli/src/define-command.ts`. Every subcommand (23 total) now declares its arg shape once; the wrapper handles `parseArgs`, `--help`, missing/extra positionals, `resolveConfig` + `Mediforce` client construction, `--json` mode, `formatCliError` envelope, and exit-code mapping. `cli.ts` dispatch is a small `TREE` map with auto-generated branch help (`mediforce workflow --help`); the long if-ladder is gone. Helpers: typed `enumArg(['a','b'] as const)` (narrows citty's `EnumArgDef.options: string[]` widening), `CommandFn` type, a TREE smoke test (`packages/cli/src/__tests__/cli-tree-smoke.test.ts`) that exercises `--help` on every leaf. Test phrasings updated to citty's native output (`USAGE …`, `Missing required positional argument: RUNID`, `Missing required argument: --workflow`) — strictly better than the historic hand-rolled strings. Net `-1726` LOC across `packages/cli/`. Adds `citty` dep, +25 tests, 183/183 green.
- ADR-0005 Phase 2 PR2 (closes Phase 2): `POST /api/processes/:instanceId/cancel` migrated to the headless handler shape. New `cancelRun` handler reuses `scope.runs.update()` (state-machine in handler, matches PR1's deviation); response is entity-echoed `{ run: WorkflowRun }` replacing the prior `{ instanceId, status }`. Contract symbols use `Run` vocabulary per ADR-0001 (`CancelRunInputSchema { runId }`); URL path keeps the legacy `processes/:instanceId` segment until a coordinated URL rename phase, the adapter translates `params.instanceId` → `runId`. `mediforce.runs.cancel({ runId, reason? })` available on the typed client + new CLI command `mediforce run cancel <runId> [--reason <text>]`. Audit emission preserved bit-for-bit via `scope.system.audit.append` bridge per ADR-0005 §7 (action `instance.cancelled` matches `workflow-engine`'s `instance.*` family and the legacy `bulkCancelProcessRuns` emit; a repo-wide `instance.*`/`process.*` → `run.*` rename ships as its own ADR pass). UI callers (`process-detail.tsx`, `agent-escalated-banner.tsx`) moved off the `cancelProcessRun` Server Action. Dead code removed: `cancelProcessRun` action, `UnclaimButton` component + `unclaimTask` action (zero callers in source). Complete/resume/resolve reclassified to Phase 3 because all three depend on the still-undecided orchestration-kick mechanism ([#499](https://github.com/Appsilon/mediforce/issues/499)).
- ADR-0005 Phase 2 PR1: typed error envelope + `tasks/claim` migration. `HandlerError` in `@mediforce/platform-api` now carries `code: ApiErrorCode` + `details?: unknown`; existing `NotFoundError`/`ForbiddenError` subclasses get the code stashed in their constructors, and `PreconditionFailedError` is added for the claim handler's state-machine throw. Other codes (unauthorized, validation, conflict, rate_limited) throw via the base `HandlerError` until a real throw site arrives. `createRouteAdapter` catches `HandlerError` once and emits the ADR-0005 §1 envelope `{ error: { code, message, details? } }` across every route (Phase 1 + 1.5 reads retroactively typed too). Client-side `ApiError` gains `code`/`details` fields parsed from the envelope (existing `status`/`message`/`body` unchanged). New `POST /api/tasks/:taskId/claim` handler (pure `(input, scope) => { task }`), new `loadOr404` helper, new `mediforce.tasks.claim()` client method. `claimTask` Server Action deleted; `claim-button.tsx` calls the typed client. Audit emission preserved bit-for-bit via `scope.system.audit.append({...})` (handler-resident bridge per ADR-0005 §7).
- Phase 1.5 of the headless-API migration: seven hybrid endpoints (`GET /api/runs`, `GET /api/runs/:runId`, `GET|PUT|DELETE /api/workflow-secrets`, `GET /api/system/docker-info`, `GET /api/system/openrouter-credits`) now compose through `createRouteAdapter` + scoped handlers in `@mediforce/platform-api/handlers`, eliminating their inline `callerCanAccess` guards. `GET /api/runs/:runId` switches from 403 → 404 for foreign-workspace ids (anti-enumeration), and `GET /api/workflow-secrets` soft-fails to `{keys: []}` instead of 403 for non-members. Docker-info stays `@public-handler` (every authenticated user fetches it). [#480](https://github.com/Appsilon/mediforce/issues/480).
- Human-task body rendering now routes through a small registry (`task-body-registry.tsx`) keyed on `ui.component`, with file-upload/selection/params/verdict extracted into focused view files. Adding a new task body kind is a one-line registry entry.
- Workflow engine copies `prevOutput.options` onto the next human task regardless of whether the next step declares `selection`, so non-selection components (like `assignment-table`) consume the items array. Selection-style steps keep the min-count validation; existing apps unaffected.
- Contract dedup + API naming alignment with CONTEXT.md. Deleted the redundant `packages/platform-api/src/contract/definitions.ts` introduced in [#450](https://github.com/Appsilon/mediforce/pull/450) — its `AgentDefinition*` / `WorkflowDefinition*` schemas duplicated `agents.ts` / `workflows.ts` shape-for-shape. SDK client `mediforce.agentDefinitions.*` and `mediforce.workflowDefinitions.*` namespaces dropped; `mediforce.agents.*` and `mediforce.workflows.*` are the only surface. Renamed `handlers/definitions/` → `handlers/workflows/` and handlers `listWorkflowDefinitions` / `getWorkflowDefinition` → `listWorkflows` / `getWorkflow`. Unified the URL surface: `/api/agent-definitions/*` (CRUD + `/:id/mcp-servers/*`) moved to `/api/agents/*` and now lives next to the existing `/api/agents/:id/oauth/*` subtree. CONTEXT.md canonicalises `Agent` (no "Definition" suffix), adds a `Workflow` umbrella entry around the existing `Workflow Definition`, reframes `Agent Run` as a workflow-execution concept, and adds an `Agent vs Agent Definition` flagged-ambiguity note that tracks the pending `AgentDefinitionSchema` / `AgentDefinitionRepository` symbol rename. ADR-0001 PLAN now proposes the Postgres table as `agents`; ADR-0004 carries a post-merge naming note. CLI commands, UI labels, route handler logic, and wire shapes all unchanged.
- `CallerIdentity` now carries `readonly isSystemActor: boolean`. The free function `isSystemActor(caller)` was replaced by reading `caller.isSystemActor` directly — one less import, future-proof against the planned `'apiKey'` discriminator rename ([#448](https://github.com/Appsilon/mediforce/issues/448)).
- Storage-layer authorization: raw repository interfaces split into system-actor and namespace-scoped variants (`listAll` / `listInNamespaces`, `getById` / `getByIdInNamespaces`, etc.). Wrappers in `platform-api/src/repositories/` become thin routers that pick the right method based on `caller.isSystemActor`. Filter logic moves from JavaScript wrappers to the storage layer, ready for ADR-0001's Postgres swap to push it down to SQL.
- Trivial GET handlers deleted in favour of two generic route helpers — `listAdapter` and `getByIdAdapter` in `@mediforce/platform-api/handlers`. `getTask`, `getProcess`, `getCoworkSession`, `listAgentDefinitions`, and `getAgentDefinition` no longer have handler files; the route wires the helper directly with the contract schema. Handlers now exist only where there is genuine logic (cross-entity load, role/ownership/state, shape transform, or `@public-handler`). ADR-0004 §Decision-10 codifies the rule.
- workflow-designer & workflow-designer-2 agentic steps (`generate-steps`, `generate-execution-proposals`) now run in the `mediforce-golden-image` Docker container; Claude Code credentials are routed through OpenRouter via the workflow `env` block, since Docker steps don't inherit the host environment and must declare credentials explicitly ([#492](https://github.com/Appsilon/mediforce/pull/492)).

### Removed
- Duplicate Python migration script `scripts/migrations/migrate_workflow_doc_ids.py` — superseded by the TypeScript `packages/platform-infra/scripts/migrate-workflow-namespacing.ts` which is the canonical version ([#424](https://github.com/Appsilon/mediforce/pull/424)).

### Added
- `packages/cli/src/__tests__/bin-shim.test.ts` — spawn-based smoke test that runs the real `bin/mediforce.cjs` shim through `tsx` and asserts exit codes + stderr/stdout for `--help`, a missing positional, and an unknown command. Catches regressions (ESM resolution, `tsx` loader, bin shim entry resolution) that the in-process `runCli` suite cannot.

### Fixed
- Script-container failures now surface the error the script wrote to `/output/result.json` (its `error`/`errors` field) when the container exits non-zero with no stdout/stderr — instead of only invocation metadata (`no stdout/stderr captured — image=…`). The reported reason is also written to the activity log the Step Log panel reads, so e.g. the workflow-designer `register`/`validate` steps now show the real cause ([#546](https://github.com/Appsilon/mediforce/pull/546); follow-up to [#538](https://github.com/Appsilon/mediforce/pull/538)).
- Workflow designers now default agent steps to the `mediforce-golden-image` Docker image. Generated `claude-code-agent` steps previously carried no `agent.image`, so a freshly designed workflow crashed on staging/production with "No Docker image configured" (imageless/local execution only works in dev with `ALLOW_LOCAL_AGENTS=true`). The `generate-execution-proposals` skills (`workflow-designer`, `workflow-designer-2`) and the `cowork` designer prompt now set the default image, omitting it only when the user explicitly asks for local execution.
- Script-container step failures now surface a usable reason instead of `Script container failed (exit code 1): no output`. When the container produces no stdout/stderr, the diagnostic (image, command, env var names — including the injected `RUN_ID`/`STEP_ID` and (for workflow runs) `MEDIFORCE_RUN_NAMESPACE` — and `input.json` size) is written into the activity log the Step Log panel reads, so a fast no-output failure no longer sticks on "Initializing log…". Container exit `code`/`signal` is emitted as a status event on every run, and the `exit code`/`killed by` formatting (with the SIGTERM→timeout hint) is now a shared `formatExitInfo` helper reused by the agent and script container plugins ([#538](https://github.com/Appsilon/mediforce/pull/538)).
- Workflow designer registers generated workflows in the run's own namespace instead of a hardcoded `default` org ([#536](https://github.com/Appsilon/mediforce/issues/536)). The `script-container` plugin now injects the run's namespace as `MEDIFORCE_RUN_NAMESPACE` (alongside `RUN_ID`/`STEP_ID`); all four designer register scripts read it. `workflow-designer` no longer falls back to `default`, the voice/cowork variants stop omitting the required `?namespace=` query param (which previously 400'd), and `workflow-designer-2` drops its `{{NAMESPACE}}` secret-template (which threw unless a per-namespace `NAMESPACE` secret was manually configured) in favour of the same auto-injected var.
- `backlog-triage` tagging loop no longer whack-a-moles or silently swallows label failures. `fetch-backlog` paginates past PRs to a stable working set of the newest `limit` real issues — a single PR-heavy page (GitHub's issues endpoint counts PRs against `per_page`) or a merge mid-session used to shift which untagged issues showed up each loop. And `apply-tags` now surfaces label failures instead of swallowing them: a `presentation.md` summary on partial failures, and on **total** failure (every row failed) it **fails the step** (`exit 1`) with the first error in the run log — so e.g. a `GITHUB_TOKEN` without label-write permission shows as a red run instead of an endless re-prompt; partial failures stay non-fatal.
- `check-gif-freshness.py` now evaluates the net PR diff vs base instead of summing per-commit diffs. Add-then-revert within the same PR no longer trips the GIF freshness check (regression surfaced on [#481](https://github.com/Appsilon/mediforce/pull/481)).
- PR #172 rebase polish: presentation-only agent outputs now render in review surfaces, report iframes escape stringify fallback data and block network exfiltration with CSP, and local script execution no longer inherits the full server environment.
- `pnpm dev:mock` now starts local Firebase emulators, seeds demo data, and then boots Next, so the browser no longer falls into offline Firestore/Auth mode on a fresh mock-dev run.
- Workspace `Runs` page no longer leaks runs from other namespaces the viewer is a member of — list is now filtered to the current `handle`, and `useProcessInstances` requires `namespace` so TypeScript catches future regressions compile-time ([#424](https://github.com/Appsilon/mediforce/pull/424), follow-ups tracked in [#447](https://github.com/Appsilon/mediforce/issues/447) and [#448](https://github.com/Appsilon/mediforce/issues/448)).
- Instance namespace backfill now resolves namespace per instance definition version instead of global latest-by-name, preventing cross-tenant run reassignment during migration ([#424](https://github.com/Appsilon/mediforce/pull/424)).
- Workflow definition storage now scopes definition and metadata keys by namespace, so tenants can register the same workflow name independently ([#424](https://github.com/Appsilon/mediforce/pull/424)).
  - Migration script splits the legacy global `workflowMeta/<name>` into one doc per owning namespace and strips `defaultVersion` for tenants that don't own that version, avoiding dangling defaults pointing at non-existent workflow definitions ([#424](https://github.com/Appsilon/mediforce/pull/424)).

## [2026-05-17]

### Added
- Phase 1 of the headless-API migration is done — 10 GET endpoints across tasks, processes, definitions, cowork and plugins now sit behind framework-free handlers in `@mediforce/platform-api`, with namespace policy enforced inside the handler (every handler signature requires a `CallerIdentity`, so a missing gate is a code-level decision in review, not a silent regression). Wraps `audit` response as `{ events }` and surfaces visibility-denied agent definitions as 404 (anti-enumeration). Supersedes [#256](https://github.com/Appsilon/mediforce/pull/256), which predated the namespace access control on `main` and would have regressed it.
- Workflows can now be moved between namespaces — copy + redirect lands you in the target tenant ([#359](https://github.com/Appsilon/mediforce/pull/359), [#370](https://github.com/Appsilon/mediforce/pull/370)).
- Workflow discovery by Docker image: `GET /api/workflows/by-image` — needed to scale step containers without grepping definitions ([#377](https://github.com/Appsilon/mediforce/pull/377)).
- Human review steps are no longer binary — each step declares its own verdicts and target transitions (e.g. approve / request changes / escalate), wired end-to-end from schema to UI ([#396](https://github.com/Appsilon/mediforce/pull/396)).
- SDTM rule migration workflow — Vedha's legacy rule definitions ported to CDISC SDTM via a dedicated Mediforce app ([#355](https://github.com/Appsilon/mediforce/pull/355)).
- Validation reports are now reproducible — landing-zone data-validator renders from a study-owned template instead of regenerating HTML each run, with an injection-demo variant for the propose-rules story ([#402](https://github.com/Appsilon/mediforce/pull/402), [#408](https://github.com/Appsilon/mediforce/pull/408)).
- Two ways to seed the Landing Zone demo on the same Hetzner SFTP host — `demo-console/` (FastAPI SPA, operator-facing) and `demo-uploader/` (Mediforce workflow with 8-verdict human review) ([#407](https://github.com/Appsilon/mediforce/pull/407)).

### Changed
- Cowork is now per-workspace billed — the OpenRouter key is read from workspace secrets instead of a global env var; chat textarea auto-grows to fit input ([#378](https://github.com/Appsilon/mediforce/pull/378), [#381](https://github.com/Appsilon/mediforce/pull/381), [#382](https://github.com/Appsilon/mediforce/pull/382)).
- Dev setup is no longer a footgun — `pnpm dev:mock` boots the app with mocks in one command, every script is named for what it does (`dev:test` → `dev:mock`, `dev:local` → `dev:no-docker`, `dev:ui:queue` → `dev:queue`), `pnpm test` runs the full pyramid instead of vitest only, and `bootstrap-dev.py` with its silent `OPENROUTER_API_KEY=fake-…` trap is gone in favour of a commented `.env.example` [#421](https://github.com/Appsilon/mediforce/pull/421).
- Agent output is now consistent across surfaces — step detail and human-task panel share one `AgentOutputDisplay`, so L2 auto-runner steps (e.g. `interpret-validation`) finally show their HTML report without needing an L3 review ([#409](https://github.com/Appsilon/mediforce/pull/409)).
- Tests now have a 5-level pyramid with API E2E as the explicit foundation — `e2e/api/` is a dedicated Playwright project running real Next + emulators over HTTP (no browser), separated from `e2e/ui/` (sparse multi-step user journeys only, not "is button visible"). Misleading legacy names cleaned up: `src/test/*.test.ts` → `src/test/integration/`, `e2e/api/` (tier-2 real-LLM) → `e2e/external/`, `e2e/journeys/` → `e2e/ui/` ([#413](https://github.com/Appsilon/mediforce/pull/413)).

### Fixed
- Agent report iframe no longer blows up its host panel — height is capped and `vh` classes inside the report are neutralised ([#392](https://github.com/Appsilon/mediforce/pull/392)).
- Mock dev workflows now run seeded agent steps through the mock Claude runtime even when their demo plugin ids are not registered, the model ranking sync helper falls back from `9003` to `9007` for `pnpm dev:mock`, root API/UI E2E wrappers pass Playwright project flags correctly, and `mediforce run start` can target a namespace.
- Staging step containers can finally see workspace files — the data dir is persisted at `/var/lib/mediforce` with an identical host bind so docker.sock-spawned containers share the same path ([#405](https://github.com/Appsilon/mediforce/pull/405)).
- CLI network failures now explain the unreachable API host, reason, and recovery hints instead of printing raw `TypeError: fetch failed` ([#397](https://github.com/Appsilon/mediforce/issues/397)).
  - Follow-up: local I/O failures (for example `EACCES`) are no longer mislabeled as API network errors and now keep their original actionable message ([#397](https://github.com/Appsilon/mediforce/issues/397)).
  - Follow-up: dual-stack fetch failures now classify `AggregateError.errors[]` causes, and staging/remote URLs no longer suggest starting a local dev server ([#397](https://github.com/Appsilon/mediforce/issues/397)).

### Dependencies
- tailwindcss v4.2.4 ([#374](https://github.com/Appsilon/mediforce/pull/374)), yaml v2.8.4 ([#371](https://github.com/Appsilon/mediforce/pull/371)), pnpm v10.33.2 ([#372](https://github.com/Appsilon/mediforce/pull/372)), fast-xml-parser v5.7.2 ([#375](https://github.com/Appsilon/mediforce/pull/375)), jsdom v29.1.1 ([#379](https://github.com/Appsilon/mediforce/pull/379)), lucide-react v1.14.0 ([#380](https://github.com/Appsilon/mediforce/pull/380)).

## [2026-05-10]

### Added
- Workflows now have per-namespace visibility and access control — no more leaking definitions across tenants ([#346](https://github.com/Appsilon/mediforce/pull/346)).
- Agents have public/private visibility — fixes the gap where staging couldn't see agents owned by other namespaces ([#353](https://github.com/Appsilon/mediforce/pull/353)).
- Per-step cost in dollars now surfaces in run + step lists, so you can see which step burned the budget ([#348](https://github.com/Appsilon/mediforce/pull/348)).
- Per-namespace OpenRouter credit tracking + `mediforce credits` CLI — each tenant sees its own balance ([#349](https://github.com/Appsilon/mediforce/pull/349)).
- Mediforce reviews its own PRs — `pr-reviewer-mediforce` workflow runs AGENTS.md-aware review on every PR ([#338](https://github.com/Appsilon/mediforce/pull/338)).

### Changed
- Public profile pages are safe for unauth viewers — member-only UI and secrets are hidden, avatar falls back to the linked user photo ([#350](https://github.com/Appsilon/mediforce/pull/350), [#354](https://github.com/Appsilon/mediforce/pull/354)).
- Public pages no longer need a Firestore client — they call the REST API directly, which unblocks SSR and avoids leaking client config ([#358](https://github.com/Appsilon/mediforce/pull/358)).
- AGENTS.md tightened around dogfooding, RED→GREEN, and self code review — fewer ways for agents to skip the safeguards ([#352](https://github.com/Appsilon/mediforce/pull/352), [#356](https://github.com/Appsilon/mediforce/pull/356)).
